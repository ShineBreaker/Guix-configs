# quick-access-terminal 初始会话：两个标签页
# 由 quick-access-terminal.conf 的 kitty_override startup_session 引用
# tmux/herdr 均为 attach-or-create 语义，关闭面板后会话在服务端持久

new_tab Quake·tmux
# 入口链（预检/--create 创建 → session 级 opt-in → follow → 纯 attach）已收敛
# 进 attach-entry，与 99-selector.fish 同一路径；attach 经 exec 收尾、退出码透传
launch sh -c 'exec ~/.config/tmux/scripts/attach-entry --create Quake'

new_tab Quake·herdr
launch herdr --session Quake
