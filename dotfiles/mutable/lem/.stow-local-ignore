# lem 运行时产物不部署（生成于 ~/.config/lem/ 真实目录侧，stow 不应看到）
# 防御性规则：即使误生成在源树也不入链接
^/logs
^/history
^/quicklisp
^/config\.lisp
# 仓库/开发元数据不部署：.gitignore 与 .agents/（WS 验证工具与 agent 工作产物），
# 均只在源树内有意义；~/.config/lem/.agents 侧是真实目录，非 stow 链接。
^/\.gitignore
^/\.agents
^/debug\.log
