#+TITLE: literal-config
#+AUTHOR: BrokenShine
#+DATE: 2026-07-04
#+PROPERTY: header-args:emacs-lisp :tangle main.el :lexical yes :mkdirp yes :noweb tangle
#+STARTUP: content
#+OPTIONS: toc:2

* 启动顺序与模块依赖速查
本文件含 8 个按顺序加载的配置域,以及本节、[[#agent-entry][Agent 修改入口]] 和末尾占位等非代码章节。模块依赖由文档顺序表达;同域内也不得把调用方移到定义方之前。

| 顺序 | 配置域             | 主要功能                                                                 | 必须已就绪的跨域接口                                                         |
|------+--------------------+--------------------------------------------------------------------------+------------------------------------------------------------------------------|
|    1 | [[#startup][启动与基础设施]] | 路径、外部命令、进程 helper、frame 生命周期、键位原语、基础行为          | 无                                                                           |
|    2 | [[#appearance][界面与外观]]   | Which-key 数据、右键菜单、帮助、modeline、tab-line、Git、主题与颜色方案 | =literal/add-frame-hook=                                                     |
|    3 | [[#editing][编辑与文本]]       | 通用编辑命令、DWIM、终端                                                   | Git 的窗口路由接口                                                           |
|    4 | [[#programming][编程与开发]]   | Tree-sitter、Eglot、Flycheck、格式化、语言模式                             | 外部命令缓存、界面刷新接口                                                   |
|    5 | [[#projects][项目与导航]]      | Projectile、目录与项目文件导航                                            | Git/display-buffer、终端                                                     |
|    6 | [[#org-knowledge][Org 与知识库]] | Org、Roam、Knowledge、agenote                                            | 路径常量、=literal/call-process=                                             |
|    7 | [[#keys-completion][键位与补全]] | Which-key 本体、直达键、前缀键、Vertico/Corfu                           | 前述所有交互命令、frame 生命周期                                             |
|    8 | [[#system-tools][系统工具与实验性]] | Guix、Dashboard、兼容性 workaround                                      | knowledge/help/color-scheme/frame;Dashboard 必须最后加载                    |

简化依赖图:

#+begin_example
startup -> appearance -> editing -> programming -> projects
        -> org-knowledge -> keys-completion -> system-tools/dashboard
#+end_example

* Agent 修改入口
:PROPERTIES:
:CUSTOM_ID: agent-entry
:END:

后续 Agent 无需阅读全文。先用仓库脚本获得动态索引,再只读取目标子树:

#+begin_example
scripts/configctl map                 # 功能 ID、行号、代码量、noweb ref
scripts/configctl show dashboard      # 只输出一个功能子树
scripts/configctl find 'eglot|flycheck'
scripts/configctl check               # 快速结构/语法/tangle 审计
scripts/configctl load                # tangle 后 batch load
#+end_example

| 修改目标             | 功能 ID / 入口                                                              | 直接依赖                                      |
|----------------------+-------------------------------------------------------------------------------+-----------------------------------------------|
| 启动路径与外部命令   | =bootstrap=                                                                   | 无                                            |
| 新 frame 行为        | =frame=                                                                       | bootstrap                                     |
| 主题、字体、深浅色   | =theme-fonts= / =color-scheme=                                                | frame                                         |
| Modeline / Tab-line  | =modeline= / =tab-line=                                                       | theme-fonts                                   |
| 编辑命令与终端       | =editing-dwim= / =terminal=                                                   | git-display                                   |
| LSP、诊断、格式化    | =programming-tools=                                                           | bootstrap、appearance                         |
| 项目导航             | =project-navigation=                                                          | terminal、git-display                         |
| Org / 知识库         | =org-core= / =knowledge= / =agenote=                                          | bootstrap、process-helper                     |
| 键位与补全           | =keybindings= / =completion=                                                  | 对应命令必须先定义                            |
| Dashboard            | =dashboard=                                                                   | knowledge、help、color-scheme、frame          |
| 临时兼容性处理       | =compatibility=                                                               | 使用方通过命名 noweb ref 在原加载位置展开     |

维护约定:

1. 功能章节使用稳定 =CUSTOM_ID= ;改名标题时不要改 ID。
2. 大模块拆成 =module/section= 命名片段,章节末尾只有一个组装块写入 =main.el= 。
3. 改动局限在一个功能子树;确有跨域接口变更时同步更新上方依赖表。
4. 新包仍需同步 Guix 的 =home-emacs-packages= ;仅重排/精简现有代码不需要改包清单。
5. 提交前至少运行 =scripts/configctl check= 与 =scripts/configctl load= 。

* 启动与基础设施
:PROPERTIES:
:CUSTOM_ID: startup
:END:
Emacs 冷启动的第一阶段:GC / 路径常量 / 外部命令缓存 / Frame 生命周期 / 键位辅助 / 包管理 / 基础行为。
本域代码必须最先执行——后续所有功能域都依赖这里定义的常量、命令缓存与 =literal/set-key= / =literal/add-frame-hook= 等基础原语。

** GC 与启动优化
:PROPERTIES:
:CUSTOM_ID: startup-gc
:END:
临时调高 GC 阈值以加速启动,启动完成后由 =gcmh-mode= 接管。

+ =user-emacs-directory= 由当前单 profile 直接指向本目录
+ =gc-cons-threshold= 启动期推到 =most-positive-fixnum= ,启动完成后由 =gcmh-mode= 重置为合理值(见 *基础行为设置* )
+ =file-name-handler-alist= 启动期清空 + 启动后恢复**只在 [[file:early-init.el][early-init.el]] 做一次**(Phase 7.1 决策)
  - 原先 early-init.el 与 main.el 都 defvar =literal--file-name-handler-alist-original=,但 main.el 加载时变量早已被 early-init 清空 → main.el 的 defvar 捕获到 nil → emacs-startup-hook 把 nil 写回(用户失去 jka-compr/tramp handler)
  - 现在 main.el 只保留 GC threshold(防御性,供 =configctl load= 等绕过 early-init 的场景),=file-name-handler-alist= 的完整生命周期由 early-init.el 独占

#+begin_src emacs-lisp
;;; main.el --- literal-config 主配置(由 emacs.org tangle 生成) -*- lexical-binding: t; -*-

;; SPDX-FileCopyrightText: 2026 BrokenShine <xchai404@gmail.com>
;; SPDX-License-Identifier: MIT

;; GC 启动期优化(启动完成后由 gcmh 重置)
;; Phase 7.1 决策(保留):与 early-init.el 的同设置重复,但保留作为 main.el
;; 自身的防御性优化——若有人绕过 early-init.el 直接 batch-load main.el(如
;; `configctl load'),GC threshold 仍需在 main.el 入口推高。early-init.el 的
;; `file-name-handler-alist = nil' 已足够覆盖正常启动路径,这里不再重复设置。
(setq gc-cons-threshold most-positive-fixnum
      gc-cons-percentage 0.6)

;; 启动完成后恢复 gc-cons-percentage(gcmh 接管 threshold,percentage 需显式恢复)。
;; Phase 7.1:file-name-handler-alist 的保存/恢复只在 early-init.el 中做一次,
;; 不在 main.el 重复——因为 main.el 加载时该变量早已被 early-init.el 清空,
;; 在这里再次 `defvar ... file-name-handler-alist' 只会捕获到 nil,导致
;; emacs-startup-hook 把 nil 写回(用户失去 jka-compr/tramp 等 handler)。
(add-hook 'emacs-startup-hook
          (lambda ()
            ;; gc-cons-percentage 启动期推到 0.6(减少启动期 GC),gcmh 只管
            ;; threshold 不管 percentage,故这里显式恢复到常规值。
            (setq gc-cons-percentage 0.1)))

(message "[literal-config] 基础设施加载完成")
#+end_src

** 路径常量与外部命令缓存
:PROPERTIES:
:CUSTOM_ID: bootstrap
:END:
集中定义所有路径常量(org 目录、guix profile、tree-sitter 库等),并在启动期一次性缓存所有外部命令路径。

+ 遵循 doom 范式: =defconst= 时调 =executable-find= ,后续只读变量,避免每次使用都搜 PATH
+ =early-init.el= 已把 =GUIX_PROFILE/bin= 和 =~/.local/bin= 加入 =exec-path= ,因此缓存能命中 =agenote= / =godot= / =dot= 等用户工具
+ 后续所有功能域都引用这里的 =literal:org-directory= 、 =literal:executable-*=

#+begin_src emacs-lisp :noweb-ref bootstrap/paths :tangle no
(require 'cl-lib)
(require 'seq)
(require 'subr-x)

;; 核心路径常量

(defconst literal:emacs-dir user-emacs-directory
  "当前 Emacs profile 的根目录。")

(defconst literal:org-directory
  (expand-file-name "~/Documents/Org/")
  "Org 文件根目录。")

(defconst literal:org-roam-directory
  (expand-file-name "roam" literal:org-directory)
  "Org-roam 笔记目录。")

(defconst literal:org-inbox-file
  (expand-file-name "inbox.org" literal:org-directory)
  "Org 收件箱。")

(defconst literal:org-default-notes-file
  (expand-file-name "notes/notes.org" literal:org-directory)
  "默认笔记文件。
DEPRECATED(Phase 2.1):notes/ 标记为历史迁移区,不再创建新文件。
新笔记统一走 capture 模板(ki=inbox / kr=roam)。此常量仅保留给
历史文件读取,Phase 7 决定是否彻底删除。")

(defconst literal:org-knowledge-directory
  (expand-file-name "experiences" literal:org-directory)
  "知识库经验卡片目录（人类域）。")

(defconst literal:agenote-directory
  (expand-file-name "agenote" literal:org-directory)
  "agenote 子域目录（agent 写入的卡片 + index.json）。")

(defconst literal:tree-sitter-lib-dir
  (or (getenv "TREE_SITTER_GRAMMAR_PATH")
      (expand-file-name "~/.guix-home/profile/lib/tree-sitter"))
  "Tree-sitter grammar 动态库目录。

Guix home-emacs-service 的 wrapper 把 home-emacs-profile/lib/tree-sitter/
通过 `TREE_SITTER_GRAMMAR_PATH' 环境变量注入 Emacs 进程(该 profile 独立于
~/.guix-home/profile,只有 emacs wrapper 能访问)。Emacs 31 会自动把该环境
变量加入 `treesit-extra-load-path',故常量本身主要用于诊断与非 Guix 环境。")

(defconst literal:guix-profile
  (or (getenv "GUIX_PROFILE")
      (expand-file-name "~/.guix-profile"))
  "Guix profile 路径。")

(defconst literal:in-guix-environment-p
  (or (getenv "GUIX_ENVIRONMENT")
      (file-exists-p literal:guix-profile))
  "是否运行在 Guix 环境中。")
#+end_src

#+begin_src emacs-lisp :noweb-ref bootstrap/executables :tangle no
;; 外部命令路径缓存（启动期一次性检测，运行时只读）
;; 遵循 doom 范式：defvar 时调 executable-find，后续只读变量。
;; 避免每次使用时都搜 PATH（每次 executable-find ~10-50μs，高频场景累积可观）。
;; early-init.el 已把 GUIX_PROFILE/bin 和 ~/.local/bin 加入 exec-path，
;; 因此这里的缓存能命中 agenote / godot / dot 等用户工具。

(defconst literal:executable-rg (executable-find "rg")
  "ripgrep 路径，启动期缓存。nil 表示未安装。")

(defconst literal:executable-fd (cl-find-if #'executable-find '("fd" "fdfind"))
  "fd 路径，启动期缓存。fdfind 为 Debian/Ubuntu 包名。")

(defconst literal:executable-jq (executable-find "jq")
  "jq 路径，启动期缓存。")

(defconst literal:executable-git (executable-find "git")
  "git 路径，启动期缓存。")

(defconst literal:executable-agenote (executable-find "agenote")
  "agenote CLI 路径（知识库工具），启动期缓存。")

(defconst literal:executable-dot (executable-find "dot")
  "Graphviz dot 路径，启动期缓存。")

(defconst literal:executable-godot (executable-find "godot")
  "Godot 引擎路径，启动期缓存。")

(defconst literal:executable-fish (executable-find "fish")
  "fish shell 路径，启动期缓存。")

(defconst literal:executable-direnv (executable-find "direnv")
  "direnv 路径，启动期缓存。")

(defconst literal:executable-java (executable-find "java")
  "java 路径，启动期缓存。")

(defconst literal:executable-kotlinc (executable-find "kotlinc")
  "kotlinc 路径，启动期缓存。")

(defconst literal:executable-python
  (or (executable-find "python3") (executable-find "python"))
  "python 路径（python3 优先），启动期缓存。")

(defconst literal:executable-aspell (executable-find "aspell")
  "aspell 路径，启动期缓存。")

(defconst literal:executable-hunspell (executable-find "hunspell")
  "hunspell 路径，启动期缓存。")
#+end_src

#+begin_src emacs-lisp
;; bootstrap 组装：路径常量 + 外部命令缓存
<<bootstrap/paths>>
<<bootstrap/executables>>
#+end_src

** 共享进程辅助
:PROPERTIES:
:CUSTOM_ID: process-helper
:END:
统一的同步进程入口与 agenote CLI 适配层。

+ =literal/call-process= :通用同步子进程(返回 =(STATUS . OUTPUT)=)
+ =literal/agenote-call= :agenote CLI 唯一入口,domain 必填,返回 plist
+ =literal/agenote-call-async= :长任务异步执行(reindex/lint/deduplicate/commit/viz)

agenote CLI 支持 =--domain human|agenote=,默认 agenote(与 MCP server 对齐)。
Emacs 端必须为每次调用显式选择 domain —— 不依赖默认值,避免人类域操作静默
打到 agent 子域。

**不缓存 Guix store 绝对路径**:daemon 长驻跨 Guix profile 更新时,缓存的
=/gnu/store/.../bin/agenote= 会指向旧 generation。=literal/agenote-call= 内部
按需 =(executable-find "agenote")=,让每次调用都解析当前 PATH。

#+begin_src emacs-lisp :noweb-ref helpers/call-process :tangle no
(defun literal/call-process (command &rest args)
  "同步执行 COMMAND 与 ARGS,返回 (STATUS . OUTPUT)。"
  (with-temp-buffer
    (cons (or (apply #'call-process command nil t nil (remq nil args)) -1)
          (string-trim (buffer-string)))))

(defconst literal:agenote-valid-domains '(human agenote)
  "agenote CLI 支持的 domain 取值。与 ~agenote --help~ 对齐。")

(defun literal/agenote--resolve-executable ()
  "按需解析 agenote 命令路径。返回可执行路径字符串或 nil。
不缓存绝对 store 路径:daemon 跨 Guix generation 后能找到新 profile 的 agenote。
`literal:executable-agenote' 仍保留作为启动期可用性 flag(在 modeline、
dashboard 等需要"是否装了 agenote"判断的地方使用),但实际调用走本函数。"
  (executable-find "agenote"))

(defun literal/agenote--ensure-domain (domain)
  "校验 DOMAIN,返回小写 symbol。无效值 fail-fast。"
  (let ((d (if (stringp domain) (intern (downcase domain)) domain)))
    (unless (memq d literal:agenote-valid-domains)
      (error "literal/agenote-call: domain must be %s, got %S"
             (mapconcat #'symbol-name literal:agenote-valid-domains "|")
             domain))
    d))

(cl-defun literal/agenote-call (domain command &rest args)
  "同步调用 agenote CLI,DOMAIN 必填(human 或 agenote),COMMAND 子命令字符串,
ARGS 附加参数。

返回 plist:`(:status :stdout :stderr :command :domain)'。
- :status  进程退出码(-1 表示无法启动)
- :stdout  stdout 字符串(trimmed)
- :stderr  stderr 字符串(trimmed)
- :command  完整 argv 列表(便于诊断)
- :domain  实际使用的 domain symbol

用法示例:
  (literal/agenote-call 'human \"list\" \"--all\")
  (literal/agenote-call 'agenote \"search\" \"emacs\" \"--limit\" \"10\")
  (plist-get (literal/agenote-call 'human \"stats\") :stdout)"
  (let* ((dom (literal/agenote--ensure-domain domain))
         (exe (literal/agenote--resolve-executable))
         (argv (delq nil (append (list exe "--domain" (symbol-name dom) command) args))))
    (if (not exe)
        (list :status -1 :stdout "" :stderr "agenote executable not found"
              :command argv :domain dom)
      (let ((stdout-buffer (generate-new-buffer " *agenote-stdout*"))
            (stderr-buffer (generate-new-buffer " *agenote-stderr*"))
            status)
        (unwind-protect
            (progn
              (setq status
                    (apply #'call-process argv nil (list t stderr-buffer) nil))
              (list :status (or status -1)
                    :stdout (string-trim
                             (with-current-buffer stdout-buffer (buffer-string)))
                    :stderr (string-trim
                             (with-current-buffer stderr-buffer (buffer-string)))
                    :command argv
                    :domain dom))
          (when (buffer-live-p stdout-buffer) (kill-buffer stdout-buffer))
          (when (buffer-live-p stderr-buffer) (kill-buffer stderr-buffer)))))))

(cl-defun literal/agenote-call-async (domain command &rest args)
  "异步调用 agenote CLI,参数同 `literal/agenote-call'。
长任务(reindex / lint / deduplicate / commit / viz)应使用本函数,避免阻塞
Emacs。结果写入 `*agenote-<command>*' buffer(special-mode,`g' 重跑,`q' 退出)。

返回启动的 process 对象;同步路径请用 `literal/agenote-call'。"
  (let* ((dom (literal/agenote--ensure-domain domain))
         (exe (literal/agenote--resolve-executable))
         (argv (delq nil (append (list exe "--domain" (symbol-name dom) command) args)))
         (buf-name (format "*agenote-%s*" command))
         (buffer (get-buffer-create buf-name)))
    (if (not exe)
        (progn
          (with-current-buffer buffer
            (let ((inhibit-read-only t))
              (erase-buffer)
              (insert "agenote executable not found\n")))
          (display-buffer buffer)
          nil)
      (with-current-buffer buffer
        (let ((inhibit-read-only t))
          (erase-buffer)
          (insert (format "$ %s\n\n" (string-join argv " ")))
          (special-mode)
          ;; `g' re-runs the same command.
          (let ((mode-map literal/agenote-async-mode-map))
            (use-local-map (or mode-map (make-sparse-keymap))))
          (define-key (current-local-map) "g"
                      (lambda () (interactive)
                        (apply #'literal/agenote-call-async dom command args)))))
      (let ((proc (make-process
                   :name (format "agenote %s" command)
                   :buffer buffer
                   :command argv
                   :connection-type 'pipe
                   :sentinel (lambda (proc event)
                               (let ((buf (process-buffer proc)))
                                 (when (buffer-live-p buf)
                                   (with-current-buffer buf
                                     (let ((inhibit-read-only t))
                                       (goto-char (point-max))
                                       (insert
                                        (format "\n[process %s]\n"
                                                (string-trim event)))))))))))
        (display-buffer buffer)
        proc))))

;; minimal keymap for async result buffers (defined here so async functions can
;; reference it before the dashboard/knowledge sections are loaded)
(defvar literal/agenote-async-mode-map
  (let ((m (make-sparse-keymap)))
    (set-keymap-parent m special-mode-map)
    m)
  "Keymap for `*agenote-<command>*' async result buffers.")

;; 向后兼容:许多旧调用点仍用 (cdr (literal/call-process literal:executable-agenote ...))
;; 形式取 stdout。提供一个 thin wrapper 让迁移期不破坏既有代码。
;;
;; 注意:这是过渡辅助,新代码应直接用 `literal/agenote-call'。Commit 5 合并
;; knowledge 与 agenote 展示层时会清掉所有调用点。
(defun literal/agenote-call-string (domain command &rest args)
  "同步调 agenote,返回 stdout 字符串(向后兼容 `literal/call-process' 调用)。
DOMAIN/COMMAND/ARGS 同 `literal/agenote-call'。失败返回空串。"
  (plist-get (apply #'literal/agenote-call domain command args) :stdout))
#+end_src

#+begin_src emacs-lisp
<<helpers/call-process>>
#+end_src

** Frame 生命周期与 daemon
:PROPERTIES:
:CUSTOM_ID: frame
:END:
daemon/client 架构下 per-frame 初始化(主题、字体、childframe、tab-line、dashboard 等)的统一注册入口。

+ 替代旧配置的 =custom/register-daemon-frame-hook=
+ 在 daemon 模式下同时挂 =after-make-frame-functions= 与 =server-after-make-frame-hook= (覆盖 =emacs --fg-daemon= + =emacsclient= 两条路径)
+ 非 daemon 模式立即用当前 frame 调一次(standalone 冷启动)
+ 使用范式: =(literal/add-frame-hook #'my-setup-display)= ,而非手写 =add-hook=
+ =literal/daemon-runtime-p= 供延迟预热场景判断是否为可交互 daemon 会话
+ **硬依赖**: color-scheme / dashboard / completion 会直接调用 =literal/add-frame-hook=; 本块确保它先于这些模块就绪

#+begin_src emacs-lisp
(defun literal/add-frame-hook (function)
  "注册 FUNCTION 在每个新 frame 创建时执行。
- daemon 模式：同时挂 `after-make-frame-functions' 和 `server-after-make-frame-hook'
- 非 daemon 模式：立即对当前 frame 调用一次（standalone 冷启动）
FUNCTION 接受一个可选参数 FRAME。"
  (if (daemonp)
      (progn
        (add-hook 'after-make-frame-functions function)
        (when (boundp 'server-after-make-frame-hook)
          (add-hook 'server-after-make-frame-hook function)))
    ;; standalone：当前 frame 已存在，直接跑一次
    (funcall function (selected-frame))))

(defun literal/remove-frame-hook (function)
  "移除 FUNCTION 的 frame hook 注册。"
  (remove-hook 'after-make-frame-functions function)
  (when (boundp 'server-after-make-frame-hook)
    (remove-hook 'server-after-make-frame-hook function)))

(defun literal/daemon-runtime-p ()
  "返回当前是否为可交互的 daemon 会话（用于延迟预热等场景）。"
  (and (daemonp) (not noninteractive)))
#+end_src
** 键位辅助函数
:PROPERTIES:
:CUSTOM_ID: key-helper
:END:
=literal/set-key= :一行完成全局键位绑定 + which-key 中文描述注册(替代旧 =custom/bind!= 宏)。

+ use-package 原生写法下,全局键位用 =keymap-global-set= ,但 which-key 中文描述需要单独调 =which-key-add-key-based-replacements= ,导致"绑定"与"描述"分离;本函数合并二者
+ which-key 尚未加载时,描述先收集到 =literal--pending-wk-descs= ,由 =literal/register-pending-wk-descs= 在 which-key 加载后一次性注册
+ **必须放在基础设施域**: =literal/register-pending-wk-descs= 会在 *界面与外观* 域的 which-key =:config= 里被调用,若晚于 which-key 加载会 void-function
+ Emacs 31 =keymap-global-set= 走严格 =key-valid-p= :修饰键必须写在 =<>= 外部( ="C-<tab>"= 而非 ="<C-tab>"= );本函数对含 =<= 的 KEY 自动转换格式

#+begin_src emacs-lisp
(defvar literal--pending-wk-descs nil
  "literal/set-key 收集的键位描述,which-key 加载后批量注册。")

(defun literal/set-key (key command &optional desc)
  "全局绑定 KEY 到 COMMAND,可选用 DESC 注册 which-key 中文描述。
KEY 与 DESC 均为字符串。DESC 为 nil 时只做绑定。
此函数替代旧配置的 `custom/bind!' 宏。

Emacs 31 的 `keymap-global-set' 走严格 `key-valid-p':
修饰键必须写在 <> 外部(\"C-<tab>\" 而非 \"<C-tab>\")。
本函数对包含 `<' 的 KEY 自动转换格式,兼容旧式写法。"
  (keymap-global-set key command)
  (when (and desc (stringp desc))
    ;; 只走一条注册路径:which-key 已加载则直接注册,否则先收集到 pending 列表。
    ;; 原先同时挂 after-load 闭包 + push pending,which-key 加载后每个描述被注册
    ;; 两次,且每次调用往 after-load-alist 塞一个闭包(几十键 = 几十表项)。
    (if (featurep 'which-key)
        (which-key-add-key-based-replacements key desc)
      (push (cons key desc) literal--pending-wk-descs))))

(defun literal/register-pending-wk-descs ()
  "把 `literal--pending-wk-descs' 注册到 which-key(一次性)。"
  (dolist (entry (nreverse literal--pending-wk-descs))
    (which-key-add-key-based-replacements (car entry) (cdr entry)))
  (setq literal--pending-wk-descs nil))

#+end_src

** 包管理与加载策略
:PROPERTIES:
:CUSTOM_ID: package-loading
:END:
声明本配置依赖 Guix 提供的包, =EMACSLOADPATH= 已预设,无需手动安装。

+ ~use-package-always-ensure nil~ :禁止 use-package 调用 package.el 安装
+ ~package-enable-at-startup nil~ :禁用 package.el 自动初始化(实际由 early-init.el 在更早时机设置,这里再保险设一次)
+ 所有包直接 =(require 'xxx)= 或 =(use-package xxx)= ,Guix 已在 =EMACSLOADPATH= 中放好

#+begin_src emacs-lisp
;; use-package(由 Guix 提供)
(require 'use-package)

;; 禁用自动安装包(Guix 管理包,不用 package.el)
(setq use-package-always-ensure nil
      package-enable-at-startup nil)

;; eln 缓存重定向到 var/,避免污染根目录(Emacs 29+)
(when (fboundp 'startup-redirect-eln-cache)
  (startup-redirect-eln-cache
   (expand-file-name "var/eln-cache/" user-emacs-directory)))
#+end_src

** 基础行为设置
:PROPERTIES:
:CUSTOM_ID: core-behavior
:END:
GC 策略、文件备份、历史记录、自动保存、custom-file 位置等基础设置。

+ ~gcmh-mode~ :启动后接管 GC,空闲时自动 GC,编辑时不打断
+ ~no-littering~ :把各种包生成的文件统一放到 =var/= 和 =etc/=
+ ~savehist~ / =recentf= :minibuffer 历史与最近文件持久化
+ ~desktop~ :会话恢复(daemon 默认关闭,避免恢复成本转移到启动)
+ ~custom-file~ :指向 =etc/custom.el= ,避免 customize 污染 init.el
+ 失去焦点时自动保存所有 buffer

#+begin_src emacs-lisp
;; GCMH + 智能垃圾回收管理(启动后接管 GC)
;; Phase 7.1 决策(保留):GCMH 在 daemon 长运行场景显著减少 GC 卡顿。
;;   - `gcmh-idle-delay 'auto' 让 GCMH 自适应 idle 时间(短空闲快 GC、长空闲
;;     等更空闲再 GC),避免编辑过程中被打断。
;;   - `gcmh-high-cons-threshold 32 MiB' 是 daemon 长运行环境的合理上限:
;;     太小(默认 16 MiB)→ GC 频繁;太大(64+ MiB)→ 单次 GC 卡顿长。
;;     32 MiB 在 8+ GiB 内存机器上不构成压力,与 gcmh-mode 维持的
;;     low-cons-threshold(默认 1 MiB)的差距让 GCMH 能区分"短暂峰值"
;;     和"持续压力"。
;;   - 不再在 use-package 外手动 setq gc-cons-threshold:GCMH 接管后
;;     该变量由 GCMH 动态调整,手动 setq 会与 GCMH 打架。
(use-package gcmh
  :demand t
  :custom
  (gcmh-idle-delay 'auto)
  (gcmh-auto-idle-delay-factor 10)
  (gcmh-high-cons-threshold (* 32 1024 1024)) ; 32MB(daemon 长运行优化)
  :config
  (gcmh-mode 1))

;; LSP 通信优化
(setq read-process-output-max (* 4 1024 1024) ; 4MB
      process-adaptive-read-buffering nil
      fast-but-imprecise-scrolling t)

;; 启动画面与基础体验
;; Phase 7.1 产品决策(PLAN §7.1:保留用户确认过的无确认退出,加注释):
;; daemon 长驻下 client 关闭不退出 Emacs;真正 kill-emacs 时无需二次确认
;; (用户已确认所有未保存内容在 focus-loss 时已落盘,见下方
;; after-focus-change-function)。
(setq inhibit-startup-screen t
      initial-scratch-message nil
      ring-bell-function 'ignore
      confirm-kill-emacs nil
      confirm-kill-processes nil)

;; no-littering + 统一管理缓存和配置文件
(use-package no-littering
  :demand t
  :config
  (setq backup-directory-alist
        `((".*" . ,(no-littering-expand-var-file-name "backup/"))))
  (setq auto-save-file-name-transforms
        `((".*" ,(no-littering-expand-var-file-name "auto-save/") t)))
  (setq custom-file (no-littering-expand-etc-file-name "custom.el"))
  (when (file-exists-p custom-file)
    (load custom-file t)))

;; auto-compile + 自动编译用户配置(不编译 Guix store)
(defun literal--auto-compile-root-directories ()
  "返回允许 auto-compile 处理的配置根目录。"
  (delete-dups
   (delq nil
         (list literal:emacs-dir
               (file-truename literal:emacs-dir)))))

(defun literal--auto-compile-file-p (file)
  "FILE 是否属于本配置并可自动编译。"
  (and (auto-compile-source-file-p file)
       (let ((expanded-file (expand-file-name file)))
         (catch 'matched
           (dolist (dir (literal--auto-compile-root-directories))
             (let ((root (file-name-as-directory (expand-file-name dir))))
               (when (string-prefix-p root expanded-file)
                 (throw 'matched t))))
           nil))))

(use-package auto-compile
  :demand t
  :config
  (setq auto-compile-use-mode-line nil)
  (setq auto-compile-predicate-function #'literal--auto-compile-file-p)
  (auto-compile-on-load-mode 1)
  (auto-compile-on-save-mode 1))

;; 历史记录与最近文件
(use-package savehist
  :defer 0.5
  :config
  (savehist-mode 1))

(use-package recentf
  :defer 1
  :custom
  (recentf-max-saved-items 300)
  :config
  (recentf-mode 1))

;; 失去焦点时自动保存所有缓冲区
;; Phase 7.1 产品决策(PLAN §7.1:保留用户确认过的 focus-loss save,加注释
;; 说明这是产品决策而非遗漏):多窗口工作流下避免忘记保存导致数据丢失;
;; 代价是切换到非 Emacs 应用时所有未保存 buffer 被写盘(对大型 org 文件
;; 可能引起轻微 IO 抖动,用户已接受此权衡)。
(add-function :after after-focus-change-function
              (lambda ()
                (unless (frame-focus-state)
                  (save-some-buffers t))))

;; Emacs Server(非 daemon standalone 时启用)
(unless (or (daemonp) noninteractive)
  (server-mode 1))

;; trusted-content(Emacs 30+,CVE-2024-53920 缓解)
;; 项必须以 / 结尾才被视为目录前缀(否则被当作单个文件名);
;; literal:emacs-lisp-dir 经 expand-file-name 无尾斜杠,需 file-name-as-directory 补上。
(when (boundp 'trusted-content)
  (add-hook 'emacs-startup-hook
            (lambda ()
              (dolist (dir (literal--auto-compile-root-directories))
                (add-to-list 'trusted-content (file-name-as-directory dir))))
            t))
#+end_src

* 界面与外观
:PROPERTIES:
:CUSTOM_ID: appearance
:END:
所有 display-dependent 的视觉配置:主题、字体、窗口分割、面板布局、mode-line、tab-line、dashboard、帮助系统、右键菜单与 which-key 中文化。
display-dependent 设置统一通过 =literal/apply-frame-appearance= 在新 frame 创建时触发(见 *主题与字体* )。

** Which-key 中文描述数据
:PROPERTIES:
:CUSTOM_ID: which-key-data
:END:
全局键、major mode 局部键与正则替换规则的数据源。行为注册位于 [[#which-key-ui][Which-key 菜单注册]]。

*** 外置翻译数据

翻译表是独立的数据资产：[[file:data/which-key-zh.el][Which-key 中文描述]]、[[file:data/context-menu-zh.el][右键菜单中文描述]] 与 [[file:data/help-zh.el][快捷键帮助中文描述]]。
修改数据文件后重启 Emacs 即可生效；其中只能写注释和预定变量的字面量 =setq=，不得定义函数、feature 或副作用。
scripts/configctl check= 会校验文件名、变量名、括号和数据边界。

#+begin_src emacs-lisp :noweb-ref appearance/i18n-data :tangle no
(defconst literal:i18n-data-directory
  (expand-file-name "data/" literal:emacs-dir)
  "本地化纯数据文件目录。")

(defconst literal:i18n-data-files
  '("which-key-zh.el" "context-menu-zh.el" "help-zh.el")
  "按加载顺序列出的本地化数据文件。")

(defvar literal:which-key-description-spec nil)
(defvar literal:which-key-major-mode-description-spec nil)
(defvar literal:which-key-regexp-replacements nil)
(defvar literal:context-menu-label-translations nil)
(defvar literal:help-dashboard-bindings nil)
(defvar literal:help-sections nil)

(defun literal/load-i18n-data ()
  "加载并验证本地化纯数据文件。"
  (dolist (name literal:i18n-data-files)
    (let ((file (expand-file-name name literal:i18n-data-directory)))
      (unless (file-readable-p file)
        (error "本地化数据文件不可读: %s" file))
      (load file nil 'nomessage)))
  (dolist (symbol '(literal:which-key-description-spec
                    literal:which-key-major-mode-description-spec
                    literal:which-key-regexp-replacements
                    literal:context-menu-label-translations
                    literal:help-dashboard-bindings
                    literal:help-sections))
    (unless (listp (symbol-value symbol))
      (error "本地化数据格式无效: %s" symbol))))

(literal/load-i18n-data)
#+end_src

#+begin_src emacs-lisp
<<appearance/i18n-data>>
#+end_src
** 右键菜单本地化
:PROPERTIES:
:CUSTOM_ID: context-menu-i18n
:END:
集中维护菜单标签翻译,并在最终 keymap 上递归替换显示文本;不改写各包的菜单生成逻辑。

*** 翻译数据
翻译表位于 [[file:data/context-menu-zh.el][data/context-menu-zh.el]]。
*** Keymap 翻译引擎
#+begin_src emacs-lisp :noweb-ref appearance/context-menu-engine :tangle no
(defun literal/context-menu--translate-string (string)
  "翻译 STRING；未登记时返回原字符串。"
  (let* ((plain (substring-no-properties string))
         (translation (cdr (assoc plain literal:context-menu-label-translations))))
    (if (not translation)
        string
      (if (> (length string) 0)
          (apply #'propertize translation (text-properties-at 0 string))
        translation))))

(defun literal/context-menu--translate-object (object)
  "递归翻译 OBJECT 中的字符串。"
  (cond
   ((stringp object)
    (literal/context-menu--translate-string object))
   ((consp object)
    (mapcar #'literal/context-menu--translate-object object))
   (t object)))

(defun literal/context-menu--translate-menu-item (item)
  "翻译菜单项 ITEM 的标题和子菜单。"
  (when (and (consp item) (eq (car item) 'menu-item))
    (let ((label-cell (cdr item))
          (binding (nth 2 item))
          (properties (nthcdr 3 item)))
      (when label-cell
        (setcar label-cell
                (literal/context-menu--translate-object (car label-cell))))
      (when (keymapp binding)
        (literal/context-menu--translate-keymap binding))
      (while (consp properties)
        (when (and (eq (car properties) :help)
                   (consp (cdr properties)))
          (setcar (cdr properties)
                  (literal/context-menu--translate-object (cadr properties))))
        (setq properties (cddr properties))))))

(defun literal/context-menu--translate-keymap (keymap)
  "递归翻译 KEYMAP 内的菜单标题。"
  (when (keymapp keymap)
    (when (and (consp keymap)
               (eq (car-safe keymap) 'keymap)
               (stringp (cadr keymap)))
      (setcar (cdr keymap)
              (literal/context-menu--translate-string (cadr keymap))))
    (map-keymap
     (lambda (_event binding)
       (cond
        ((and (consp binding) (eq (car binding) 'menu-item))
         (literal/context-menu--translate-menu-item binding))
        ((keymapp binding)
         (literal/context-menu--translate-keymap binding))))
     keymap))
  keymap)

(defun literal/context-menu-translate (menu)
  "翻译 `context-menu-map' 返回的 MENU。"
  (literal/context-menu--translate-keymap menu))

(when (fboundp 'context-menu-map)
  (unless (advice-member-p #'literal/context-menu-translate #'context-menu-map)
    (advice-add 'context-menu-map :filter-return #'literal/context-menu-translate)))

(with-eval-after-load 'magit-section
  (unless (advice-member-p #'literal/context-menu--translate-string
                           #'magit-menu-format-desc)
    (advice-add 'magit-menu-format-desc :filter-return
                #'literal/context-menu--translate-string)))
#+end_src

#+begin_src emacs-lisp
<<appearance/context-menu-engine>>
#+end_src

** 快捷键帮助
:PROPERTIES:
:CUSTOM_ID: help
:END:
维护完整快捷键参考与 Dashboard 复用的精简入口。

*** 帮助数据
帮助分组数据已外置到 [[file:data/help-zh.el][data/help-zh.el]],由 [[#which-key-data][外置翻译数据]] 的 =literal/load-i18n-data= 统一加载到 =literal:help-dashboard-bindings= 与 =literal:help-sections=;修改后重启 Emacs 即可生效。这里只保留面向渲染的 face 和把外置数据别名到 =literal/help--...= 命名的变量,使渲染逻辑不感知数据来源。

#+begin_src emacs-lisp :noweb-ref appearance/help-data :tangle no
(defface literal-help-section-title
  '((t :weight bold :height 1.2))
  "帮助缓冲区分类标题样式。前景色由 color-scheme hook 统一设置。"
  :group 'help)

;; 外置数据(literal:...) 在 appearance/i18n-data 中由 literal/load-i18n-data
;; 先于此块求值加载。这里为渲染层提供带 literal/ 前缀的别名,避免渲染代码耦合
;; 数据加载机制。
(defconst literal/help--dashboard-bindings literal:help-dashboard-bindings
  "Dashboard 展示用的精简前缀总览(数据源自 data/help-zh.el)。")

(defconst literal/help--sections literal:help-sections
  "完整快捷键帮助分组(数据源自 data/help-zh.el)。")

#+end_src

*** 帮助渲染与入口
#+begin_src emacs-lisp :noweb-ref appearance/help-render :tangle no
(defun literal/help--extract-dashboard-bindings ()
  "返回 dashboard 需要展示的关键前缀绑定。"
  literal/help--dashboard-bindings)

(defun literal/help--insert-section (title bindings)
  "向当前 buffer 插入一个分类段落。
TITLE 为分类标题,BINDINGS 为 ((key . desc) ...) 列表。"
  (insert (propertize (format "═══ %s ═══" title)
                      'face 'literal-help-section-title))
  (insert "\n\n")
  (dolist (binding bindings)
    (insert (format "  %-24s %s\n"
                    (propertize (car binding) 'face 'font-lock-keyword-face)
                    (cdr binding))))
  (insert "\n"))

(defun literal/show-help ()
  "显示完整的快捷键帮助。"
  (interactive)
  (let ((buf (get-buffer-create "*Shortcuts Help*")))
    (with-current-buffer buf
      (let ((inhibit-read-only t))
        (erase-buffer)
        (special-mode)
        (insert "\n")
        (insert (propertize "快捷键参考(Emacs 前缀分组版)\n\n"
                            'face '(:height 1.5 :weight bold)))
        (insert "本配置不再使用单一总前缀。命令按"文件 / 项目 / 搜索 / 跳转 / 代码 / Git / 切换 / Org / 帮助"分布在多个前缀里。\n")
        (insert "由于 ")
        (insert (propertize "C-h" 'face 'font-lock-keyword-face))
        (insert " 保留给左移，帮助入口改为 ")
        (insert (propertize "F1" 'face 'font-lock-keyword-face))
        (insert " 前缀（F1 i = Info, F1 f = 查函数, ...）；增强版帮助用 ")
        (insert (propertize "C-c h" 'face 'font-lock-keyword-face))
        (insert " 前缀。\n")
        (insert "按 ")
        (insert (propertize "F1 ?" 'face 'font-lock-keyword-face))
        (insert " 或 ")
        (insert (propertize "C-c h ?" 'face 'font-lock-keyword-face))
        (insert " 打开这份完整参考。\n\n")
        (dolist (section literal/help--sections)
          (literal/help--insert-section (car section) (cdr section)))
        (insert (propertize "提示:" 'face '(:weight bold)))
        (insert " 按 ")
        (insert (propertize "q" 'face 'font-lock-keyword-face))
        (insert " 关闭此帮助窗口\n"))
      (goto-char (point-min)))
    (pop-to-buffer buf)))

(global-set-key (kbd "<f1> ?") #'literal/show-help)
(global-set-key (kbd "C-c h ?") #'literal/show-help)
#+end_src

#+begin_src emacs-lisp
<<appearance/help-data>>
<<appearance/help-render>>
#+end_src

** Modeline
:PROPERTIES:
:CUSTOM_ID: modeline
:END:
自写响应式 mode-line,按窗口宽度降级信息密度并缓存高频状态。

*** 常量与基础辅助
#+begin_src emacs-lisp :noweb-ref appearance/modeline-core :tangle no


(require 'battery)
(require 'project)

(declare-function nerd-icons-icon-for-file "nerd-icons")
(declare-function nerd-icons-icon-for-mode "nerd-icons")
(declare-function nerd-icons-giticon "nerd-icons")
(declare-function nerd-icons-faicon "nerd-icons")
(declare-function nerd-icons-mdicon "nerd-icons")
(declare-function jsonrpc-running-p "jsonrpc")
(defvar mode-line-format-right-align)

;; 常量与缓存

;; Phase 4.2:删除未使用 defvaralias literal:modeline-name-max-width
;; (无任何代码引用,只是 path-max-width 的旧别名)和孤儿 defgroup literal-modeline
;; (defcustom 都未指定 :group,defgroup 仅声明未引用)。

(defcustom literal:modeline-show-time t
  "是否在 mode-line 右段显示当前时间。"
  :type 'boolean)

(defcustom literal:modeline-show-vcs t
  "是否在 mode-line 右段显示 VCS 分支。"
  :type 'boolean)

(defcustom literal:modeline-path-max-width 40
  "路径段最大显示宽度。超过部分截断为省略号。"
  :type 'integer)

(defvar literal:modeline--nerd-icons-available
  (require 'nerd-icons nil t)
  "启动期一次性缓存 nerd-icons 是否可用。")

(defvar-local literal:modeline--flymake-counts '(0 0)
  "当前 buffer 的 Flymake 计数缓存，格式为 (ERRORS WARNINGS)。
由 `flymake-after-diagnostics-hook' 刷新(P0 #2 修复后 Eglot 不再 stay-out-of
flymake,诊断走 Flymake 公开 API)。")

(defvar-local literal:modeline--eglot-info nil
  "当前 buffer 的 Eglot 缓存，格式为 (SERVER-NAME STATUS)。")

(defvar-local literal:modeline--project-cache :unknown
  "当前 buffer 的 project 缓存。
值为 :unknown 表示未计算；nil 表示不在项目中；否则为 (ROOT NAME)。")

(defvar-local literal:modeline--vc-state-cache :unknown
  "当前 buffer 的 VC 状态缓存。
值为 :unknown 表示未计算；nil 表示不在 VC 控制下；否则为 `vc-state' 返回值。")

;; 基础辅助函数

(defun literal/modeline-tier ()
  "根据当前窗口宽度返回 `wide'、`medium'、`narrow' 或 `compact'。"
  (let ((width (window-width)))
    (cond
     ((>= width 120) 'wide)
     ((>= width 100) 'medium)
     ((>= width 80) 'narrow)
     (t 'compact))))

(defun literal/modeline-wide-p ()
  "当前 mode-line 是否处于 wide 档。"
  (eq (literal/modeline-tier) 'wide))

(defun literal/modeline-narrow-p ()
  "当前 mode-line 是否处于 narrow 档。"
  (eq (literal/modeline-tier) 'narrow))

(defun literal/modeline-compact-p ()
  "当前 mode-line 是否处于 compact 档。"
  (eq (literal/modeline-tier) 'compact))

(defun literal/modeline--graphic-icons-p ()
  "当前 frame 是否适合显示 nerd-icons 图标。"
  (and (display-graphic-p)
       literal:modeline--nerd-icons-available))

(defun literal/modeline-icon (kind name &optional file)
  "返回 KIND 类型的 nerd-icons 图标 NAME。
FILE 只在 KIND 为 `file' 时使用。图标不可用或出错时返回空串。"
  (if (literal/modeline--graphic-icons-p)
      (or (ignore-errors
            (pcase kind
              ('file (nerd-icons-icon-for-file (or file name)))
              ('mode (nerd-icons-icon-for-mode major-mode))
              ('git (nerd-icons-giticon name))
              ('md (nerd-icons-mdicon name))
              ('fa (if (string-prefix-p "nf-md-" name)
                       (nerd-icons-mdicon name)
                     (nerd-icons-faicon name)))
              (_ (nerd-icons-faicon name))))
          "")
    ""))

(defun literal/modeline--escape-percent (text)
  "转义 TEXT 中的 %，避免 mode-line 把它当格式控制。"
  (replace-regexp-in-string "%" "%%" (or text "") nil t))

(defun literal/modeline--with-icon (icon text)
  "拼接 ICON 与 TEXT，任一为空时只返回另一项。"
  (let ((safe-icon (or icon ""))
        (safe-text (or text "")))
    (cond
     ((string-empty-p safe-icon) safe-text)
     ((string-empty-p safe-text) safe-icon)
     (t (concat safe-icon " " safe-text)))))

(defun literal/modeline--segment (text &optional face)
  "返回带 FACE 的 TEXT；TEXT 为空时返回空串。"
  (let ((safe-text (or text "")))
    (if (string-empty-p safe-text)
        ""
      (if face
          (propertize safe-text 'face face)
        safe-text))))

(defun literal/modeline--separator-before (text)
  "TEXT 非空时在前面加分隔符。"
  (if (string-empty-p (or text ""))
      ""
    (concat " · " text)))

(defun literal/modeline--truncate (text width)
  "把 TEXT 截断到 WIDTH。"
  (truncate-string-to-width (or text "") width 0 nil t))

(defun literal/modeline--file-in-directory-p (file directory)
  "判断 FILE 是否在 DIRECTORY 内。"
  (let ((file-dir (file-name-directory (expand-file-name file)))
        (root (file-name-as-directory (expand-file-name directory))))
    (and file-dir (file-in-directory-p file-dir root))))

(defun literal/modeline--path-component-initial (component)
  "返回路径 COMPONENT 的首字母。
隐藏目录名保留点，例如 .config 显示为 .c。"
  (let* ((trimmed (string-trim component))
         (hidden (string-prefix-p "." trimmed))
         (body (if hidden
                   (replace-regexp-in-string "\\`[.[:space:]]+" "" trimmed)
                 trimmed)))
    (cond
     ((string-empty-p body) "")
     (hidden (concat "." (substring body 0 1)))
     (t (substring body 0 1)))))

(defun literal/modeline--initials-path (path)
  "把 PATH 中每个目录名缩成首字母。"
  (let* ((remote-prefix (or (file-remote-p path) ""))
         (local-name (or (file-remote-p path 'localname) path))
         (abbrev (abbreviate-file-name local-name))
         (without-home (if (string-prefix-p "~/" abbrev)
                           (substring abbrev 2)
                         abbrev))
         (parts (split-string without-home "/" t))
         (initials (seq-remove
                    #'string-empty-p
                    (mapcar #'literal/modeline--path-component-initial parts))))
    (concat remote-prefix (string-join initials "/"))))

(defun literal/modeline--project-info ()
  "返回当前 buffer 的 project 缓存，格式为 (ROOT NAME) 或 nil。"
  (when (eq literal:modeline--project-cache :unknown)
    (setq literal:modeline--project-cache
          (condition-case nil
              (when-let* ((project (project-current nil))
                          (root (project-root project))
                          (name (file-name-nondirectory
                                 (directory-file-name root))))
                (list root name))
            (error nil))))
  literal:modeline--project-cache)

(defun literal/modeline--reset-project-cache ()
  "失效当前 buffer 的 project 缓存。"
  (setq literal:modeline--project-cache :unknown))

(defun literal/modeline--refresh-vc-state (&rest _)
  "刷新当前 buffer 的 VC 状态缓存。"
  (setq literal:modeline--vc-state-cache
        (if buffer-file-name
            (condition-case nil
                (when-let* ((backend (vc-backend buffer-file-name)))
                  (vc-state buffer-file-name backend))
              (error nil))
          nil)))

#+end_src

*** 状态缓存
#+begin_src emacs-lisp :noweb-ref appearance/modeline-cache :tangle no
;; 缓存刷新 hook

(defun literal/modeline--refresh-flymake-counts (&rest _)
  "刷新当前 buffer 的 Flymake 错误/警告计数缓存。
走 Flymake 公开 API(`flymake-diagnostics' + `flymake-diagnostic-type'),
不依赖任何私有符号。无 flymake-mode 时置 0/0。"
  (setq literal:modeline--flymake-counts
        (if (and (bound-and-true-p flymake-mode)
                 (fboundp 'flymake-diagnostics))
            (let ((errors 0) (warnings 0))
              (dolist (diag (flymake-diagnostics))
                (let* ((type (flymake-diagnostic-type diag))
                       (sev (and type
                                 (assq type flymake-diagnostic-types)
                                 (cdr (assq 'severity
                                            (cdr (assq type flymake-diagnostic-types)))))))
                  (cond
                   ((and sev (>= sev 2)) (cl-incf errors))      ; :error or worse
                   ((and sev (>= sev 1)) (cl-incf warnings))))) ; :warning
              (list errors warnings))
          '(0 0))))

(defun literal/modeline--eglot-server-name (server)
  "返回 SERVER 的简短名称。
Phase 4.2:不再调 `eglot--server-nickname' 私有 API;server 存在即返回
固定字符串 \"eglot\"(避免私有 API 漂移,Server 类型信息对用户意义有限)。"
  (if server "eglot" ""))

(defun literal/modeline--refresh-eglot-info (&rest _)
  "刷新当前 buffer 的 Eglot 状态缓存。
Phase 4.2:由 `eglot-managed-mode-hook' / `eglot-server-initialized-hook'
驱动,不在每次 redisplay 调;状态只反映 stable 连接状态(PLAN §4.2)。"
  (setq literal:modeline--eglot-info
        (when (and (bound-and-true-p eglot--managed-mode)
                   (fboundp 'eglot-current-server))
          (let* ((server (ignore-errors (eglot-current-server)))
                 (name (literal/modeline--eglot-server-name server))
                 (live (and server
                            (fboundp 'jsonrpc-running-p)
                            (ignore-errors (jsonrpc-running-p server)))))
            (when (and name (not (string-empty-p name)))
              (list name (cond (live 'connected)
                               (server 'connecting)
                               (t 'disconnected))))))))

(with-eval-after-load 'flymake
  (add-hook 'flymake-after-diagnostics-hook
            #'literal/modeline--refresh-flymake-counts)
  (add-hook 'flymake-mode-hook
            #'literal/modeline--refresh-flymake-counts))

(with-eval-after-load 'eglot
  (add-hook 'eglot-managed-mode-hook
            #'literal/modeline--refresh-eglot-info)
  (add-hook 'eglot-server-initialized-hook
            #'literal/modeline--refresh-eglot-info))

(add-hook 'after-save-hook #'literal/modeline--reset-project-cache)
(add-hook 'after-change-major-mode-hook #'literal/modeline--reset-project-cache)
(add-hook 'find-file-hook #'literal/modeline--refresh-vc-state)
(add-hook 'after-save-hook #'literal/modeline--refresh-vc-state)
(add-hook 'after-revert-hook #'literal/modeline--refresh-vc-state)
;; Phase 4.2:VC 分支字符串由 vc-git-mode-hook 刷新(原仅 find-file/save/revert
;; 时刷新,checkout/branch switch 时 mode-line 一直显示旧分支,直到下一次 redisplay
;; 触发 :unknown 重算;这里补 hook 让分支切换立即刷新)。
(with-eval-after-load 'vc-git
  (add-hook 'vc-git-mode-hook #'literal/modeline--refresh-vc-state))

#+end_src

*** 左侧文件状态
#+begin_src emacs-lisp :noweb-ref appearance/modeline-left :tangle no
;; 左段：文件相关

(defun literal/modeline-file-icon ()
  "返回当前文件类型图标。"
  (if (literal/modeline-compact-p)
      ""
    (literal/modeline-icon 'file "file" (or (buffer-file-name) (buffer-name)))))

(defun literal/modeline-path ()
  "返回简化目录段。文件名由 `literal/modeline-buffer-name' 显示。"
  (cond
   ((literal/modeline-compact-p) "")
   ((not (buffer-file-name)) "")
   (t
    (let* ((file (buffer-file-name))
           (project-info (literal/modeline--project-info))
           (root (car-safe project-info))
           (project-name (cadr project-info))
           (relative (and root
                          (not (file-remote-p file))
                          (literal/modeline--file-in-directory-p file root)
                          (file-relative-name file root)))
           (relative-dir (and relative
                              (file-name-directory relative)))
           (display (cond
                     ((and relative-dir
                           (not (string= relative-dir "./")))
                      (literal/modeline--initials-path
                       (directory-file-name relative-dir)))
                     (relative
                      (literal/modeline--initials-path project-name))
                     (t
                      (when-let* ((directory (file-name-directory file)))
                        (literal/modeline--initials-path
                         (directory-file-name directory))))))
           (icon (literal/modeline-icon 'fa "nf-md-folder_open")))
      (if (and display
               (not (string-empty-p display))
               (not (string= display ".")))
          (literal/modeline--segment
           (literal/modeline--with-icon
            icon
            (literal/modeline--truncate display literal:modeline-path-max-width))
           'shadow)
        "")))))

(defun literal/modeline-buffer-name ()
  "返回带文件类型图标的当前 buffer 文件名或 buffer 名。"
  (if (literal/modeline-compact-p)
      ""
    (let* ((name (if-let* ((file (buffer-file-name)))
                     (file-name-nondirectory file)
                   (buffer-name)))
           (width (pcase (literal/modeline-tier)
                    ('wide 32)
                    ('medium 24)
                    (_ 18))))
      (literal/modeline--segment
       (literal/modeline--with-icon
        (literal/modeline-file-icon)
        (literal/modeline--truncate name width))
       'font-lock-function-name-face))))

(defun literal/modeline-buffer-name-section ()
  "返回文件名段。当前面无路径段时不加前置分隔符。"
  (let ((name (literal/modeline-buffer-name)))
    (cond
     ((string-empty-p name) "")
     ((string-empty-p (literal/modeline-path)) name)
     (t (literal/modeline--separator-before name)))))

(defun literal/modeline-flymake ()
  "返回 Flymake 错误/警告数。"
  (if (and (not (literal/modeline-compact-p))
           (not (literal/modeline-narrow-p))
           (bound-and-true-p flymake-mode)
           (fboundp 'flymake-diagnostics))
      (let* ((errors (or (car literal:modeline--flymake-counts) 0))
             (warnings (or (cadr literal:modeline--flymake-counts) 0))
             (total (+ errors warnings))
             (tier (literal/modeline-tier))
             (icon (cond
                    ((> errors 0) (literal/modeline-icon 'fa "nf-md-times_circle"))
                    ((> warnings 0) (literal/modeline-icon 'fa "nf-md-alert"))
                    (t (literal/modeline-icon 'fa "nf-md-check_circle"))))
             (text (pcase tier
                     ('wide (format "E:%d W:%d" errors warnings))
                     ('medium (format "%d:%d" errors warnings))
                     (_ (number-to-string total))))
             (face (cond
                    ((> errors 0) 'error)
                    ((> warnings 0) 'warning)
                    (t 'success))))
        (literal/modeline--segment
         (literal/modeline--with-icon icon text)
         face))
    ""))

(defun literal/modeline-eglot ()
  "返回 Eglot 服务器与状态。"
  (when (and (bound-and-true-p eglot--managed-mode)
             (fboundp 'eglot-current-server))
    (literal/modeline--refresh-eglot-info))
  (if-let* ((info literal:modeline--eglot-info)
            (name (car-safe info))
            (status (cadr info)))
      (let* ((point (pcase status
                      ('connected "●")
                      ('connecting "◐")
                      (_ "○")))
             (face (pcase status
                     ('connected 'success)
                     ('connecting 'warning)
                     (_ 'shadow)))
             (text (if (literal/modeline-wide-p)
                       (format "%s %s" name point)
                     point)))
        (if (or (literal/modeline-narrow-p)
                (literal/modeline-compact-p))
            ""
          (literal/modeline--segment
           (literal/modeline--with-icon
            (literal/modeline-icon 'fa "nf-md-server_network")
            text)
           face)))
    ""))

(defun literal/modeline-position ()
  "返回当前光标位置百分比与 buffer 大小。"
  (if (or (literal/modeline-narrow-p)
          (literal/modeline-compact-p))
      ""
    (let* ((range (max 1 (- (point-max) (point-min))))
           (percent (round (* 100.0 (/ (float (- (point) (point-min))) range))))
           (text (if (literal/modeline-wide-p)
                     (format "%s %s"
                             (literal/modeline--with-icon
                              (literal/modeline-icon 'fa "nf-md-flag_checkered")
                              (format "%d%%%%" percent))
                             (literal/modeline--with-icon
                              (literal/modeline-icon 'fa "nf-md-format_list_bulleted")
                              (file-size-human-readable (buffer-size))))
                   (format "%d%%%%" percent))))
      (literal/modeline--segment
       (literal/modeline--with-icon
        (if (literal/modeline-wide-p)
            ""
          (literal/modeline-icon 'fa "nf-md-flag_checkered"))
        text)
       'font-lock-doc-face))))

(defun literal/modeline-major-mode ()
  "返回 major-mode 简化名。"
  (let* ((text (symbol-name major-mode))
         (icon (literal/modeline-icon 'fa "nf-md-code_tags")))
    (literal/modeline--segment
     (literal/modeline--with-icon icon text)
     'font-lock-keyword-face)))

(defun literal/modeline-major-mode-section ()
  "返回 compact 档左侧 major-mode 段。"
  (let ((mode (literal/modeline-major-mode)))
    (if (literal/modeline-compact-p) mode "")))

(defun literal/modeline-right-major-mode ()
  "返回右段 major-mode。compact 档由左侧独占显示。"
  (if (literal/modeline-compact-p)
      ""
    (literal/modeline-major-mode)))

#+end_src

*** 右侧系统状态
#+begin_src emacs-lisp :noweb-ref appearance/modeline-right :tangle no
;; 右段：Emacs / 系统 / 插件状态

(defun literal/modeline-format-time ()
  "返回格式化的时间字符串。"
  (if (and literal:modeline-show-time
           (not (literal/modeline-compact-p)))
      ;; Phase 4.2:用 display-time-mode 1 周期更新的 \`display-time-string\`
      ;; 而非每次 redisplay 重新 format-time-string(PLAN §4.2)。
      ;; 没启用 display-time-mode 时降级为 format-time-string(单次调用)。
      (let* ((time (or (and (boundp 'display-time-string)
                            (stringp display-time-string)
                            (not (string-empty-p display-time-string))
                            display-time-string)
                       (format-time-string "%H:%M")))
             (icon (literal/modeline-icon
                    'fa "nf-md-clock")))
        (literal/modeline--segment
         (literal/modeline--with-icon icon time)
         'font-lock-string-face))
    ""))

(defun literal/modeline-battery ()
  "返回电池状态。"
  (if (literal/modeline-wide-p)
      (let* ((text (and (boundp 'battery-mode-line-string)
                        (stringp battery-mode-line-string)
                        (string-trim battery-mode-line-string)))
             (percent (and text
                           (string-match "\\([0-9]+\\)%%" text)
                           (string-to-number (match-string 1 text))))
             (face (cond
                    ((not percent) 'font-lock-doc-face)
                    ((>= percent 50) 'success)
                    ((>= percent 20) 'warning)
                    (t 'error))))
        (if (and text (not (string-empty-p text)))
            (literal/modeline--segment
             (literal/modeline--with-icon
              (literal/modeline-icon 'fa "nf-md-battery")
              (literal/modeline--escape-percent text))
             face)
          ""))
    ""))

(defun literal/modeline-global-status ()
  "返回不与自定义段重复的 `global-mode-string' 内容。"
  (if (literal/modeline-compact-p)
      ""
    (let* ((items (and (boundp 'global-mode-string) global-mode-string))
           (filtered (seq-remove
                      (lambda (item)
                        (eq item 'battery-mode-line-string))
                      (if (listp items) items (list items))))
           (text (string-trim (format-mode-line filtered))))
      (if (string-empty-p text) "" text))))

(defun literal/modeline-vcs-info ()
  "返回当前 buffer 所在 VCS 分支名。"
  (if (and literal:modeline-show-vcs
           (not (literal/modeline-compact-p)))
      (condition-case nil
          (let ((line (and (bound-and-true-p vc-mode)
                           (stringp vc-mode)
                           vc-mode)))
            (if (and line (string-match "\\(?:Git\\|Hg\\|SVN\\)[-:]\\([^() ]+\\)" line))
                (let ((branch (string-trim (match-string 1 line))))
                  (literal/modeline--segment
                   (literal/modeline--with-icon
                    (literal/modeline-icon 'fa "nf-md-source_branch")
                    branch)
                   'font-lock-type-face))
              ""))
        (error ""))
    ""))

(defun literal/modeline-git-status ()
  "返回当前 buffer 的 Git/VC 状态。"
  (if (or (literal/modeline-compact-p)
          (not buffer-file-name))
      ""
    (when (eq literal:modeline--vc-state-cache :unknown)
      (literal/modeline--refresh-vc-state))
    (let* ((state (cond
                   ((buffer-modified-p) 'buffer-modified)
                   (t literal:modeline--vc-state-cache)))
           (spec (pcase state
                   ('up-to-date (list "clean" "nf-md-check_circle" 'success))
                   ('edited (list "edited" "nf-md-pencil" 'warning))
                   ('buffer-modified (list "unsaved" "nf-md-content_save_alert" 'warning))
                   ('added (list "added" "nf-md-plus_circle" 'success))
                   ('removed (list "removed" "nf-md-minus_circle" 'error))
                   ('missing (list "missing" "nf-md-alert_circle" 'error))
                   ('conflict (list "conflict" "nf-md-alert_octagon" 'error))
                   ('needs-merge (list "merge" "nf-md-source_merge" 'warning))
                   ('needs-update (list "update" "nf-md-cloud_download" 'warning))
                   ('ignored (list "ignored" "nf-md-eye_off" 'shadow))
                   ('unregistered (list "untracked" "nf-md-help_circle" 'warning))
                   (_ nil))))
      (if spec
          (pcase-let ((`(,text ,icon ,face) spec))
            (literal/modeline--segment
             (literal/modeline--with-icon
              (literal/modeline-icon 'fa icon)
              text)
             face))
        ""))))

(defun literal/modeline-coding ()
  "返回当前 buffer 的编码系统缩写名。"
  (if (or (literal/modeline-narrow-p)
          (literal/modeline-compact-p))
      ""
    (let* ((base (coding-system-base buffer-file-coding-system))
           (name (upcase (symbol-name (or base 'undecided)))))
      (literal/modeline--segment
       (literal/modeline--with-icon
        (literal/modeline-icon 'fa "nf-md-lock")
        name)
       'font-lock-string-face))))

(defun literal/modeline-location ()
  "返回当前行列位置。"
  (if (literal/modeline-compact-p)
      ""
    (literal/modeline--segment
     (literal/modeline--with-icon
      (literal/modeline-icon 'fa "nf-md-map_marker")
      "(%l,%c)")
     'shadow)))

#+end_src

*** 布局与初始化
#+begin_src emacs-lisp :noweb-ref appearance/modeline-layout :tangle no
;; 默认 mode-line 格式

(defvar literal:modeline-active-format nil
  "自写 starship 风格 mode-line 的 active 格式。")

(defvar literal:modeline-inactive-format nil
  "自写 starship 风格 mode-line 的 inactive 格式。")

(setq literal:modeline-active-format
      '(" "
        (:eval (literal/modeline-path))
        (:eval (literal/modeline-buffer-name-section))
        (:eval (literal/modeline--separator-before (literal/modeline-vcs-info)))
        (:eval (literal/modeline--separator-before (literal/modeline-git-status)))
        (:eval (literal/modeline--separator-before (literal/modeline-coding)))
        (:eval (literal/modeline--separator-before (literal/modeline-position)))
        (:eval (literal/modeline--separator-before (literal/modeline-location)))
        (:eval (literal/modeline-major-mode-section))
        mode-line-format-right-align
        " "
        (:eval (literal/modeline-flymake))
        (:eval (literal/modeline--separator-before (literal/modeline-eglot)))
        (:eval (literal/modeline--separator-before (literal/modeline-global-status)))
        (:eval (literal/modeline--separator-before (literal/modeline-right-major-mode)))
        (:eval (literal/modeline--separator-before (literal/modeline-battery)))
        (:eval (literal/modeline--separator-before (literal/modeline-format-time)))))

(setq literal:modeline-inactive-format
      '(" "
        (:eval (literal/modeline-path))
        (:eval (literal/modeline-buffer-name-section))
        (:eval (literal/modeline--separator-before (literal/modeline-vcs-info)))
        (:eval (literal/modeline--separator-before (literal/modeline-git-status)))
        (:eval (literal/modeline--separator-before (literal/modeline-location)))
        (:eval (literal/modeline-major-mode-section))
        mode-line-format-right-align
        " "
        (:eval (literal/modeline--separator-before (literal/modeline-right-major-mode)))
        (:eval (literal/modeline--separator-before (literal/modeline-format-time)))))

;; 初始化

(setq-default mode-line-format literal:modeline-active-format)
(setq-default mode-line-inactive-format literal:modeline-inactive-format)

;; Phase 4.2:size-indication-mode / display-battery-mode 在 *主题与字体*
;; (theme-fonts) 域统一初始化,不在 mode-line 域重复(PLAN §4.2)。
;; 时间:用 display-time-mode 1 的 \`display-time-string\` 更新源,
;; 不在每次 redisplay 重新 format-time-string。
(display-time-mode 1)

#+end_src

#+begin_src emacs-lisp
<<appearance/modeline-core>>
<<appearance/modeline-cache>>
<<appearance/modeline-left>>
<<appearance/modeline-right>>
<<appearance/modeline-layout>>
#+end_src

** Tab-line
:PROPERTIES:
:CUSTOM_ID: tab-line
:END:
基于内置 =tab-line= 的 per-frame 文件标签,隔离多个 emacsclient frame 的缓冲区序列。

*** 常量与窗口定位
#+begin_src emacs-lisp :noweb-ref appearance/tab-line-core :tangle no

(require 'tab-line)

;; 关闭内置 tab-bar,改用 window-local `tab-line' 作为文件标签栏。
(tab-bar-mode -1)

(defconst literal:tabs-name-max-width 36
  "单个标签名最大宽度。")

(defun literal/tab-visible-p (buffer)
  "判断 BUFFER 是否应该显示在标签栏中。
白名单机制:只有关联文件的 buffer 才显示。"
  (and (buffer-live-p buffer)
       (buffer-file-name buffer)))

(defun literal--tabs-target-window (&optional window)
  "返回标签命令应优先操作的 WINDOW。"
  (or (and (window-live-p window) window)
      (selected-window)))
#+end_src

*** Per-frame 缓冲区跟踪
#+begin_src emacs-lisp :noweb-ref appearance/tab-line-tracking :tangle no

;; Per-frame 标签缓冲区跟踪(Phase 4.1 硬需求:多 emacsclient frame 隔离)。
;; 每个 frame 维护一个有序的文件 buffer 列表(frame parameter
;; `literal--frame-tab-buffers'),window-buffer-change 是主要注册路径。

(defun literal--tabs-get-frame-buffer-list (&optional frame)
  "返回 FRAME 的标签缓冲区列表。"
  (frame-parameter (or frame (selected-frame)) 'literal--frame-tab-buffers))

(defun literal--tabs-set-frame-buffer-list (buffers &optional frame)
  "设置 FRAME 的标签缓冲区列表为 BUFFERS。"
  (set-frame-parameter (or frame (selected-frame)) 'literal--frame-tab-buffers buffers))

(defun literal--tabs-register-buffer (buffer &optional frame)
  "将 BUFFER 注册到 FRAME 的标签列表尾部(去重)。"
  (let* ((target-frame (or frame (selected-frame)))
         (buffers (or (literal--tabs-get-frame-buffer-list target-frame) '())))
    (unless (memq buffer buffers)
      (literal--tabs-set-frame-buffer-list
       (append buffers (list buffer))
       target-frame))))

(defun literal--tabs-unregister-buffer (buffer &optional frame)
  "从 FRAME 的标签列表中移除 BUFFER。"
  (let* ((target-frame (or frame (selected-frame)))
         (buffers (or (literal--tabs-get-frame-buffer-list target-frame) '())))
    (when (memq buffer buffers)
      (literal--tabs-set-frame-buffer-list
       (delq buffer buffers)
       target-frame))))

(defun literal--tabs-on-window-buffer-change (frame-or-window)
  "缓冲区变更时,把新缓冲区注册到对应 frame。
FRAME-OR-WINDOW 可以是 frame(全局 hook)或 window(buffer-local hook)。
Phase 4.1 合并:这是唯一注册路径,删除 find-file-hook / server-switch-hook
重复(PLAN §4.1)。"
  (cond
   ((frame-live-p frame-or-window)
    (walk-windows
     (lambda (window)
       (when (eq (window-frame window) frame-or-window)
         (let ((buffer (window-buffer window)))
           (when (and (buffer-live-p buffer)
                      (literal/tab-visible-p buffer)
                      (not (minibufferp buffer)))
             (literal--tabs-register-buffer buffer frame-or-window)))))
     'no-mini frame-or-window))
   ((window-live-p frame-or-window)
    (let ((buffer (window-buffer frame-or-window))
          (frame (window-frame frame-or-window)))
      (when (and (buffer-live-p buffer)
                 (literal/tab-visible-p buffer)
                 (not (minibufferp buffer)))
        (literal--tabs-register-buffer buffer frame))))))

(defun literal--tabs-on-kill-buffer ()
  "缓冲区被关闭时,从所有 frame 的标签列表中移除。"
  (let ((buffer (current-buffer)))
    (dolist (frame (frame-list))
      (when (frame-live-p frame)
        (let ((buffers (literal--tabs-get-frame-buffer-list frame)))
          (when (memq buffer buffers)
            (literal--tabs-set-frame-buffer-list (delq buffer buffers) frame)))))))

(defun literal/tabs-clear-for-frame (&optional frame)
  "清空 FRAME 的标签缓冲区列表(项目切换时调用)。
只清 frame parameter,不重建;后续 window-buffer-change 自然填充。"
  (interactive)
  (let ((target-frame (or frame (selected-frame))))
    (literal--tabs-set-frame-buffer-list nil target-frame)
    (walk-windows
     (lambda (window)
       (when (eq (window-frame window) target-frame)
         (set-window-parameter window 'tab-line-cache nil)))
     'no-mini target-frame)
    (when (called-interactively-p 'interactive)
      (message "已清空当前 frame 的标签列表"))))

(defun literal--tabs-on-frame-deletion (frame)
  "FRAME 删除时清理标签状态。"
  (literal--tabs-set-frame-buffer-list nil frame))

(defun literal--tabs-frame-buffers (&optional window)
  "返回 WINDOW 所在 frame 的存活 + 可见缓冲区列表。"
  (let* ((target-window (literal--tabs-target-window window))
         (target-frame (window-frame target-window))
         (frame-buffers (literal--tabs-get-frame-buffer-list target-frame)))
    (seq-filter (lambda (buf)
                  (and (buffer-live-p buf)
                       (literal/tab-visible-p buf)))
                (or frame-buffers '()))))
#+end_src

*** 标签渲染
#+begin_src emacs-lisp :noweb-ref appearance/tab-line-render :tangle no

;; 标签渲染(Phase 4.1:不再运行时切 face;交给 defface 继承 + tab-line 内置 face)。

(defun literal/tab-line-tabs ()
  "返回当前窗口标签栏要展示的 buffer 列表。"
  (or (literal--tabs-frame-buffers (selected-window))
      (list (current-buffer))))

(defun literal/tab-line-tab-name (buffer &optional _buffers)
  "生成 BUFFER 的标签名(图标 + 名称 + 修改标记)。"
  (let* ((name (truncate-string-to-width
                (buffer-name buffer)
                literal:tabs-name-max-width 0 nil t))
         (icon (when (display-graphic-p)
                 (require 'nerd-icons)
                 (with-current-buffer buffer
                   (nerd-icons-icon-for-buffer))))
         (prefix (if (and (stringp icon)
                          (not (string-empty-p icon)))
                     (concat icon " ")
                   ""))
         (suffix (if (buffer-modified-p buffer) " ●" "")))
    (concat prefix name suffix)))

(defun literal--tab-line-help-echo (selected-p)
  "返回标签的中文提示文本。"
  (if selected-p
      "当前标签:鼠标中键关闭,右键打开菜单"
    "左键切换标签,鼠标中键关闭,右键打开菜单"))

(defun literal/tab-line-tab-name-format (tab tabs)
  "以接近 IDE 的样式格式化 TAB。
Phase 4.1:face 选择交给内置 `tab-line-tab-face-functions',不再手动
构造 RGB plist;current/inactive 用内置 face,自动跟随主题。"
  (let* ((buffer-p (bufferp tab))
         (selected-p (if buffer-p
                         (eq tab (window-buffer))
                       (cdr (assq 'selected tab))))
         (name (if buffer-p
                   (funcall tab-line-tab-name-function tab tabs)
                 (cdr (assq 'name tab))))
         (face (if selected-p 'tab-line-tab-current 'tab-line-tab-inactive))
         (label (concat " " (string-replace "%" "%%" name)))
         (help-echo (literal--tab-line-help-echo selected-p)))
    (dolist (fn tab-line-tab-face-functions)
      (setq face (funcall fn tab tabs face buffer-p selected-p)))
    (propertize
     (concat label
             (when (and tab-line-close-button-show
                        (or (eq tab-line-close-button-show t)
                            (eq tab-line-close-button-show
                                (if selected-p 'selected 'non-selected))))
               tab-line-close-button))
     'face face
     'keymap tab-line-tab-map
     'mouse-face (unless selected-p 'tab-line-highlight)
     'help-echo help-echo
     'follow-link 'ignore
     'tab tab)))
#+end_src

*** 按钮与 Face
#+begin_src emacs-lisp :noweb-ref appearance/tab-line-faces :tangle no

;; 按钮文本 + defface(Phase 4.1:删除 170 行显式 RGB face 预设,
;; 改用 defface 继承主题语义 face,主题切换自动更新)。

(defun literal/apply-tab-line-button-preset ()
  "应用统一的标签栏按钮文本样式。"
  (setq tab-line-close-button
        (propertize " x "
                    'rear-nonsticky nil
                    'keymap tab-line-tab-close-map
                    'mouse-face 'tab-line-close-highlight
                    'help-echo "关闭标签")
        tab-line-left-button
        (propertize " < "
                    'rear-nonsticky nil
                    'keymap tab-line-left-map
                    'mouse-face 'tab-line-highlight
                    'help-echo "向左滚动标签栏")
        tab-line-right-button
        (propertize " > "
                    'rear-nonsticky nil
                    'keymap tab-line-right-map
                    'mouse-face 'tab-line-highlight
                    'help-echo "向右滚动标签栏")))

;; Face 走主题语义色继承(替代原 apply-tab-line-face-preset 的 RGB 计算):
;; - 当前标签:继承 mode-line(强对比底色) + 加粗 + 下划线
;; - 非当前标签:继承 shadow(低对比)
;; - 高亮 hover:继承 mode-line-highlight
;; TTY 下 unspecified/inherit 自动降级,无需专门 TTY plist。
(set-face-attribute 'tab-line nil
                    :inherit nil
                    :height 0.95
                    :box nil
                    :overline nil
                    :underline nil)
(set-face-attribute 'tab-line-tab-current nil
                    :inherit 'mode-line
                    :weight 'semibold
                    :underline t)
(set-face-attribute 'tab-line-tab-inactive nil
                    :inherit 'shadow
                    :box nil)
(set-face-attribute 'tab-line-highlight nil
                    :inherit 'mode-line-highlight)
(set-face-attribute 'tab-line-close-highlight nil
                    :inherit 'error
                    :background 'unspecified
                    :box nil
                    :underline nil)
#+end_src

*** 标签操作
#+begin_src emacs-lisp :noweb-ref appearance/tab-line-actions :tangle no

;; 标签切换与关闭(Phase 4.1:close 语义统一 bury+unregister,无 kill buffer)。

(defun literal--tabs-switch-to-buffer (buffer &optional window)
  "在 WINDOW 中切换到 BUFFER,并刷新标签显示。"
  (when (buffer-live-p buffer)
    (let ((target-window (literal--tabs-target-window window))
          (switch-to-buffer-obey-display-actions nil))
      (with-selected-window target-window
        (switch-to-buffer buffer)
        (tab-line-force-update t)))))

(defun literal/tabs-next (&optional arg)
  "切换到当前标签列表中的下一个标签。ARG 为步数。"
  (interactive "p")
  (let* ((target-window (literal--tabs-target-window))
         (buffers (literal--tabs-frame-buffers target-window))
         (current (window-buffer target-window))
         (position (seq-position buffers current))
         (step (or arg 1)))
    (when (and position buffers)
      (literal--tabs-switch-to-buffer
       (nth (mod (+ position step) (length buffers)) buffers)
       target-window))))

(defun literal/tabs-previous (&optional arg)
  "切换到当前标签列表中的上一个标签。ARG 为步数。"
  (interactive "p")
  (literal/tabs-next (- (or arg 1))))

(defun literal/tabs-select-index (index)
  "切换到当前标签列表中的第 INDEX 个标签。"
  (interactive)
  (let* ((target-window (literal--tabs-target-window))
         (buffers (literal--tabs-frame-buffers target-window))
         (buffer (nth (1- index) buffers)))
    (if buffer
        (literal--tabs-switch-to-buffer buffer target-window)
      (user-error "当前标签数量不足 %d 个" index))))

(defun literal/tabs-close-buffer (&optional window)
  "关闭当前标签(bury + unregister,不 kill buffer)。
Phase 4.1:键盘与鼠标关闭语义一致,修原鼠标分支缺 fallback 的不对称。"
  (interactive)
  (let* ((target-window (literal--tabs-target-window window))
         (buffer (window-buffer target-window))
         (target-frame (window-frame target-window)))
    (literal--tabs-unregister-buffer buffer target-frame)
    (if-let* ((next (seq-find (lambda (b)
                                (and (buffer-live-p b)
                                     (literal/tab-visible-p b)
                                     (not (eq b buffer))))
                              (literal--tabs-get-frame-buffer-list target-frame))))
        (literal--tabs-switch-to-buffer next target-window)
      (with-selected-window target-window
        (bury-buffer buffer)
        (let ((fallback (seq-find (lambda (b)
                                    (and (buffer-live-p b)
                                         (not (eq b buffer))))
                                  (buffer-list target-frame))))
          (when fallback
            (switch-to-buffer fallback)))
        (tab-line-force-update t)))))

(defun literal/tab-line-close-tab (tab)
  "鼠标关闭按钮回调:与键盘 close 走同一路径(语义对称)。"
  (interactive)
  (let ((buffer (if (bufferp tab) tab (current-buffer))))
    (when (buffer-live-p buffer)
      (save-window-excursion
        (select-window (literal--tabs-target-window))
        (switch-to-buffer buffer)
        (literal/tabs-close-buffer)))))
#+end_src

*** 全局 Hook
#+begin_src emacs-lisp :noweb-ref appearance/tab-line-hooks :tangle no

;; 全局配置与 Hook 注册(Phase 4.1:window-buffer-change 是唯一注册路径)。

(setq tab-line-tabs-function #'literal/tab-line-tabs
      tab-line-tab-name-function #'literal/tab-line-tab-name
      tab-line-tab-name-format-function #'literal/tab-line-tab-name-format
      tab-line-close-tab-function #'literal/tab-line-close-tab
      tab-line-close-button-show 'selected
      tab-line-new-button-show nil
      tab-line-switch-cycling t
      tab-line-separator "")

(literal/apply-tab-line-button-preset)

(global-tab-line-mode 1)

;; Per-frame 缓冲区跟踪 hook
;; Phase 4.1 合并:删除 find-file-hook / server-switch-hook 第三路径
;; (window-buffer-change-functions 已覆盖;server-switch-hook 原为补漏
;; daemon emacsclient 场景,window-buffer-change 也兼容该场景)。
(add-hook 'window-buffer-change-functions #'literal--tabs-on-window-buffer-change)
(add-hook 'kill-buffer-hook #'literal--tabs-on-kill-buffer)
(add-hook 'delete-frame-functions #'literal--tabs-on-frame-deletion)

;; 项目切换时清空当前 frame 的标签列表(等价行为已在 *项目与导航* 域
;; project-navigation 子树通过 project-after-switch-project-functions 注册)。
#+end_src

#+begin_src emacs-lisp
<<appearance/tab-line-core>>
<<appearance/tab-line-tracking>>
<<appearance/tab-line-render>>
<<appearance/tab-line-faces>>
<<appearance/tab-line-actions>>
<<appearance/tab-line-hooks>>
#+end_src

** Git 与窗口路由
:PROPERTIES:
:CUSTOM_ID: git-display
:END:
提供 Git 文件/仓库操作及 Magit、终端等复用的 display-buffer 路由。

*** Git 命令
#+begin_src emacs-lisp :noweb-ref appearance/git-commands :tangle no


;; Git 操作辅助函数（供 C-c g 前缀和右键菜单复用）。
;; - Magit 状态页路由到底部 side-window（40% 高度）
;; - COMMIT_EDITMSG 经 server-window 路由到上方主编辑窗口
;;   （保持「上编辑 + 下 magit」布局一致）
;; - 文件级操作：blame / log / diff / stage / discard / timemachine
;; - 仓库级操作：push / pull / stash push / stash pop
;; 所有操作复用启动域缓存的 `literal:executable-git'。


;; 仓库检测辅助

(defun literal/git-repo-root (&optional file)
  "返回 FILE 所在 Git 仓库根目录。"
  (when-let* ((target (or file (buffer-file-name) default-directory)))
    (locate-dominating-file target ".git")))

(defun literal/git-buffer-file ()
  "返回当前 buffer 对应的文件，不存在时抛出用户错误。"
  (or (buffer-file-name)
      (user-error "当前缓冲区没有关联文件")))

(defun literal/git--ensure-repo (&optional file)
  "确保 FILE 或当前 buffer 位于 Git 仓库中，并返回仓库根目录。"
  (or (literal/git-repo-root file)
      (user-error "当前不在 Git 仓库中")))

(defun literal/git--executable ()
  "返回可用的 git 可执行文件路径并缓存兜底探测结果。"
  (or literal:executable-git
      (setq literal:executable-git (executable-find "git"))))

(defun literal/git--run (&rest args)
  "在当前仓库中执行 Git ARGS。"
  (let ((default-directory (literal/git--ensure-repo)))
    (with-temp-buffer
      (let ((status (apply #'process-file (literal/git--executable) nil t nil args))
            (output (string-trim (buffer-string))))
        (unless (eq status 0)
          (error "Git 命令失败: %s"
                 (if (string-empty-p output) (string-join args " ") output)))
        output))))

;; 文件级操作（C-c g 前缀）

(defun literal/git-status-dwim ()
  "打开当前仓库的 Magit 状态页。"
  (interactive)
  (magit-status-setup-buffer (literal/git--ensure-repo)))

(defun literal/git-blame-current-file ()
  "对当前文件执行 blame。"
  (interactive)
  (literal/git-buffer-file)
  (call-interactively #'magit-blame-addition))

(defun literal/git-log-current-file ()
  "查看当前文件历史。"
  (interactive)
  (literal/git-buffer-file)
  (call-interactively #'magit-log-buffer-file))

(defun literal/git-diff-current-file ()
  "查看当前文件 diff。"
  (interactive)
  (literal/git-buffer-file)
  (call-interactively #'magit-diff-buffer-file))

(defun literal/git-stage-current-file ()
  "Stage 当前文件。"
  (interactive)
  (let ((file (literal/git-buffer-file)))
    (literal/git--ensure-repo file)
    (literal/git--run "add" "--" file)
    (message "已 stage: %s" (file-name-nondirectory file))))

(defun literal/git-discard-current-file ()
  "丢弃当前文件所有未提交修改。"
  (interactive)
  (let ((file (literal/git-buffer-file)))
    (literal/git--ensure-repo file)
    (when (y-or-n-p (format "丢弃 %s 的所有未提交修改？"
                            (file-name-nondirectory file)))
      (literal/git--run "restore" "--source=HEAD" "--staged" "--worktree" "--" file)
      (revert-buffer t t t)
      (message "已丢弃: %s" (file-name-nondirectory file)))))

(defun literal/git-timemachine-toggle ()
  "切换当前文件的 git-timemachine。"
  (interactive)
  (literal/git-buffer-file)
  (call-interactively #'git-timemachine-toggle))

;; 仓库级操作

(defun literal/git-push-current-repo ()
  "Push 当前仓库。"
  (interactive)
  (literal/git--ensure-repo)
  (call-interactively #'magit-push-current-to-upstream))

(defun literal/git-pull-current-repo ()
  "Pull 当前仓库。"
  (interactive)
  (literal/git--ensure-repo)
  (call-interactively #'magit-pull-from-upstream))

(defun literal/git-stash-push ()
  "Stash 当前仓库中的修改。"
  (interactive)
  (literal/git--ensure-repo)
  (literal/git--run "stash" "push" "-m"
                    (format-time-string "stash-%Y%m%d-%H%M%S"))
  (message "已创建 stash"))

(defun literal/git-stash-pop ()
  "恢复最近一次 stash。"
  (interactive)
  (literal/git--ensure-repo)
  (when (y-or-n-p "恢复最近一次 stash？")
    (literal/git--run "stash" "pop")
    (message "已恢复最近一次 stash")))

(defun literal/git-commit-setup-buffer-maybe ()
  "当前 buffer 是 Git 提交消息文件时加载并启用 `git-commit-mode'。"
  (when (and buffer-file-name
             (member (file-name-nondirectory buffer-file-name)
                     '("COMMIT_EDITMSG" "MERGE_MSG" "TAG_EDITMSG"
                       "NOTES_EDITMSG" "PULLREQ_EDITMSG")))
    (require 'git-commit)
    (git-commit-setup-check-buffer)))

#+end_src

*** Display-buffer 路由
#+begin_src emacs-lisp :noweb-ref appearance/git-display :tangle no
;; display-buffer 辅助（窗口路由）

(cl-defun literal/register-side-window (regex side size &key slot width)
  "把 REGEX 匹配的 buffer 路由到 SIDE 边的 side-window，占用 SIZE 比例。

REGEX : buffer-name 匹配字符串正则
SIDE  : bottom / right / left / top（符号）
SIZE  : 0~1 浮点比例
SLOT  : 默认 0（同 side 多窗口的堆叠顺序）
WIDTH : 非 nil 强制按 window-width；nil（默认）时按 SIDE 自动推导
        （left/right → window-width，top/bottom → window-height）。

规则追加到 `display-buffer-alist' 末尾。顺序敏感：更具体的规则应先注册。"
  (let* ((slot (or slot 0))
         (use-width (or width (memq side '(left right))))
         (window-dim (if use-width 'window-width 'window-height)))
    (add-to-list 'display-buffer-alist
                 `(,regex
                   (display-buffer-in-side-window)
                   (side . ,side)
                   (slot . ,slot)
                   (,window-dim . ,size))
                 t)))

(defun literal/display-buffer-in-main-window (buffer &optional _alist)
  "把 BUFFER 显示在「主编辑窗口」并选中。
主编辑窗口 = 当前 frame 内最靠上（y 坐标最小）的非 side-window 窗口。
保证无论光标当前在哪个窗口（如底部 magit），COMMIT_EDITMSG
都稳定落到上方编辑窗口，与「上编辑 + 下 magit」布局一致。

若无候选（如 frame 只剩 side-window），退化为 `display-buffer-pop-up-window'。

同时用作 `display-buffer-alist' 的 action 函数（参数 ALIST 被忽略）和
`server-window' 的值（server.el 调用时只传 buffer 一个参数）。"
  (let* ((frame (selected-frame))
         (candidates
          (cl-remove-if
           (lambda (w) (window-parameter w 'window-side))
           (window-list frame 'no-minibuf (frame-first-window frame))))
         (target (car (sort candidates
                            (lambda (a b)
                              (let ((ya (nth 1 (window-edges a nil t t)))
                                    (yb (nth 1 (window-edges b nil t t))))
                                (or (< ya yb)
                                    (and (= ya yb)
                                         (< (nth 0 (window-edges a nil t t))
                                            (nth 0 (window-edges b nil t t)))))))))))
    (if (window-live-p target)
        (progn
          (set-window-buffer target buffer)
          (select-window target)
          target)
      (display-buffer buffer '(display-buffer-pop-up-window)))))

(defun literal/display-or-focus (buffer action &optional select)
  "有则聚焦，无则按 ACTION 创建。
BUFFER 已在某窗口显示时，聚焦该窗口（SELECT 非 nil）；否则用 ACTION
（display-buffer 动作列表）创建新窗口。临时绑定 display-buffer-alist 为 nil，
避免递归匹配 display-buffer-alist 中的规则。"
  (when (buffer-live-p buffer)
    (if-let* ((win (get-buffer-window buffer)))
        (when select (select-window win))
      (let ((display-buffer-alist nil))
        (display-buffer buffer action)))))
#+end_src

#+begin_src emacs-lisp
<<appearance/git-commands>>
<<appearance/git-display>>
#+end_src

** 主题与字体
:PROPERTIES:
:CUSTOM_ID: theme-fonts
:END:
背景透明度、字体高度、行号、括号匹配、鼠标支持、ef-themes、spacious-padding、ultra-scroll、pulsar。

+ *主题* : =ef-themes= 通过本域 [[#color-scheme][颜色方案]] 跟随 Darkman 自动切换深浅色
+ *字体大小* :110(11pt),由 =literal:default-font-height= 控制
+ *行号* :相对行号 + 自适应宽度
+ 所有 display-dependent 设置都通过 =literal/apply-frame-appearance= 在新 frame 创建时触发

#+begin_src emacs-lisp
;; 背景透明度(仅 GUI)
(defcustom literal:frame-background-opacity 88
  "GUI frame 背景透明度(0~100,100 = 不透明)。"
  :type 'integer
  :group 'faces)

(defcustom literal:default-font-height 110
  "默认字体大小(1/10 pt)。"
  :type 'integer
  :group 'faces)

;; 界面简化(通过 default-frame-alist 禁用菜单栏、工具栏、滚动条)
(dolist (param '((menu-bar-lines . 0)
                 (tool-bar-lines . 0)
                 (vertical-scroll-bars)
                 (horizontal-scroll-bars)))
  (unless (assq (car param) default-frame-alist)
    (push param default-frame-alist)))

;; 行号显示(相对行号 + 自适应宽度)
(setq display-line-numbers-type 'relative)
(global-display-line-numbers-mode 1)

(defun literal/adjust-line-number-width ()
  "按当前 buffer 的总行数设置行号列宽度,避免大文件滚动时列宽跳变。"
  (when (and (boundp 'display-line-numbers-width)
             buffer-file-name
             global-display-line-numbers-mode)
    (let* ((max-line (line-number-at-pos (point-max) 'absolute))
           (digits (max 3 (length (number-to-string max-line)))))
      (setq display-line-numbers-width (1+ digits))
      ;; 注意:不要在这里把 fringe 清零(set-window-fringes ... 0)。
      ;; diff-hl 用 fringe 显示 Git 差异、flymake-fringe-indicator-position 设为
      ;; left-fringe,fringe 宽度为 0 时这些指示器全部不可见。
      )))

(add-hook 'find-file-hook #'literal/adjust-line-number-width)

;; 在某些模式下禁用行号
(dolist (mode '(ghostel-mode term-mode eshell-mode shell-mode
                             dashboard-mode special-mode
                             agent-shell-mode))
  (add-hook (intern (concat (symbol-name mode) "-hook"))
            (lambda () (display-line-numbers-mode -1))))

;; 鼠标支持
(xterm-mouse-mode 1)
(context-menu-mode 1)

;; 高亮当前行
(global-hl-line-mode 1)

;; 统一 per-frame 外观设置入口
(defun literal/apply-terminal-faces ()
  "为当前 frame 应用终端 ANSI 颜色(仅 TTY)。"
  (condition-case err
      (let ((tty (selected-frame)))
        (set-face-attribute 'highlight tty :background "brightblack" :foreground "white")
        (set-face-attribute 'region tty :background "brightblack" :foreground "white")
        (set-face-attribute 'hl-line tty :background "brightblack")
        (set-face-attribute 'show-paren-match tty :background "cyan" :foreground "black")
        (set-face-attribute 'show-paren-mismatch tty :background "red" :foreground "white")
        (set-face-attribute 'lazy-highlight tty :background "brightblack" :foreground "brightyellow")
        (set-face-attribute 'isearch tty :background "cyan" :foreground "black")
        (set-face-attribute 'line-number tty :foreground "brightblack")
        (set-face-attribute 'line-number-current-line tty :foreground "brightgreen")
        (set-face-attribute 'mode-line tty :background 'unspecified :foreground "white" :box nil)
        (set-face-attribute 'mode-line-inactive tty :background 'unspecified :foreground "brightblack" :box nil)
        (set-face-attribute 'help-key-binding tty :foreground "cyan")
        (set-face-attribute 'button tty :foreground "cyan"))
    (error
     (message "literal/apply-terminal-faces: %s" (error-message-string err)))))

(defun literal/apply-frame-appearance (&optional frame)
  "为 FRAME 应用所有 display-dependent 外观设置。
+ GUI: 字体高度、透明度、window-divider、tab-line face 重算
+ TTY: 终端 ANSI 颜色覆盖、diff-hl margin 模式"
  (condition-case err
      (with-selected-frame (or frame (selected-frame))
        (let ((graphic-p (display-graphic-p)))
          ;; per-frame 光标类型:setq-default 是全局的,混用 GUI+TTY client 时
          ;; 后创建的 TTY frame 会把所有 GUI frame 的光标也改成 box。
          (set-frame-parameter nil 'cursor-type (if graphic-p 'bar 'box))
          (if graphic-p
              (progn
                (set-face-attribute 'default nil :height literal:default-font-height)
                (set-frame-parameter nil 'alpha-background literal:frame-background-opacity)
                (when (fboundp 'literal/apply-tab-line-face-preset)
                  (literal/apply-tab-line-face-preset))
                (when (fboundp 'ef-themes-take-over-modus-themes-mode)
                  (ef-themes-take-over-modus-themes-mode 1)))
            (progn
              (window-divider-mode -1)
              (literal/apply-terminal-faces)
              ;; TTY 没有 fringe,diff-hl 的 fringe 指示器不可见;改用 margin。
              ;; diff-hl 是 :defer t,这里 fboundp 守卫避免强制加载。
              (when (fboundp 'diff-hl-margin-mode)
                (diff-hl-margin-mode 1))))
          (force-mode-line-update t)))
    (error
     (message "literal/apply-frame-appearance: %s" (error-message-string err)))))

(literal/add-frame-hook #'literal/apply-frame-appearance)

;; Ef-themes(深浅色由后续 color-scheme 章节跟随 Darkman 控制)
(use-package ef-themes
  :defer t
  :bind
  (("<f6>" . ef-themes-rotate)
   ("C-<f6>" . ef-themes-select)
   ("M-<f6>" . ef-themes-load-random))
  :custom
  (ef-themes-mixed-fonts t)
  (ef-themes-italic-constructs t)
  :config
  (when (fboundp 'literal/apply-tab-line-face-preset)
    (add-hook 'ef-themes-after-load-theme-hook
              #'literal/apply-tab-line-face-preset)))

;; Spacious Padding(GUI 增加留白)
;; 无条件启用:daemon 启动时 (display-graphic-p) 为 nil,GUI client 永远进不了
;; 启用分支。spacious-padding 在 TTY 下是 no-op,可安全无条件启用。
(use-package spacious-padding
  :defer 0.5
  :custom
  (spacious-padding-widths
   '(:internal-border-width 8
                            :header-line-width 4
                            :mode-line-width 1
                            :tab-width 4
                            :right-divider-width 1
                            :scroll-bar-width 8))
  :config
  (spacious-padding-mode 1))

;; Ultra Scroll(GUI 像素级平滑滚动)
;; 同理无条件启用:TTY 下 ultra-scroll-mode 无效果,但 daemon GUI client 需要它。
(use-package ultra-scroll
  :defer 0.5
  :init
  (setq scroll-conservatively 101
        scroll-margin 0
        pixel-scroll-precision-interpolate-page t)
  :config
  (ultra-scroll-mode 1))

;; Pulsar(跳转后短暂高亮当前行)
(use-package pulsar
  :defer 0.5
  :custom
  (pulsar-pulse t)
  (pulsar-delay 0.045)
  (pulsar-iterations 8)
  (pulsar-face 'pulsar-cyan)
  :config
  (setq pulsar-pulse-functions
        '(recenter-top-bottom
          reposition-window
          other-window
          windmove-left windmove-right windmove-up windmove-down
          consult-line consult-ripgrep consult-buffer
          xref-find-definitions xref-find-references
          xref-go-back xref-go-forward
          literal/code-jump-back literal/code-jump-forward
          flymake-goto-next-error flymake-goto-prev-error))
  (pulsar-global-mode 1))

;; size-indication-mode / display-battery-mode(mode-line 右段)
(size-indication-mode 1)
(display-battery-mode 1)
;; 注:line-spacing 默认 nil(等效 0),且 setq 只影响当前 buffer;曾是纯 no-op,已删。
#+end_src

** 窗口与面板布局
:PROPERTIES:
:CUSTOM_ID: window-layout
:END:
=display-buffer-alist= 工具面板路由:把 magit / terminal / help / flycheck / compilation / grep 等面板固定到底部 side-window。

+ [[#git-display][Git 与窗口路由]] 已提供 =literal/register-side-window= / =literal/display-buffer-in-main-window= / =literal/display-or-focus= 与全部 =literal/git-*= 命令
+ =literal/register-side-window= 统一注册底部面板
+ COMMIT_EDITMSG 经 emacsclient/with-editor → server.el 打开,通过 =server-window= 路由到主窗口

#+begin_src emacs-lisp
;; 打开 COMMIT_EDITMSG 等提交消息文件时启用 git-commit-mode(函数内有文件名守卫)
(add-hook 'find-file-hook #'literal/git-commit-setup-buffer-maybe)

;; display-buffer-alist(工具面板路由)
(literal/register-side-window "^\\*magit: \\|magit-status-mode\\b" 'bottom 0.4)
(add-to-list 'display-buffer-alist
             '("^\\*git-commit\\|\\`COMMIT_EDITMSG\\|\\`MERGE_MSG"
               (literal/display-buffer-in-main-window))
             t)
(literal/register-side-window "^\\*ghostel\\|ghostel-mode\\b\\|^\\*term\\|term-mode\\b" 'bottom 0.33)
(literal/register-side-window "^\\*Help\\*\\|^\\*helpful\\|help-mode\\b\\|helpful-mode\\b" 'bottom 0.4)
(literal/register-side-window "^\\*Flymake diagnostics\\|flymake-diagnostics-buffer-mode\\b" 'bottom 0.3)
(literal/register-side-window "^\\*compilation\\|compilation-mode\\b" 'bottom 0.33)
(literal/register-side-window "^\\*grep\\|grep-mode\\b\\|^\\*xref\\|xref--xref-buffer-mode\\b" 'bottom 0.4)

;; COMMIT_EDITMSG 经 emacsclient/with-editor → server.el 打开
(setq server-window #'literal/display-buffer-in-main-window)
#+end_src

** 右键菜单与 Which-key 中文化
:PROPERTIES:
:CUSTOM_ID: which-key-ui
:END:
右键菜单翻译 + which-key 中文描述注册。

+ *context-menu* : 对 =context-menu-map= 做 advice,翻译右键菜单英文项(数据见 =data/context-menu-zh.el=)
+ *which-key 数据* : 集中维护 C-x / C-c / M-s / M-g 等前缀子树的中文描述,数据见 =data/which-key-zh.el=
  - 全局前缀 + 叶子命令: =literal:which-key-description-spec=
  - major mode 局部前缀: =literal:which-key-major-mode-description-spec=
  - 通用 key/description 替换: =literal:which-key-regexp-replacements=
  - =(KEY . DESC)= 表示叶子命令; =(KEY DESC CHILD...)= 表示前缀组,自动展开为完整键序列
+ which-key 本体在 *键位与补全* 域用 use-package 加载;本块仅注册描述,=with-eval-after-load 'which-key= 触发

#+begin_src emacs-lisp

;; Which-key 中文描述注册(数据由 =appearance/i18n-data= 加载)

(defun literal/which-key--join-key (prefix key)
  "将 PREFIX 与 KEY 合并为 which-key 可用的键序列字符串。"
  (if (and prefix (> (length prefix) 0))
      (concat prefix " " key)
    key))

(defun literal/which-key--flatten-spec (prefix spec)
  "将 PREFIX 下的单个 SPEC 展开为 ((key . desc) ...) 列表。"
  (pcase spec
    (`(,key ,desc . ,children)
     (let ((full-key (literal/which-key--join-key prefix key)))
       (append (list (cons full-key desc))
               (literal/which-key--flatten-specs full-key children))))
    (`(,key . ,desc)
     (list (cons (literal/which-key--join-key prefix key) desc)))
    (_
     (error "无法识别的 which-key 描述项: %S" spec))))

(defun literal/which-key--flatten-specs (prefix specs)
  "将 PREFIX 下的 SPECS 展开为平铺的 ((key . desc) ...) 列表。"
  (apply #'append
         (mapcar (lambda (spec)
                   (literal/which-key--flatten-spec prefix spec))
                 specs)))

(defconst literal:which-key-replacements
  (literal/which-key--flatten-specs nil literal:which-key-description-spec)
  "Which-key 全局中文描述映射表。")

(defvar literal:which-key-descriptions-applied nil
  "避免重复写入 `which-key-replacement-alist'。")

(defun literal/which-key-apply-descriptions ()
  "将中文描述注册到 `which-key'。"
  (when (and (featurep 'which-key)
             (not literal:which-key-descriptions-applied))
    (dolist (entry literal:which-key-regexp-replacements)
      (setq which-key-replacement-alist
            (append which-key-replacement-alist (list entry))))
    (dolist (entry literal:which-key-replacements)
      (which-key-add-key-based-replacements (car entry) (cdr entry)))
    (dolist (mode-entry literal:which-key-major-mode-description-spec)
      (let ((mode (car mode-entry))
            (specs (cdr mode-entry)))
        (dolist (entry (literal/which-key--flatten-specs nil specs))
          (which-key-add-major-mode-key-based-replacements
            mode (car entry) (cdr entry)))))
    (setq literal:which-key-descriptions-applied t)))

(with-eval-after-load 'which-key
  ;; 先注册 literal/set-key 收集的延迟描述
  (literal/register-pending-wk-descs)
  ;; 再注册集中维护的描述
  (literal/which-key-apply-descriptions))

#+end_src

** 颜色方案(ef-themes)
:PROPERTIES:
:CUSTOM_ID: color-scheme
:END:
ef-themes 通过 D-Bus 跟随 Darkman 自动切换深浅色。

+ =literal/color-scheme-init= 注册 D-Bus 监听器,深浅色变化时自动 =ef-themes-select= 对应主题
+ 通过 [[#frame][Frame 生命周期]] 在新 frame 创建时应用当前色彩状态
+ 导出 =literal/register-buffer-refresh!= / =literal/register-theme-refresh!= 供 Dashboard 使用

*** 状态与主题应用
#+begin_src emacs-lisp :noweb-ref appearance/color-scheme-core :tangle no


(require 'dbus)

;; frame 生命周期 hook（由启动与基础设施域的 Frame 块定义,已在更早位置加载）

;; 配置变量

(defcustom literal/color-scheme-light-theme 'ef-cyprus
  "浅色模式下加载的主题。"
  :type 'symbol
  :group 'appearance)

(defcustom literal/color-scheme-dark-theme 'ef-owl
  "深色模式下加载的主题。"
  :type 'symbol
  :group 'appearance)

(defcustom literal/color-scheme-light-bg "#f5f5f5"
  "浅色模式的背景色（用于防闪屏）。"
  :type 'string
  :group 'appearance)

(defcustom literal/color-scheme-dark-bg "#0a0a0a"
  "深色模式的背景色（用于防闪屏）。"
  :type 'string
  :group 'appearance)

(defconst literal/color-scheme-state-file
  (expand-file-name "var/color-scheme-state.el" user-emacs-directory)
  "颜色方案状态文件路径。")

(defvar literal/color-scheme-current-mode nil
  "当前颜色方案模式（'light 或 'dark）。")

(defvar literal/color-scheme--initialized nil
  "颜色方案模块是否已完成初始化。")

(defvar literal/color-scheme--applying-theme nil
  "是否正在执行主题切换，避免 frame hook 重入。")

(defun literal/color-scheme-clear-bootstrap-frame-colors ()
  "移除 early-init 为防闪屏注入的占位前景/背景色。"
  (dolist (alist-var '(default-frame-alist initial-frame-alist))
    (set alist-var
         (assq-delete-all
          'background-color
          (assq-delete-all 'foreground-color (symbol-value alist-var))))))

(defun literal/color-scheme-read-state ()
  "从状态文件读取当前模式。返回 'light 或 'dark，文件不存在或读取失败时返回 nil。"
  (condition-case err
      (when (file-exists-p literal/color-scheme-state-file)
        (let ((content (with-temp-buffer
                         (insert-file-contents literal/color-scheme-state-file)
                         (buffer-string))))
          (cond
           ((string-match-p "dark" content) 'dark)
           ((string-match-p "light" content) 'light)
           (t nil))))
    (error
     (message "[color-scheme] 无法读取状态文件: %s" (error-message-string err))
     nil)))

(defun literal/color-scheme-save-state (mode)
  "将当前模式保存到状态文件。MODE 为 'light 或 'dark。"
  (condition-case err
      (let ((dir (file-name-directory literal/color-scheme-state-file)))
        (unless (file-exists-p dir)
          (make-directory dir t))
        (with-temp-buffer
          (insert ";; color-scheme-state.el -*- lexical-binding: t; -*-\n")
          (insert ";; 自动生成，勿手动修改\n")
          (insert (format "(setq literal/color-scheme-current-mode '%s)\n" mode))
          (insert (format "(setq literal/color-scheme-current-bg \"%s\")\n"
                          (if (eq mode 'light)
                              literal/color-scheme-light-bg
                            literal/color-scheme-dark-bg)))
          (write-region (point-min) (point-max) literal/color-scheme-state-file)))
    (error
     (message "[color-scheme] 无法保存状态文件: %s" (error-message-string err)))))

;; D-Bus 接口常量

(defconst literal/color-scheme-dbus-service "nl.whynothugo.darkman"
  "Darkman D-Bus 服务名称。")

(defconst literal/color-scheme-dbus-path "/nl/whynothugo/darkman"
  "Darkman D-Bus 对象路径。")

(defconst literal/color-scheme-dbus-interface "nl.whynothugo.darkman"
  "Darkman D-Bus 接口。")

;; 核心函数

(defun literal/color-scheme-get-from-dbus ()
  "通过 D-Bus 获取当前 Darkman 模式。返回 'light、'dark 或 nil。"
  (condition-case err
      (when (featurep 'dbus)
        (let ((mode (dbus-get-property
                     :session
                     literal/color-scheme-dbus-service
                     literal/color-scheme-dbus-path
                     literal/color-scheme-dbus-interface
                     "Mode")))
          (cond
           ((string= mode "light") 'light)
           ((string= mode "dark") 'dark)
           (t nil))))
    (error
     (message "[color-scheme] D-Bus 获取失败: %s" (error-message-string err))
     nil)))

(defun literal/color-scheme-apply-theme (mode)
  "根据 MODE 加载对应的主题并保存状态。
MODE 为 'light 或 'dark。
终端 standalone 模式仅记录状态；daemon 模式下只要存在 GUI frame 即加载主题。"
  (setq literal/color-scheme-current-mode mode)
  (literal/color-scheme-save-state mode)
  (unless literal/color-scheme--applying-theme
    (let ((literal/color-scheme--applying-theme t)
          (target-theme (pcase mode
                          ('dark literal/color-scheme-dark-theme)
                          ('light literal/color-scheme-light-theme)
                          (_ nil))))
      (when target-theme
        (when (or (display-graphic-p)
                  (and (daemonp) (literal/graphic-frame-exists-p)))
          (unless (eq (car custom-enabled-themes) target-theme)
            (ef-themes-load-theme target-theme))
          (literal/color-scheme-clear-bootstrap-frame-colors)
          (message "[color-scheme] 已同步主题: %s" target-theme))))))

#+end_src

*** D-Bus 监听
#+begin_src emacs-lisp :noweb-ref appearance/color-scheme-dbus :tangle no
;; D-Bus 信号监听

(defvar literal/color-scheme-dbus-handler nil
  "D-Bus 信号监听器的注册 ID。")

(defun literal/color-scheme-register-listener ()
  "注册 D-Bus 信号监听器，响应 Darkman 模式变化。"
  (condition-case err
      (when (and (featurep 'dbus)
                 (not literal/color-scheme-dbus-handler))
        (setq literal/color-scheme-dbus-handler
              (dbus-register-signal
               :session
               literal/color-scheme-dbus-service
               literal/color-scheme-dbus-path
               literal/color-scheme-dbus-interface
               "ModeChanged"
               (lambda (mode)
                 (let ((new-mode (cond
                                  ((string= mode "light") 'light)
                                  ((string= mode "dark") 'dark))))
                   (when (and new-mode
                              (not (eq new-mode literal/color-scheme-current-mode)))
                     (message "[color-scheme] Darkman 模式变化: %s" mode)
                     (literal/color-scheme-apply-theme new-mode))))))
        (message "[color-scheme] 已注册 D-Bus 监听器"))
    (error
     (message "[color-scheme] 无法注册 D-Bus 监听器: %s" (error-message-string err))
     nil)))

#+end_src

*** Frame 初始化
#+begin_src emacs-lisp :noweb-ref appearance/color-scheme-init :tangle no
;; 初始化

(defun literal/graphic-frame-exists-p ()
  "返回当前会话中是否存在任意 GUI frame。"
  (catch 'found
    (dolist (frame (frame-list))
      (when (display-graphic-p frame)
        (throw 'found t)))
    nil))

(defun literal/color-scheme-init ()
  "初始化颜色方案适配：
1. 先从状态文件读取模式，立即应用主题
2. 异步验证 D-Bus，不一致则更新
3. 注册 D-Bus 监听器"
  (let ((initial-mode (or (literal/color-scheme-read-state)
                          (literal/color-scheme-get-from-dbus)
                          'dark)))
    (literal/color-scheme-apply-theme initial-mode)
    (setq literal/color-scheme--initialized t)
    (literal/color-scheme-register-listener)
    (run-with-idle-timer 1 nil
                         (lambda ()
                           (let ((dbus-mode (literal/color-scheme-get-from-dbus)))
                             (when (and dbus-mode
                                        (not (eq dbus-mode literal/color-scheme-current-mode)))
                               (message "[color-scheme] 状态文件与 D-Bus 不一致，更新为 %s" dbus-mode)
                               (literal/color-scheme-apply-theme dbus-mode)))))))

(defun literal/color-scheme-apply-for-frame (&optional frame)
  "在 FRAME 可用时补齐与主题相关的 frame 初始化。
主题是全局状态，不能在每个新 frame 上重复 `load-theme`，否则多 client
几乎同时创建 frame 时容易在 PGTK/GTK 初始化路径中崩溃。"
  (with-selected-frame (or frame (selected-frame))
    (when (display-graphic-p)
      (when (fboundp 'ef-themes-take-over-modus-themes-mode)
        (ef-themes-take-over-modus-themes-mode 1))
      (if (not literal/color-scheme--initialized)
          (literal/color-scheme-init)
        (let ((target-theme (pcase literal/color-scheme-current-mode
                              ('dark literal/color-scheme-dark-theme)
                              ('light literal/color-scheme-light-theme)
                              (_ nil))))
          (when (and target-theme
                     (not (eq (car custom-enabled-themes) target-theme)))
            (ef-themes-load-theme target-theme)
            (literal/color-scheme-clear-bootstrap-frame-colors)
            (message "[color-scheme] 新 frame 同步主题: %s" target-theme)))))))

(defun literal/color-scheme-delayed-init (&optional frame)
  "延迟初始化颜色方案（daemon 模式首帧创建时触发）。
仅执行一次，之后自动移除自身。"
  (when (and frame
             (display-graphic-p frame)
             (not literal/color-scheme--initialized))
    (with-selected-frame frame
      (require 'ef-themes)
      (literal/color-scheme-init)))
  (literal/remove-frame-hook #'literal/color-scheme-delayed-init))

;; daemon 模式：延迟到首帧创建时再加载主题和初始化
;; 非 daemon 模式：帧已存在，立即初始化
(if (daemonp)
    (progn
      (literal/add-frame-hook #'literal/color-scheme-delayed-init)
      (literal/add-frame-hook #'literal/color-scheme-apply-for-frame))
  (require 'ef-themes)
  (literal/color-scheme-init))

#+end_src

*** Face 与缓冲区刷新
#+begin_src emacs-lisp :noweb-ref appearance/color-scheme-refresh :tangle no
;; 自定义 face 颜色同步

;; Phase 4.4:删除 (require 'modus-themes)(ef-themes 是当前主题,不应耦合
;; modus-themes)。颜色通过 face 继承或 ef palette API 获取,不在此函数硬编码。
(defun literal/color-scheme-apply-custom-face-colors ()
  "主题切换后重新应用自定义 face 颜色。
Phase 4.4:修正 \`custom-help-section-title\` → \`literal-help-section-title\`
(defface 在 help 子树定义,原引用是历史名漂移,主题切换后 help 标题着色失效)。
不再依赖 modus-themes palette;dashboard-card 背景由 face 继承自动更新。"
  (when (facep 'literal-help-section-title)
    (set-face-attribute 'literal-help-section-title nil
                        :inherit 'font-lock-keyword-face)))

;; 主题切换后的级联刷新

(defvar literal/theme-refresh-functions nil
  "主题切换后需要调用的刷新函数列表。")

(defun literal/register-theme-refresh! (fn)
  "注册 FN 为主题切换后调用的刷新函数（幂等）。"
  (unless (memq fn literal/theme-refresh-functions)
    (setq literal/theme-refresh-functions
          (append literal/theme-refresh-functions (list fn)))))

(defun literal/run-theme-refresh ()
  "执行所有已注册的主题切换刷新函数。"
  (dolist (fn literal/theme-refresh-functions)
    (condition-case err
        (funcall fn)
      (error
       (message "[color-scheme] theme-refresh %S failed: %S"
                fn (error-message-string err))))))

(defvar literal/theme-buffer-refreshers nil
  "Alist of (MAJOR-MODE . REFRESH-FN) pairs。")

(defun literal/register-buffer-refresh! (mode refresh-fn)
  "为 MAJOR-MODE 注册主题切换后的 buffer 重渲染函数 REFRESH-FN。幂等。"
  (let ((entry (assq mode literal/theme-buffer-refreshers)))
    (unless (and entry (eq (cdr entry) refresh-fn))
      (if entry
          (setcdr entry refresh-fn)
        (push (cons mode refresh-fn) literal/theme-buffer-refreshers)))))

(defun literal/color-scheme-refresh-registered-buffers ()
  "重新渲染所有可见窗口中已注册 mode 的缓冲区。"
  (dolist (window (window-list nil 'no-minibuf))
    (when (window-live-p window)
      (let ((buffer (window-buffer window)))
        (when (buffer-live-p buffer)
          (with-current-buffer buffer
            (let ((refresher (cdr (assq major-mode literal/theme-buffer-refreshers))))
              (when (and refresher (functionp refresher))
                (condition-case err
                    (with-selected-window window
                      (funcall refresher))
                  (error
                   (message "[color-scheme] buffer-refresh %s failed: %S"
                            major-mode (error-message-string err))))))))))))

(literal/register-theme-refresh! #'literal/color-scheme-apply-custom-face-colors)
(literal/register-theme-refresh! #'literal/color-scheme-refresh-registered-buffers)
(add-hook 'ef-themes-after-load-theme-hook #'literal/run-theme-refresh)
(literal/run-theme-refresh)
#+end_src

#+begin_src emacs-lisp
<<appearance/color-scheme-core>>
<<appearance/color-scheme-dbus>>
<<appearance/color-scheme-init>>
<<appearance/color-scheme-refresh>>
#+end_src

* 编辑与文本
:PROPERTIES:
:CUSTOM_ID: editing
:END:
通用编辑习惯、DWIM 智能编辑函数、ghostel 终端集成。

** 通用编辑习惯
:PROPERTIES:
:CUSTOM_ID: editing-defaults
:END:
缩进 / tab、括号匹配、EditorConfig、选区扩展、Git 差异高亮、Smartparens、文档模式自动换行。

+ ~indent-tabs-mode nil~ + ~tab-width 2~ :默认用空格缩进
+ ~show-paren-mode~ :高亮匹配括号
+ ~save-place-mode~ / ~repeat-mode~ :内置体验增强
+ ~~editorconfig~ :遵循项目 =.editorconfig=
+ ~smartparens~ :替代 ~electric-pair-mode~ ,支持 wrap/unwrap/slurp/barf
+ ~diff-hl~ :fringe 显示 Git 差异(终端用 margin)
+ 文档模式(org/markdown/text)自动 =visual-line-mode=

#+begin_src emacs-lisp
;; 通用编辑习惯
(setq-default indent-tabs-mode nil
              tab-width 2
              fill-column 100
              tab-always-indent nil
              truncate-lines t
              word-wrap nil)

(show-paren-mode 1)
(save-place-mode 1)
(repeat-mode 1)
(when (boundp 'show-paren-context-when-offscreen)
  (setq show-paren-context-when-offscreen 'child-frame))

;; EditorConfig(项目级缩进和空白规则)
(use-package editorconfig
  :defer 0.3
  :custom
  (editorconfig-trim-whitespaces-mode
   (if (fboundp 'delete-trailing-whitespace-mode)
       'delete-trailing-whitespace-mode
     'ws-butler-mode))
  :config
  (editorconfig-mode 1))

;; Expand Region(逐步扩展/收缩选区)
(use-package expand-region
  :defer t
  :commands (er/expand-region er/contract-region))

;; 自动清理行尾空格(Emacs 31+ 用内置 mode,29-30 fallback ws-butler)
(if (fboundp 'delete-trailing-whitespace-mode)
    (add-hook 'text-mode-hook #'delete-trailing-whitespace-mode)
  (use-package ws-butler
    :defer t
    :hook ((text-mode . ws-butler-mode)
           (prog-mode . ws-butler-mode))))

;; Git 差异高亮(GUI 用 fringe,TTY 用 margin)
;; 原先 (unless (display-graphic-p) (diff-hl-margin-mode 1)) 在 daemon 启动期判断
;; display-graphic-p 永远为 nil → GUI client 也被强启 margin 模式(fringe 指示器被
;; margin 取代,且 margin 模式在 GUI 下有显示问题)。改为:margin 模式由 frame hook
;; 的 TTY 分支按需启用,GUI 走默认 fringe。
(use-package diff-hl
  :defer t
  :hook ((prog-mode . diff-hl-mode)
         (text-mode . diff-hl-mode))
  :config
  (global-diff-hl-mode 1))

;; Smartparens(智能括号匹配与结构化编辑)
(use-package smartparens
  :hook ((prog-mode . smartparens-mode)
         (markdown-mode . smartparens-mode)
         (org-mode . smartparens-mode))
  :config
  (require 'smartparens-config))

;; Crux(实用编辑函数集合)
(use-package crux
  :defer t
  :commands (crux-open-with
             crux-rename-file-and-buffer
             crux-delete-file-and-buffer
             crux-copy-file-preserve-attributes
             crux-duplicate-current-line-or-region))

;; Git blame 弹窗(查看某行最近 commit 消息,与 magit-blame 的逐行 blame 侧栏用途不同)
;; C-c g b 已被 literal/git-blame-current-file 占用,这里用 C-c g m(否则 :bind 是
;; git-messenger 唯一的加载触发器,被覆盖后整个包不可用)。
(use-package git-messenger
  :defer t
  :bind ("C-c g m" . git-messenger:popup-message)
  :custom
  (git-messenger:show-detail t)
  (git-messenger:use-magit-popup t))

;; 文档模式自动换行
(dolist (hook '(org-mode-hook markdown-mode-hook gfm-mode-hook text-mode-hook))
  (add-hook hook #'visual-line-mode))
#+end_src

** DWIM 智能编辑函数
:PROPERTIES:
:CUSTOM_ID: editing-dwim
:END:
智能复制 / 剪切 / 注释 / 重命名等 DWIM 函数,以及前向声明。

+ =literal/copy-dwim= :有选区复制选区,否则复制当前行(类 VS Code)
+ =delete-selection-mode= :有选区时输入/粘贴自动覆盖选区(替代旧 =literal/paste-dwim= ,内置且覆盖所有插入类命令,不止 yank)
+ =literal/cut-dwim= :有选区剪切选区,否则剪切整行
+ =literal/comment-dwim-stable= :稳定执行注释(region 优先)
+ =literal/rename-dwim= :eglot 管理时调 eglot-rename,否则 rename-visited-file
+ 全选用内置 =mark-whole-buffer= (C-a),关闭窗口+buffer 用内置 =kill-buffer-and-window= (C-x k)

#+begin_src emacs-lisp
(defun literal/copy-dwim ()
  "智能复制:有选区时复制选区,否则复制当前行。"
  (interactive)
  (cond
   ((use-region-p)
    (kill-ring-save (region-beginning) (region-end))
    (deactivate-mark)
    (message "已复制选区"))
   (t
    (kill-ring-save (line-beginning-position) (line-beginning-position 2))
    (message "已复制当前行"))))

;; 有选区时输入/粘贴自动覆盖选区(类 VS Code 行为)。
;; 替代旧 literal/paste-dwim:delete-selection-mode 覆盖所有插入类命令(不止
;; yank),一行内置设置即可,C-S-v 直接绑 yank。
(delete-selection-mode 1)

(defun literal/cut-dwim ()
  "智能剪切:有选区时剪切选区,否则剪切当前行。"
  (interactive)
  (cond
   ((use-region-p)
    (kill-region (region-beginning) (region-end)))
   (t
    (kill-whole-line))))

(defun literal/comment-dwim-stable ()
  "稳定执行注释/取消注释。优先处理 region,否则处理当前行。"
  (interactive)
  (if (use-region-p)
      (progn
        (comment-or-uncomment-region (region-beginning) (region-end))
        (deactivate-mark))
    (comment-line 1)))

(defun literal/rename-dwim ()
  "重命名符号或当前文件。"
  (interactive)
  (cond
   ((and (fboundp 'eglot-managed-p)
         (eglot-managed-p)
         (fboundp 'eglot-rename))
    (call-interactively #'eglot-rename))
   ((and buffer-file-name
         (fboundp 'rename-visited-file))
    (call-interactively #'rename-visited-file))
   (t
    (user-error "当前缓冲区不支持重命名"))))

(defun literal/reset-text-scale ()
  "重置当前缓冲区缩放级别。"
  (interactive)
  (text-scale-set 0))

;; 关闭 buffer + 窗口直接用内置 kill-buffer-and-window(C-x k 绑它)。
;; 原自定义版 (condition-case nil ... (error nil)) 会静默吞掉所有错误(如未
;; 保存确认被拒绝时用户得不到任何反馈),内置版行为一致且不吞错误。

;; indent-bars — tree-sitter 缩进竖线(Phase 7.2 配置)
;; 类似 indent-guide,但基于 tree-sitter,只对启用了 ts-mode 的语言生效。
;; PLAN §7.2 决定保留 + 配置(原 0 引用,本 commit 接入)。
(use-package indent-bars
  :defer t
  ;; 只在 ts-mode 启用;非 ts-mode 缺乏可靠的缩进 AST,indent-bars 会画错。
  :hook ((c-ts-mode c++-ts-mode go-ts-mode java-ts-mode json-ts-mode
          python-ts-mode rust-ts-mode typescript-ts-mode yaml-ts-mode) .
         indent-bars-mode)
  :custom
  ;; 竖线用浅色,继承 shadow;pattern 让连续缩进可视层级清晰
  (indent-bars-color nil)                       ; 不自己算颜色,follow 主题
  (indent-bars-color-by-depth '(:homogeneity 0.5)) ; 深度递减饱和度
  (indent-bars-pattern ".")                     ; 单字符 dot 列
  (indent-bars-width-frac 0.15)                 ; 竖线宽度(占字符宽度比例)
  (indent-bars-pad-pos 1)                       ; padding 位置
  (indent-bars-zigzag nil)                      ; 不用锯齿(避免 ts 节点抖动)
  (indent-bars-no-stretch-char nil))

;; goto-chg — 跳到上一处/下一处修改点(Phase 7.2 配置)
;; 提供 undo-tree 风格的"按修改点跳转",比 undo 一次一次翻历史更精准。
(use-package goto-chg
  :defer t
  :commands (goto-last-change goto-last-change-reverse))

;; multiple-cursors — 多光标编辑(Phase 7.2 配置)
;; 在选中 region 内每个匹配字符串处插入 cursor,适合批量重命名/格式化。
(use-package multiple-cursors
  :defer t
  :commands (mc/edit-lines
             mc/mark-next-like-this mc/mark-previous-like-this
             mc/mark-all-like-this mc/mark-all-like-this-in-defun))
#+end_src

** 终端集成 ghostel
:PROPERTIES:
:CUSTOM_ID: terminal
:END:
ghostel 终端模拟器(libghostty-vt 后端): =literal/open-terminal= 在底部 side-window 打开,自动 cd 到项目根目录。

+ 底部 side-window(高度 1/3),自动切换到项目根目录;不在项目则用当前目录;带前缀参数则用当前文件所在目录
+ 有已显示的终端窗口则聚焦;有终端 buffer 但未显示则显示到底部;无则新建
+ =C-h= 在 ghostel 内重绑为发送原始 Ctrl+H(全局被 backward-char 占用), =M-方向键= 保持窗口切换
+ 关键 hack:终端里的 =git commit= / =git rebase -i= 需要 EDITOR 返回;ghostel 的 pre-spawn-hook 跑在进程创建 *之前* , =get-buffer-process= 返回 nil,故不能用 =with-editor-export-git-editor= (会抛 "Cannot export environment variables in this buffer"),必须直接调 =with-editor--setup= (内部 API),手动绑定 =with-editor--envvar=

#+begin_src emacs-lisp
(eval-when-compile
  (require 'with-editor nil t))

;; agent-shell 后端标记的前向声明,避免 byte-compile free variable 警告
(defvar literal--agent-ghostel-p nil)

;; ghostel 配置

(defun literal/ghostel-export-git-editor ()
  "在 `ghostel-pre-spawn-hook' 里导出 GIT_EDITOR 到即将启动的 shell 进程。
等价于 `(with-editor \"GIT_EDITOR\")' 宏展开后的 setup 部分,但不带 body ——
只保留对当前动态绑定 `process-environment' 的副作用。"
  (when (fboundp 'with-editor--setup)
    (let ((with-editor--envvar "GIT_EDITOR"))
      (with-editor--setup))))

(defun literal/ghostel-send-ctrl-h ()
  "向 ghostel 发送 C-h(全局被 backward-char 占用,需显式重绑)。"
  (interactive)
  (ghostel-send-key "h" "ctrl"))

;; 接线:ghostel 包惰性加载,hook/keymap 加载前 void,须 after-load 守卫
(with-eval-after-load 'ghostel
  (add-hook 'ghostel-pre-spawn-hook #'literal/ghostel-export-git-editor)
  (when (boundp 'ghostel-mode-map)
    (define-key ghostel-mode-map (kbd "C-h") #'literal/ghostel-send-ctrl-h)))

;; 智能 ghostel:自动 cd 到项目根目录

(defconst literal:terminal-side-action
  '((display-buffer-in-side-window
     (side . bottom) (slot . 0) (window-height . 0.33)))
  "终端面板的 display-buffer action:底部 side-window,高度 1/3。")

(defun literal--terminal-existing-buffer ()
  "返回当前已存在的普通 ghostel buffer(非 agent 后端),无则 nil。"
  (cl-find-if
   (lambda (buf)
     (and (buffer-live-p buf)
          (with-current-buffer buf (derived-mode-p 'ghostel-mode))
          (not (buffer-local-value 'literal--agent-ghostel-p buf))))
   (buffer-list)))

(defun literal/open-terminal (&optional arg)
  "打开 ghostel 终端(底部 side-window,高度 1/3),自动切换到项目根目录。
如果不在项目中,则使用当前目录。
带前缀参数 ARG 时,使用当前文件所在目录。

有已显示的终端窗口则聚焦;有终端 buffer 但未显示则按
`literal:terminal-side-action' 显示到底部;无则新建。"
  (interactive "P")
  (let* ((project-root (when-let* ((proj (project-current nil)))
                         (project-root proj)))
         (shell (or shell-file-name
                    (getenv "SHELL")
                    "/bin/sh"))
         (default-directory
          (cond
           (arg default-directory)                  ; 前缀参数:当前文件目录
           (project-root project-root)              ; 在项目中:项目根目录
           (t default-directory))))                 ; 不在项目中:当前目录
    ;; let 绑定 shell-file-name(原 setq 是永久全局副作用,污染后续所有子进程)。
    ;; ghostel 在此 let 内 spawn,继承动态绑定的值;let 退出后恢复原值。
    (let ((shell-file-name shell)
          (buf (or (literal--terminal-existing-buffer)
                   ;; 无现存普通 ghostel:用 save-window-excursion 隔离窗口副作用
                   (let ((before (buffer-list)))
                     (save-window-excursion (ghostel))
                     (cl-find-if
                      (lambda (b)
                        (with-current-buffer b (derived-mode-p 'ghostel-mode)))
                      (cl-set-difference (buffer-list) before))))))
      (when (buffer-live-p buf)
        (literal/display-or-focus buf literal:terminal-side-action t)))))

;; Agent Shell(Phase 7.2 接入:原 0 引用,加 use-package + C-c a a 入口)
;; PLAN §7.2:Agent Shell 若保留,必须在 C-c a 或其他声明式入口中可达。
;; Agent Shell 是 AI agent 的 Emacs 端 UI,与 terminal / ghostel 独立。
;; agent-shell 主入口加载完整 UI;agent-shell-toggle 切换显示/隐藏。
(use-package agent-shell
  :defer t
  :commands (agent-shell agent-shell-toggle))
;; C-c a a 入口见 *前缀键体系* 块。

#+end_src

* 编程与开发
:PROPERTIES:
:CUSTOM_ID: programming
:END:
Eglot (LSP 客户端)、Tree-sitter、语言注册表、格式化、拼写检查、envrc、各语言 major mode。

** LSP / Tree-sitter / 诊断 / 格式化
:PROPERTIES:
:CUSTOM_ID: programming-tools
:END:
Eglot (LSP 客户端)、Tree-sitter、语言注册表、Apheleia 保存时格式化、Flycheck 诊断、envrc direnv 集成。

+ ~eglot~ :Emacs 29+ 内置 LSP 客户端,配合 =eglot-x= 扩展协议
+ ~treesit-enabled-modes t~ :Emacs 31 一键启用所有已安装 ts 模式
+ LSP 服务器注册延迟到首次进入 prog-mode(避免启动期 spawn 进程)
+ ~apheleia~ :保存时自动格式化(语言对应关系集中维护)
+ ~flycheck~ :代码诊断(替代 flymake)
+ ~envrc~ :direnv buffer-local 环境变量注入

#+begin_src emacs-lisp
(require 'json)

;; 语言能力表(Phase 3.2:单一真相源)
;; PLAN §3.2:统一 major mode / tree-sitter mode / LSP server / formatter,
;; 避免四份独立 alist 漂移。下面的 defconst 是派生数据;唯一真相源是
;; `literal:language-capabilities',新增语言只改这一份。
(defconst literal:language-capabilities
  '((:modes (c-mode c-ts-mode)
     :ts-mode c-ts-mode
     :server ("ccls")
     :formatter clang-format)
    (:modes (c++-mode c++-ts-mode)
     :ts-mode c++-ts-mode
     :server ("ccls")
     :formatter clang-format)
    (:modes (gdscript-mode)
     :server nil                       ; gdscript LSP 由 godot 自带
     :formatter nil)
    (:modes (go-mode go-ts-mode)
     :ts-mode go-ts-mode
     :server ("gopls")
     :formatter gofmt)
    (:modes (java-mode java-ts-mode)
     :ts-mode java-ts-mode
     :server nil
     :formatter nil)
    (:modes (json-mode json-ts-mode js-json-mode)
     :ts-mode json-ts-mode
     :server nil
     :formatter js-beautify)
    (:modes (markdown-mode gfm-mode)
     :server nil
     :formatter pandoc-markdown)
    (:modes (nix-mode)
     :server nil
     :formatter nixfmt)
    (:modes (python-mode python-ts-mode)
     :ts-mode python-ts-mode
     :server ("pylsp")
     :formatter black)
    (:modes (rust-mode rust-ts-mode)
     :ts-mode rust-ts-mode
     :server ("rust-analyzer")
     :formatter rustfmt)
    (:modes (typescript-mode typescript-ts-mode)
     :ts-mode typescript-ts-mode
     :server nil
     :formatter js-beautify)
    (:modes (sh-mode bash-mode)
     :server nil
     :formatter shfmt)
    (:modes (fish-mode)
     :server nil
     :formatter fish-indent)
    (:modes (yaml-mode)
     :server nil
     :formatter yq-yaml)
    (:modes (zig-mode)
     :server ("zls")
     :formatter zig-fmt))
  "统一的语言能力表。每条条目:
  :modes     生效的所有 major mode 列表(传统 mode 在前,ts-mode 在后)
  :ts-mode   Tree-sitter 对应 mode(nil 表示无 ts 版本)
  :server    LSP server 启动命令列表(nil 表示无 LSP)
  :formatter Apheleia formatter 符号(nil 表示不格式化)")

(defun literal/language--collect (field &optional non-nil)
  "收集所有语言能力表中 FIELD 的值。NON-NIL 时只收集非 nil 条目。"
  (let (result)
    (dolist (entry literal:language-capabilities)
      (let ((value (plist-get entry field)))
        (when (or (not non-nil) value)
          (push value result))))
    (nreverse result)))

;; Tree-sitter remap:从能力表派生
(defconst literal:language-treesit-remaps
  (delq nil
        (mapcar (lambda (entry)
                  (let ((orig-mode (car (plist-get entry :modes)))
                        (ts-mode (plist-get entry :ts-mode)))
                    (when (and orig-mode ts-mode)
                      (cons orig-mode ts-mode))))
                literal:language-capabilities))
  "Tree-sitter major mode remap 列表(从 `literal:language-capabilities' 派生)。")

;; eglot auto-modes:所有声明了 :server 的 mode 列表(打平)
(defconst literal:language-eglot-auto-modes
  (apply #'append
         (mapcar (lambda (entry)
                   (if (plist-get entry :server)
                       (plist-get entry :modes)
                     nil))
                 literal:language-capabilities))
  "进入这些 major mode 时自动触发 `eglot-ensure'。
Phase 3.2:不再用 `eglot--lookup-mode' 私有 API 检测可用性;进入 mode 时
无条件 `eglot-ensure',错误由 Eglot 自身暴露(PLAN §3.2)。")

;; eglot server programs:按 server 命令去重合并 modes
(defconst literal:language-eglot-server-programs
  (let ((seen (make-hash-table :test 'equal))
        (result nil))
    (dolist (entry literal:language-capabilities)
      (let ((server (plist-get entry :server)))
        (when server
          (let ((key (mapconcat #'identity server " ")))
            (if-let* ((pair (gethash key seen)))
                (setcar pair (append (car pair) (plist-get entry :modes)))
              (push (cons (plist-get entry :modes) server) result)
              (puthash key (car result) seen))))))
    (nreverse result))
  "附加到 `eglot-server-programs' 的语言服务器配置(从能力表派生)。")

;; Apheleia formatter 定义 + mode-alist(派生)
(defconst literal:language-apheleia-formatters
  '((pandoc-markdown "pandoc" "-f" "gfm+yaml_metadata_block" "-t" "gfm" "--standalone" "--wrap=none"))
  "Apheleia 自定义 formatter 命令定义(mode-alist 只引用符号)。")

(defconst literal:language-apheleia-mode-alist
  (let (result)
    (dolist (entry literal:language-capabilities)
      (let ((formatter (plist-get entry :formatter)))
        (when formatter
          (dolist (mode (plist-get entry :modes))
            (push (cons mode formatter) result)))))
    (nreverse result))
  "Apheleia major mode 对应关系(从 `literal:language-capabilities' 派生)。")

;; Tree-sitter
;; grammar 加载:Guix home-emacs-service 的 wrapper 通过 TREE_SITTER_GRAMMAR_PATH
;; 环境变量注入 home-emacs-profile/lib/tree-sitter/(18 个 .so)。Emacs 31 自动把
;; 该环境变量加入 treesit-extra-load-path,无需手动设置。下面的 setq 是非 Guix
;; 环境的 fallback(Guix 下 literal:tree-sitter-lib-dir 虽指向 env var 路径,但
;; Emacs 已自动处理,file-directory-p 守卫确保只在路径真实存在时设置)。
(when (file-directory-p literal:tree-sitter-lib-dir)
  (add-to-list 'treesit-extra-load-path literal:tree-sitter-lib-dir))

(dolist (mapping literal:language-treesit-remaps)
  (when (fboundp (cdr mapping))
    (add-to-list 'major-mode-remap-alist mapping)))

(with-eval-after-load 'treesit
  (when (boundp 'treesit-enabled-modes)
    (setq treesit-enabled-modes t))
  (when (boundp 'treesit-auto-install-grammar)
    (setq treesit-auto-install-grammar 'always)))

;; Eglot(LSP 客户端)
(use-package eglot
  :defer t
  :custom
  (eglot-autoshutdown t)
  (eglot-workspace-configuration
   `((:pylsp . (:plugins
                (:black (:enabled t)
                        :pylsp_mypy (:enabled t :live_mode nil)
                        :rope (:enabled t)
                        :pycodestyle (:enabled ,json-false)
                        :mccabe (:enabled ,json-false)
                        :pyflakes (:enabled ,json-false))))))
  :config
  (add-hook 'eglot-managed-mode-hook
            (lambda ()
              (when (fboundp 'eglot-inlay-hints-mode)
                (eglot-inlay-hints-mode 1)))))

;; Phase 3.2:不再调 `eglot--lookup-mode' 私有 API 做可用性预检测。
;; 进入声明了 :server 的 mode 时直接 eglot-ensure;server 缺失的错误由
;; Eglot 自身暴露(PLAN §3.2)。

(defun literal--eglot-setup-once ()
  "首次进入 prog-mode 时触发 eglot 加载,由 after-load 钩子完成实际配置。"
  ;; eglot 是 :defer t:首次进 prog-mode 时 require 它,eglot 加载后由下方
  ;; with-eval-after-load 注册 server 程序表与 auto-mode hooks。
  (remove-hook 'prog-mode-hook #'literal--eglot-setup-once)
  (require 'eglot))

(add-hook 'prog-mode-hook #'literal--eglot-setup-once)

;; eglot 加载后(无论由 prog-mode-hook 触发 require,还是 eglot 命令 autoload):
;; 注册语言服务器程序表 + 为声明了 :server 的语言挂 eglot-ensure。
(with-eval-after-load 'eglot
  (dolist (prog literal:language-eglot-server-programs)
    (add-to-list 'eglot-server-programs prog))
  (dolist (mode literal:language-eglot-auto-modes)
    (add-hook (intern (format "%s-hook" mode)) #'eglot-ensure)))

;; eglot-x — LSP 协议扩展(声明/实现/类型定义跳转)
(with-eval-after-load 'eglot
  (require 'eglot-x)
  (eglot-x-setup))

;; Apheleia(保存时格式化)
(defcustom literal/format-on-save-enabled t
  "是否启用保存时自动格式化。"
  :type 'boolean
  :group 'literal)

(use-package apheleia
  ;; P0 #5 修复(Phase 1.3):原 `:defer t' 没有可靠的首次加载入口,
  ;; apheleia-global-mode 不会启用,保存时格式化在用户首次手动调 Apheleia
  ;; 命令前都失效。改用 prog-mode-hook 触发加载:进任何 prog buffer 就启用
  ;; global mode,保存时格式化立即生效。
  :hook (prog-mode . apheleia-mode)
  :config
  (when literal/format-on-save-enabled
    (apheleia-global-mode 1))
  (dolist (formatter literal:language-apheleia-formatters)
    (setf (alist-get (car formatter) apheleia-formatters)
          (cdr formatter)))
  (dolist (mapping literal:language-apheleia-mode-alist)
    (setf (alist-get (car mapping) apheleia-mode-alist)
          (cdr mapping))))

;; Flymake(代码诊断,Emacs 内置)
;; Eglot 默认通过 flymake 接收 LSP diagnostics,不再禁用 flymake 后端。
;; 切换自原 Flycheck 链:P0 #2 修复(eglot-stay-out-of 不含 flymake)。
;; flymake-mode 不需要 global 化 —— Eglot 在它管理的 buffer 内自动启用
;; flymake-mode-hook;非 LSP buffer 由各 major mode 的 syntax-table 提供诊断。
;; 全局跳转键 M-g n/p 与 C-c l e 在 *前缀键体系* 域统一绑定,不在此处 :bind
;; (避免 flymake 未加载时键位悬空)。
(use-package flymake
  :ensure nil                                  ; 内置包,Guix 不必单列
  :custom
  (flymake-fringe-indicator-position 'left-fringe)
  (flymake-no-changes-timeout 0.5))

(defun literal/flymake-list-errors-dwim ()
  "打开/关闭 Flymake 诊断列表面板。"
  (interactive)
  (let ((buf (get-buffer "*Flymake diagnostics*")))
    (if (and buf (get-buffer-window buf))
        (quit-window nil (get-buffer-window buf))
      (flymake-show-buffer-diagnostics))))

;; envrc (direnv buffer-local 环境变量注入)
(use-package envrc
  :defer 0.5
  :if literal:executable-direnv
  :config
  (envrc-global-mode 1))

;; rainbow-delimiters (彩虹括号)
(use-package rainbow-delimiters
  :hook (prog-mode . rainbow-delimiters-mode))
#+end_src

** 各语言 major mode
:PROPERTIES:
:CUSTOM_ID: language-modes
:END:
GDScript、Fish、JSON / kdl / Kotlin / Markdown / Nix / Rust / TypeScript / Web / Zig、Scheme (Guile) + Arei、Yasnippet。

+ 各 mode 以 =:defer t= + =:mode= 触发,lazy-load
+ Arei(Guile 开发)只在真实 Scheme buffer 启用,跳过 org src 临时 buffer
+ **Arei 兼容性**:arei 0.9.7 的 Guix .elc 与 Emacs 31 =buffer-local-set-state= 宏不兼容,配置从 .el 源加载绕过;=arei-mode= 调用包裹 =condition-case= 防止拖垮 scheme-mode 染色

#+begin_src emacs-lisp
;; 各语言 major mode 声明

;; GDScript
(use-package gdscript-mode
  :mode "\\.gd\\'"
  :defer t
  :custom
  (gdscript-godot-executable (or literal:executable-godot "godot")))

;; Fish Shell
(use-package fish-mode :mode "\\.fish\\'" :defer t)
(use-package fish-completion
  :defer t
  :config
  (when literal:executable-fish
    (global-fish-completion-mode)))
(use-package helm-fish-completion
  :defer t
  :commands (helm-fish-completion helm-fish-completion-make-eshell-source))

;; JSON / kdl / Kotlin / Markdown / Nix / Rust / TypeScript / Web / Zig
(use-package json-mode :mode "\\.json\\'" :defer t)
(use-package kdl-mode :mode "\\.kdl\\'" :defer t)
(use-package kotlin-mode :mode "\\.kt\\'" :defer t)
(use-package markdown-mode
  :mode ("\\.md\\'" "\\.markdown\\'")
  :defer t
  :custom
  (markdown-command "pandoc")
  (markdown-fontify-code-blocks-natively t))
(use-package nix-mode :mode "\\.nix\\'" :defer t)
(use-package yaml :defer t :commands (yaml-parse-string yaml-encode))
(use-package yaml-mode :mode ("\\.ya?ml\\'" "\\.yml\\.dist\\'") :defer t)
(use-package rust-mode :mode "\\.rs\\'" :defer t)
(use-package typescript-mode :mode "\\.ts\\'" :defer t)
(use-package web-mode :mode ("\\.html\\'" "\\.jsx\\'" "\\.tsx\\'" "\\.vue\\'") :defer t)
(use-package zig-mode :mode "\\.zig\\'" :defer t)

;; Scheme (Guile) + Arei
;; arei 0.9.7 的 .elc 与 Emacs 31 不兼容(详见 *系统工具与实验性 → 实验性*)。
;; 兼容性 shim(从源加载 + condition-case 守卫)的定义集中在此处展开:
<<emacs31/arei-compat>>

(use-package arei
  :defer t
  :init
  ;; arei 首次 autoload 时,从 .el 源加载以绕过坏 .elc。
  ;; (:init 在 use-package 展开时立即执行,先于 autoload 触发)
  (literal--load-arei-from-source)
  :hook (scheme-mode . literal--maybe-enable-arei-mode))

;; Yasnippet
(use-package yasnippet :defer 0.5 :config (yas-global-mode 1))
(use-package yasnippet-snippets :after yasnippet :defer t)

;; CSharp
(use-package csharp-mode
  :mode ("\\.cs\\'" "\\.csx\\'" "\\.cake\\'")
  :defer t)

;; PDF Tools(Phase 7.2 接入:原 0 引用,加 use-package + C-c a p 入口)
;; PLAN §7.2:PDF Tools 必须在 C-c a 声明式入口中可达。
;; pdf-tools-install 启动后端(epdfinfo 服务进程),pdf-view-mode 关联 .pdf。
(use-package pdf-tools
  :defer t
  :mode ("\\.pdf\\'" . pdf-view-mode)
  :magic ("%PDF" . pdf-view-mode)
  :config
  (pdf-tools-install t t))     ; no-prompt, no-func
;; C-c a p 入口见 *前缀键体系* 块(打开 PDF 文件)
#+end_src

* 项目与导航
:PROPERTIES:
:CUSTOM_ID: projects
:END:
项目管理(project.el + consult-fd + consult-ripgrep)、文件与缓冲区导航。

** 项目管理 project.el
:PROPERTIES:
:CUSTOM_ID: project-navigation
:END:
配置内置 =project.el= 项目管理:自动识别项目根、文件搜索、grep、ibuffer 按项目分组。

+ Phase 3 迁移自 Projectile:删 projectile / consult-projectile / ibuffer-projectile
+ =project-switch-commands= = Dired / consult-fd / consult-ripgrep
+ =consult-fd= : 项目内快速文件搜索( =literal/find-file-in-project= ),fd 缺失时降级为 find
+ ibuffer 用内置 project filter 分组( =project-current= + =project-root= )
+ 项目前缀 =C-x p= 在 *键位与补全* 域绑定
+ 项目切换清空当前 frame 的 tab-line( =project-after-switch-project-functions= )

#+begin_src emacs-lisp
;; project.el 项目管理(Phase 3 迁移自 Projectile)
;; PLAN §3.1:删除 projectile / consult-projectile / ibuffer-projectile,
;; 改用内置 project.el 的公开 API。consult-fd / consult-ripgrep 接收
;; project-root 作为 default-directory,不再依赖 Projectile 的项目检测。

(defun literal/project-root-or-default ()
  "返回当前 project 的 root,不在项目中时返回 `default-directory'。"
  (or (when-let* ((proj (project-current nil)))
        (project-root proj))
      default-directory))

(defun literal/switch-project ()
  "切换到已知项目,并以 Dired 打开项目根目录。

使用 `project-switch-project'(Emacs 29+ project.el);切换后
`project-after-switch-project-functions' 触发(见 tab-line 域)。"
  (interactive)
  (require 'project)
  (call-interactively #'project-switch-project)
  (when-let* ((proj (project-current nil))
              (root (project-root proj)))
    (dired root)))

(defun literal/open-project-folder ()
  "打开任意文件夹:切换 `default-directory' 并显示 Dired。

含 .git 的目录会通过 `project-remember-project' 注册到已知项目。"
  (interactive)
  (let ((dir (read-directory-name "项目目录: ")))
    (when (file-directory-p dir)
      (setq dir (file-name-as-directory (expand-file-name dir))
            default-directory dir)
      (when (file-directory-p (expand-file-name ".git" dir))
        (project-remember-project (project-current nil)))
      (dired dir))))

(defun literal/open-directory ()
  "打开当前文件所在目录(或当前 default-directory)的 Dired。"
  (interactive)
  (let ((dir (if buffer-file-name
                 (file-name-directory buffer-file-name)
               default-directory)))
    (when dir
      (dired dir))))

;; project.el 配置
;; switch-commands 控制 `project-switch-project' 后默认动作;由 *键位与补全*
;; 域的 C-x p prefix 提供更细粒度入口,这里只设置默认。
(setq project-switch-commands
      (list (cons "Dired" #'project-dired)
            (cons "Find file (consult-fd)" #'literal/find-file-in-project)
            (cons "Ripgrep" #'literal/project-search)))

;; ibuffer 用内置 project filter 分组(替代 ibuffer-projectile)
(with-eval-after-load 'ibuffer
  (add-hook 'ibuffer-hook
            (lambda ()
              (require 'project)
              (let ((ibuffer-filter-groups
                     (cons (cons "project"
                                 (lambda (buf)
                                   (with-current-buffer buf
                                     (when-let* ((proj (project-current nil)))
                                       (project-root proj)))))
                           ibuffer-filter-groups)))
                (unless (eq ibuffer-sorting-mode 'major-mode)
                  (ibuffer-do-sort-by-major-mode))))))

;; consult-fd - 项目内文件搜索

(defun literal/find-file-in-project ()
  "用 consult-fd 查找当前项目内文件。

在 project.el 项目内时限定到项目根目录;否则从当前目录搜索。
依赖外部命令 fd(启动期缓存在 `literal:executable-fd'),缺失时
consult-fd 自动降级为 consult-find(find 命令)。"
  (interactive)
  (require 'consult)
  (let ((default-directory (literal/project-root-or-default)))
    (call-interactively #'consult-fd)))

(defun literal/project-search ()
  "用 consult-ripgrep 在当前项目内搜索。"
  (interactive)
  (require 'consult)
  (let ((default-directory (literal/project-root-or-default)))
    (call-interactively #'consult-ripgrep)))

;; Tab-line 项目切换 hook:Phase 3 迁移自 projectile-after-switch-project-hook。
;; Commit 7 重写 tab-line 时会一并处理;此处先注册等价行为保持不退化。
(with-eval-after-load 'project
  (add-hook 'project-after-switch-project-functions
            (lambda (&rest _) (literal/tabs-clear-for-frame))))
#+end_src

* Org 与知识库
:PROPERTIES:
:CUSTOM_ID: org-knowledge
:END:
Org 基础设置、capture 模板、org-roam、Babel、org-modern UI、Olivetti 写作模式;知识库系统(agenote CLI 集成)由 =org/knowledge= 与 =org/agenote= 两个 noweb 模块内联提供。

** Org 基础 / Capture / Roam / UI
:PROPERTIES:
:CUSTOM_ID: org-core
:END:
Org 目录确保、Org 基础设置、capture 模板、org-modern / org-appear UI、org-roam 笔记网络、Olivetti / Deft。

+ =org-directory= :=~/Documents/Org/=
+ =org-startup-folded 'overview= :打开时仅显示一级标题
+ =org-capture-templates= :经验卡片三种 ENTRY_TYPE(note / mistake / ascended)
+ =org-roam= :笔记网络( =roam/= 子目录)
+ =org-modern= + =org-appear= :UI 现代化
+ 知识库 CRUD 由 =org/knowledge= 模块提供(本文档内联,见后文)

#+begin_src emacs-lisp
;; Org 目录确保
(defun literal--ensure-org-directories ()
  "确保所有 Org 相关目录存在。"
  (dolist (dir (list literal:org-directory
                     (expand-file-name "agenda" literal:org-directory)
                     (expand-file-name "notes" literal:org-directory)
                     literal:org-roam-directory
                     (expand-file-name "babel" literal:org-directory)
                     (expand-file-name "tangle" literal:org-directory)))
    (unless (file-exists-p dir)
      (make-directory dir t))))
(run-with-idle-timer 2 nil #'literal--ensure-org-directories)

;; Org Mode 基础
(use-package org
  :defer t
  :custom
  (org-directory literal:org-directory)
  (org-agenda-files (list (expand-file-name "agenda" literal:org-directory)))
  (org-default-notes-file literal:org-default-notes-file)
  (org-hide-emphasis-markers t)
  (org-startup-indented t)
  (org-pretty-entities t)
  (org-use-sub-superscripts '{})
  (org-cycle-separator-lines 2)
  (org-startup-folded 'overview)
  (org-blank-before-new-entry '((heading . t) (plain-list-item . auto)))
  (org-src-fontify-natively t)
  (org-src-tab-acts-natively t)
  (org-src-preserve-indentation t)
  (org-src-window-setup 'current-window)
  (org-edit-src-content-indentation 0)
  (org-image-actual-width '(300))
  :config
  ;; daemon/client 下按 frame 判断图片显示能力
  (defun literal--org-toggle-inline-images ()
    (setq-local org-startup-with-inline-images (display-graphic-p)))
  (add-hook 'org-mode-hook #'literal--org-toggle-inline-images)

  ;; C-j 在 Org 内分发:表格内走原命令,表格外走 next-line
  (defun literal/org-c-j-dispatch ()
    "在 Org buffer 中分发 C-j:表格内走 `org-return-and-maybe-indent',
其余位置走 `next-line'。"
    (interactive)
    (call-interactively
     (if (org-at-table-p)
         #'org-return-and-maybe-indent
       #'next-line)))
  (define-key org-mode-map (kbd "C-j") #'literal/org-c-j-dispatch)
  (define-key org-mode-map (kbd "TAB") #'org-cycle)
  (define-key org-mode-map (kbd "<backtab>") #'org-shifttab))

;; org-capture 模板:统一入口(生活系统 + 经验卡片)
;; org 改为 :defer t 后,capture 模板从 org 的 :config 移出到独立的 after-load:
;; 这些辅助函数只依赖 bootstrap 常量(无 org 依赖),但 org-capture-templates
;; 需 org-capture 加载后设置才有意义。模板设置延迟到首次 capture 时。
;;
;; Phase 2.2 统一 Capture 入口:四类数据生命周期对齐 PLAN §2.1。
;;   inbox.org  → ki 一句话草稿、临时想法、待分流事项
;;   agenda/    → kt 普通任务、kd 带日期任务、ke 日程事件
;;   roam/      → kr 长期笔记与双向关联知识
;;   experiences/ → kn/km/ka 经验卡片(对齐 agenote-base entry-types)
(defvar literal/org-capture--last-category nil
  "本次 capture 的经验卡 category。")
(defun literal/org-capture--category ()
  "返回本次 capture 选定的 category。"
  (or literal/org-capture--last-category "general"))
(defun literal/org-capture--experience-file ()
  "生成经验卡片目标文件路径(人类域,从 `agenote fields --category' 拉分类)。"
  (let* ((choices (or (condition-case nil
                          (split-string
                           (literal/agenote-call-string 'human "fields" "--category")
                           "\n" t "[ \t]+")
                        (error nil))
                      '("emacs" "guix" "general" "gamedev" "devops"
                        "research" "tooling")))
         (cat (completing-read "经验卡类别: " choices nil t))
         (dir (expand-file-name (concat "experiences/" cat)
                                literal:org-directory)))
    (setq literal/org-capture--last-category cat)
    (unless (file-directory-p dir)
      (make-directory dir t))
    (expand-file-name (format "%s.org" (format-time-string "%Y%m%d-%H%M%S"))
                      dir)))
(defun literal/org-capture--agenda-file ()
  "返回本次 capture 的目标 agenda 文件。
当前 `org-agenda-files' 只有一项;未来扩展为多文件时改为 completing-read。"
  (car (org-agenda-files)))
(with-eval-after-load 'org-capture
  (setq org-capture-templates
        `(("ki" "Inbox 草稿" entry
           (file ,literal:org-inbox-file)
           "* TODO %?\n:PROPERTIES:\n:CREATED: %U\n:END:\n%i\n")
          ("kt" "任务 (agenda)" entry
           (file literal/org-capture--agenda-file)
           "* TODO %?\n:PROPERTIES:\n:CREATED: %U\n:END:\n%i\n")
          ("kd" "带日期任务 (agenda)" entry
           (file literal/org-capture--agenda-file)
           "* TODO %?\nSCHEDULED: %^t\n:PROPERTIES:\n:CREATED: %U\n:END:\n%i\n")
          ("ke" "日程事件 (agenda)" entry
           (file literal/org-capture--agenda-file)
           "* %?\n:PROPERTIES:\n:CREATED: %U\n:END:\n%i\n")
          ("kr" "Roam 长期笔记" plain
           (file literal/org-capture--experience-file)
           "%?")
          ("kn" "经验-笔记 (note)" plain
           (file literal/org-capture--experience-file)
           "* %^{标题}\n:PROPERTIES:\n:ID:       %(format-time-string \"%Y%m%d-%H%M%S\")\n:CREATED:  %(format-time-string \"[%Y-%m-%d %a]\")\n:CATEGORY: %(literal/org-capture--category)\n:TECH:     %^{技术栈}\n:TYPE:     workflow\n:STATUS:   done\n:ENTRY_TYPE: note\n:END:\n#+filetags: :%^{标签}:\n\n** 事项内容\n%?\n** 为什么值得长期保留\n\n** 适用场景与例外\n\n** 后续行动\n")
          ("km" "经验-踩坑 (mistake)" plain
           (file literal/org-capture--experience-file)
           "* %^{标题}\n:PROPERTIES:\n:ID:       %(format-time-string \"%Y%m%d-%H%M%S\")\n:CREATED:  %(format-time-string \"[%Y-%m-%d %a]\")\n:CATEGORY: %(literal/org-capture--category)\n:TECH:     %^{技术栈}\n:TYPE:     debug\n:STATUS:   done\n:ENTRY_TYPE: mistake\n:END:\n#+filetags: :%^{标签}:\n\n** 原始问题\n%?\n** 纠错反馈\n\n** 这次到底错在哪里\n\n** 最终正确处理\n")
          ("ka" "经验-试错后最优 (ascended)" plain
           (file literal/org-capture--experience-file)
           "* %^{标题}\n:PROPERTIES:\n:ID:       %(format-time-string \"%Y%m%d-%H%M%S\")\n:CREATED:  %(format-time-string \"[%Y-%m-%d %a]\")\n:CATEGORY: %(literal/org-capture--category)\n:TECH:     %^{技术栈}\n:TYPE:     debug\n:STATUS:   done\n:ENTRY_TYPE: ascended\n:END:\n#+filetags: :%^{标签}:\n\n** 前几轮失败的根因\n%?\n** 检索过的知识源\n\n** 核对过的真实文件或输出\n\n** 最终采用的最强方案\n"))))

;; UI 增强
(use-package org-modern
  :hook (org-mode . org-modern-mode)
  :custom
  (org-modern-star 'replace)
  (org-modern-hide-stars t)
  (org-modern-table nil)
  (org-modern-keyword t)
  (org-modern-todo t)
  (org-modern-tag t)
  (org-modern-block-name t)
  ;; 无条件用 4:原 (if (display-graphic-p) 4 nil) 在 daemon 启动期判断永远为 nil
  ;; → GUI client 的 block fringe 被关掉。TTY 下 fringe 参数被忽略,设 4 无副作用。
  (org-modern-block-fringe 4))

(use-package org-appear
  :hook (org-mode . org-appear-mode)
  :custom
  (org-appear-autoemphasis t)
  (org-appear-autolinks t)
  (org-appear-autosubmarkers t))

;; Org-roam 笔记网络
(use-package org-roam
  :defer t
  :commands (org-roam-node-find org-roam-node-insert org-roam-buffer-toggle)
  :custom
  (org-roam-directory literal:org-roam-directory)
  (org-roam-database-connector 'sqlite)
  (org-roam-completion-everywhere nil)
  :config
  ;; autosync 在首次 roam 命令(:commands 触发加载)后启动。
  (org-roam-db-autosync-mode))

;; Olivetti(专注写作模式)
(use-package olivetti
  :defer t
  :commands (olivetti-mode)
  :custom
  (olivetti-body-width 100))

;; Deft(笔记搜索)
;; Phase 2.1:notes/ 标记为历史迁移区,不再创建新文件。Deft 仅作为历史
;; notes/ 内容的只读检索入口,保留到 Phase 7 manifest 决定是否移除包。
;; 新笔记统一走 capture 模板(ki inbox / kr roam),不要继续用 Deft 创建。
(use-package deft
  :defer t
  :commands (deft)
  :custom
  (deft-directory (expand-file-name "notes" literal:org-directory))
  :config
  ;; 历史迁移区:不递归到其他 Org 子目录
  (setq deft-recursive nil))

;; DEPRECATED(Phase 2.1):literal/note-new 把新笔记写到 notes/ 历史目录,
;; 违反"notes/ 标记为历史迁移区,不再创建新文件"(PLAN §2.1)。
;; 替代:统一走 capture 模板(ki 写 inbox / kr 写 roam)。
;; 保留函数定义 + 绑定到 Phase 7 manifest 决定去留前,避免破坏既有习惯。
(defun literal/note-new ()
  "创建新笔记到 notes/ 目录。(DEPRECATED,使用 capture ki/kr 替代)"
  (interactive)
  (message "DEPRECATED: literal/note-new 写入历史 notes/ 目录;请改用 \
`C-c c' → ki (inbox) 或 kr (roam)。详见 PLAN §2.1。")
  (let ((dir (expand-file-name "notes" literal:org-directory)))
    (unless (file-directory-p dir)
      (make-directory dir t)))
  (find-file (expand-file-name
              (format-time-string "notes/%Y%m%d-%H%M%S.org")
              literal:org-directory)))
#+end_src

** Knowledge 系统
:PROPERTIES:
:CUSTOM_ID: knowledge
:END:
agenote CLI 集成的知识库 CRUD。

+ =literal/knowledge-*= :知识库经验卡片 CRUD 与双视图
+ =literal/agenote-*= :agenote 子域浏览与健康度面板
+ Knowledge 与 agenote 直接复用启动域路径常量和 [[#process-helper][共享进程辅助]],不维护重复注入点或私有进程封装

*** 路径与文件辅助
#+begin_src emacs-lisp :noweb-ref org/knowledge-core :tangle no


;; 辅助：递归扫描 experiences 目录

(defun literal/knowledge--collect-org-files (dir)
  "递归收集 DIR 下所有 .org 文件（含子目录）。"
  (when (file-directory-p dir)
    (let (result)
      (dolist (entry (directory-files-and-attributes dir t nil t))
        (let ((name (car entry))
              (type (file-attribute-type (cdr entry))))
          (cond
           ((string-prefix-p "." (file-name-nondirectory name)))
           ((eq type t)
            (setq result (nconc result (literal/knowledge--collect-org-files name))))
           ((string-match-p "\\.org\\'" name)
            (push name result)))))
      (nreverse result))))

;; 公开别名:双横线名为模块私有约定,跨模块调用(dashboard 收集知识库文件)
;; 应走公开 API。defalias 让 dashboard 调 literal/knowledge-collect-org-files。
(defalias 'literal/knowledge-collect-org-files #'literal/knowledge--collect-org-files)

;; 目录确保
;; Phase 1.3/2.2:不再用 run-with-idle-timer 3 秒预建。改为在真正需要
;; 目录的命令(capture / inbox 打开 / before-save)前幂等 ensure,消除
;; idle timer 与用户操作的竞态(PLAN §1.3)。

(defun custom--ensure-knowledge-directories ()
  "确保知识库相关目录和索引文件存在(幂等)。
experiences/ 下的类别子目录在 Capture 时按需创建，不在此处预建。
inbox.org 不存在时用最小骨架初始化,已存在则保持原样。"
  (let ((dirs (list (expand-file-name "experiences" literal:org-directory))))
    (dolist (dir dirs)
      (unless (file-exists-p dir)
        (make-directory dir t))))
  (dolist (file (list literal:org-inbox-file))
    (unless (file-exists-p file)
      (let ((base (file-name-base file)))
        (with-temp-buffer
          (insert (format "#+title: %s\n#+date: [%s]\n\n* Inbox\n"
                          base (format-time-string "%Y-%m-%d %a")))
          (write-region (point-min) (point-max) file))))))

;; 幂等 ensure:挂在 org-capture 和 org-mode before-save 上,确保目录在
;; 真正需要时存在,而不是固定 3 秒后(可能与用户操作竞态)。
(with-eval-after-load 'org-capture
  (add-hook 'org-capture-before-finalize-hook
            #'custom--ensure-knowledge-directories))
(with-eval-after-load 'org
  (add-hook 'org-mode-hook
            (lambda ()
              (when (and buffer-file-name
                         (string-prefix-p literal:org-directory buffer-file-name))
                (custom--ensure-knowledge-directories)))))

#+end_src

*** Capture 与卡片命名
#+begin_src emacs-lisp :noweb-ref org/knowledge-capture :tangle no
;; 新建经验卡片

(defun literal/knowledge-capture ()
  "捕获一张经验卡片，走 org-capture 模板。
`org-capture nil \"k\"' 进入 k 前缀子菜单（note/mistake/ascended，
对齐 agenote-base entry-types 语义），由 `org-capture-templates' 的
kn/km/ka 三条模板负责生成结构化 PROPERTIES + body 章节，落到
experiences/<category>/<timestamp>.org。模板定义见 org-core 域。

P0 #5 修复(Phase 1.3):不再预检查 `org-capture-templates'。`org-capture' 是
autoload,首次调用会自动加载 org-capture 并装配模板;预检查反而会在 fresh
daemon(org 未加载)时拒绝运行,要求用户先手动调一次。"
  (interactive)
  (org-capture nil "k"))

;; Phase 2.2: Emacs 侧重命名 / slug / 时间戳业务逻辑已删除。
;; 这些职责应由 agenote CLI 的 finalize-hook 在 capture 收尾时统一执行:
;;   - 提取标题 → slug 化文件名
;;   - 从 :ID: / 文件名规范化时间戳
;;   - reindex 索引同步
;; TODO(bug): agenote CLI 目前没有 finalize-hook 命令;补 CLI 后此处不再需要
;; 任何 Emacs 侧包装。历史实现见 git history (Commit 4)。

#+end_src

*** 检索与 Inbox
#+begin_src emacs-lisp :noweb-ref org/knowledge-search :tangle no
;; 检索函数

(defun literal/knowledge-search (query)
  "在知识库中全文检索经验。
使用 consult-ripgrep 递归搜索 experiences/。"
  (interactive "s检索关键词: ")
  (let ((dir (expand-file-name "experiences" literal:org-directory)))
    (cond
     ((fboundp 'consult-ripgrep)
      (consult-ripgrep (list dir) query))
     ((fboundp 'rgrep)
      (rgrep query "*.org" (list dir)))
     (t
      (message "未找到可用的搜索工具")))))

(defun literal/knowledge-search-by-tag (tag)
  "在知识库中按标签检索经验。
优先使用 agenote tags 命令，回退到 ripgrep 搜索 :TAG: 模式。"
  (interactive "s标签: ")
  (if (literal/agenote--resolve-executable)
      (let ((buf (get-buffer-create "*agenote-tags*")))
        (with-current-buffer buf
          (erase-buffer)
          (insert (literal/agenote-call-string 'human "tags" tag)))
        (display-buffer buf))
    (literal/knowledge-search (format ":%s:" tag))))

;; Inbox 归档

(defun literal/knowledge-archive-inbox-entry (category)
  "将 inbox.org 中光标所在的条目归档到 experiences/<CATEGORY>/ 目录。
提取标题作为文件名，移动整个 subtree 到新文件，并从 inbox.org 中删除原条目。
归档完成后调用 `agenote reindex` 重建索引。"
  (interactive
   (list (completing-read "归档分类: "
                          (or (condition-case nil
                                  (split-string
                                   (literal/agenote-call-string 'human "fields" "--category")
                                   "\n" t "[ \t]+")
                                (error nil))
                              '("emacs" "guix" "general" "gamedev" "devops" "research" "tooling"))
                          nil t)))
  (let* ((inbox-file literal:org-inbox-file)
         (exp-dir (expand-file-name (concat "experiences/" category) literal:org-directory))
         (heading (org-get-heading t t t t))
         (title (if (or (not heading) (string-empty-p heading))
                    (format-time-string "entry-%Y%m%d-%H%M%S")
                  (let* ((s (replace-regexp-in-string "\\[\\[.*?\\]\\[\\(.*?\\)\\]\\]" "\\1" heading))
                         (s (replace-regexp-in-string "[^[:alnum:]_-]+" "-" s))
                         (s (replace-regexp-in-string "-+" "-" s))
                         (s (replace-regexp-in-string "^-\\|-\\'" "" s)))
                    (format "%s-%s" (format-time-string "%Y%m%d-%H%M%S") s))))
         (target-file (expand-file-name (concat title ".org") exp-dir)))
    (unless (file-directory-p exp-dir)
      (make-directory exp-dir t))
    (org-copy-subtree)
    (with-temp-buffer
      (insert (car kill-ring))
      (write-region (point-min) (point-max) target-file))
    (org-cut-subtree)
    (message "已归档到 %s" (file-relative-name target-file literal:org-directory))
    ;; 同步重建索引(人类域)
    (when (literal/agenote--resolve-executable)
      (message "正在重建知识库索引...")
      (literal/agenote-call-async 'human "reindex")
      (message "索引已同步"))))

#+end_src

*** 生命周期与 CLI
#+begin_src emacs-lisp :noweb-ref org/knowledge-cli :tangle no
;; agenote CLI 集成

(defun literal/knowledge-stats ()
  "显示人类知识库统计信息（调 `agenote --domain human stats`）。"
  (interactive)
  (if (literal/agenote--resolve-executable)
      (let ((buf (get-buffer-create "*agenote-stats*")))
        (with-current-buffer buf
          (erase-buffer)
          (insert (literal/agenote-call-string 'human "stats"))
          (goto-char (point-min)))
        (display-buffer buf))
    (message "未找到 agenote 命令")))

(defun literal/knowledge-open-file (file)
  "打开知识库 FILE。"
  (interactive "f知识库文件: ")
  (let ((target-file (expand-file-name file)))
    (find-file target-file)))

(defun literal/knowledge-open-inbox ()
  "打开知识库收件箱。"
  (interactive)
  (literal/knowledge-open-file literal:org-inbox-file))

;; 卡片生命周期（agenote touch/archive/restore/review）
;; 这些命令作用于人类域（experiences/）卡片，ID 从当前 buffer 的 :ID: 属性取。
;; 在非经验卡片 buffer 调用会提示并 abort。

(defun literal/knowledge--current-id ()
  "从当前 buffer 的 PROPERTIES 取 :ID: 值。
返回 ID 字符串或 nil（当前 buffer 非经验卡片 / 无 :ID:）。"
  (when (and (eq major-mode 'org-mode)
             (buffer-file-name))
    (org-with-wide-buffer
     (goto-char (point-min))
     (when (re-search-forward "^:ID:\\s-+\\([0-9]\\{8\\}-[0-9]\\{6\\}\\)" nil t)
       (match-string 1)))))

(defun literal/knowledge--ensure-experience-buffer ()
  "确保当前 buffer 是人类域经验卡片，返回其 :ID:。
否则 message 报错并返回 nil。人类域 = buffer 文件位于
`literal:org-knowledge-directory' 之下（不含 agenote 子域）。"
  (let ((id (literal/knowledge--current-id))
        (file (buffer-file-name))
        (exp-dir (expand-file-name literal:org-knowledge-directory))
        (agenote-dir (expand-file-name "agenote" literal:org-directory)))
    (cond
     ((null file)
      (message "当前 buffer 无关联文件") nil)
     ((string-prefix-p agenote-dir (expand-file-name file))
      (message "当前卡片在 agenote 子域；人类域操作请打开 experiences/ 下的卡片") nil)
     ((not (string-prefix-p exp-dir (expand-file-name file)))
      (message "当前文件不在 experiences/ 下") nil)
     ((null id)
      (message "当前卡片无 :ID: 属性") nil)
     (t id))))

(defun literal/knowledge-touch (&optional used-only)
  "更新当前经验卡片的 LAST_USED + LAST_VERIFIED（人类域,调 `agenote touch <ID>'）。
带前缀参数 USED-ONLY 时只更新 LAST_USED。"
  (interactive "P")
  (let ((id (literal/knowledge--ensure-experience-buffer)))
    (when id
      (if (literal/agenote--resolve-executable)
          (let ((result (apply #'literal/agenote-call
                               'human "touch" id
                               (when used-only '("--used-only")))))
            (if (zerop (plist-get result :status))
                (message "已 touch: %s%s" id (if used-only " (仅 USED)" ""))
              (message "touch 失败: %s" (plist-get result :stderr))))
        (message "未找到 agenote 命令")))))

(defun literal/knowledge-archive (reason)
  "归档当前经验卡片（人类域,调 `agenote archive <ID> --reason REASON'）。
卡片被移到 archived/，可通过 `literal/knowledge-restore' 恢复。"
  (interactive "s归档原因: ")
  (let ((id (literal/knowledge--ensure-experience-buffer)))
    (when id
      (if (literal/agenote--resolve-executable)
          (let* ((result (literal/agenote-call 'human "archive" id "--reason" reason)))
            (if (zerop (plist-get result :status))
                (message "已归档: %s (%s)" id reason)
              (message "归档失败: %s" (plist-get result :stderr))))
        (message "未找到 agenote 命令")))))

(defun literal/knowledge-restore (id)
  "恢复已归档的经验卡片（人类域,调 `agenote restore <ID>'）。
ID 通过 completing-read 从归档列表选取（若可获取），否则手动输入。"
  (interactive
   (list (read-string "恢复的卡片 ID: "
                      (when-let* ((cur (literal/knowledge--current-id)))
                        cur))))
  (if (literal/agenote--resolve-executable)
      (let ((result (literal/agenote-call 'human "restore" id)))
        (if (zerop (plist-get result :status))
            (message "已恢复: %s" id)
          (message "恢复失败: %s" (plist-get result :stderr))))
    (message "未找到 agenote 命令")))

(defun literal/knowledge-review ()
  "审查当前经验卡片（人类域,调 `agenote review <ID>'），输出显示在临时 buffer。"
  (interactive)
  (let ((id (literal/knowledge--ensure-experience-buffer)))
    (when id
      (if (literal/agenote--resolve-executable)
          (let ((buf (get-buffer-create "*agenote-review*"))
                (result (literal/agenote-call 'human "review" id)))
            (with-current-buffer buf
              (erase-buffer)
              (insert (plist-get result :stdout))
              (goto-char (point-min)))
            (display-buffer buf))
        (message "未找到 agenote 命令")))))

;; 其余 CLI 能力（memory / connect / merge / deduplicate / lint / commit）
;; 这些命令大多带子选项或作用于多 ID，封装时让 minibuffer 收集参数后一次性
;; 同步调用，结果展示在对应 buffer 或 message。`agenote profile' 不存在，
;; 已从设计删除（plan §3.6 实测核对）。

(defun literal/knowledge-memory (arg)
  "人类域记忆系统概览或操作。
无前缀：调 `agenote --domain human memory' 显示概览到 buffer。
前缀 ARG 非 nil：提示操作（--stale / --type X）。"
  (interactive "P")
  (if (literal/agenote--resolve-executable)
      (let* ((extra-args (if arg
                             (let ((op (completing-read
                                        "memory 操作: "
                                        '("--stale" "--type feedback"
                                          "--type project" "--type reference")
                                        nil t)))
                               (split-string-and-unquote op))
                           nil))
             (result (apply #'literal/agenote-call 'human "memory" extra-args))
             (buf (get-buffer-create "*agenote-memory*")))
        (with-current-buffer buf
          (erase-buffer)
          (insert (plist-get result :stdout))
          (goto-char (point-min)))
        (display-buffer buf))
    (message "未找到 agenote 命令")))

(defun literal/knowledge-connect (id-a id-b &optional desc)
  "双向链接两张卡片（人类域,调 `agenote connect ID_A ID_B --desc DESC'）。
ID-A 默认取当前 buffer 的 :ID:，ID-B 与 DESC 在 minibuffer 收集。"
  (interactive
   (let* ((a (or (literal/knowledge--current-id)
                 (read-string "主卡片 ID: ")))
          (b (read-string "链接到卡片 ID: "))
          (d (read-string "描述 (可选): ")))
     (list a b d)))
  (if (literal/agenote--resolve-executable)
      (let* ((extra-args (delq nil (list id-a id-b
                                          (unless (string-empty-p desc) "--desc")
                                          (unless (string-empty-p desc) desc))))
             (result (apply #'literal/agenote-call 'human "connect" extra-args)))
        (if (zerop (plist-get result :status))
            (message "已链接: %s ↔ %s" id-a id-b)
          (message "链接失败: %s" (plist-get result :stderr))))
    (message "未找到 agenote 命令")))

(defun literal/knowledge-merge (primary-id secondary-ids reason)
  "合并卡片：把 SECONDARY-IDS 并入 PRIMARY-ID（人类域,调 `agenote merge'）。
SECONDARY-IDS 为空格分隔的多个 ID，REASON 记录合并原因。"
  (interactive
   (let* ((p (or (literal/knowledge--current-id)
                 (read-string "主卡片 ID（保留）: ")))
          (s (read-string "次卡片 ID（合并后删除，多个用空格）: "))
          (r (read-string "合并原因: ")))
     (list p (split-string s) r)))
  (if (literal/agenote--resolve-executable)
      (let* ((extra-args (append (list primary-id) secondary-ids
                                 (unless (string-empty-p reason)
                                   (list "--reason" reason))))
             (result (apply #'literal/agenote-call 'human "merge" extra-args)))
        (if (zerop (plist-get result :status))
            (message "已合并 %d 张到 %s" (length secondary-ids) primary-id)
          (message "合并失败: %s" (plist-get result :stderr))))
    (message "未找到 agenote 命令")))

(defun literal/knowledge-deduplicate ()
  "检测重复卡片（人类域,异步调 `agenote deduplicate'），结果显示在 buffer。"
  (interactive)
  (if (literal/agenote--resolve-executable)
      (literal/agenote-call-async 'human "deduplicate")
    (message "未找到 agenote 命令")))

(defun literal/knowledge-lint (arg)
  "格式校验所有经验卡片（人类域,异步调 `agenote lint'）。
无前缀：仅检查并显示报告。
前缀 ARG 非 nil：调 `agenote lint --fix' 自动修复。"
  (interactive "P")
  (if (literal/agenote--resolve-executable)
      (let ((args (if arg '("--fix") '("--check"))))
        (apply #'literal/agenote-call-async 'human "lint" args)
        (when arg (message "正在自动修复格式问题...")))
    (message "未找到 agenote 命令")))

(defun literal/knowledge-commit (summary)
  "提交知识库变更到 git（人类域,异步调 `agenote commit -m SUMMARY'）。
index.json 由 .gitignore 排除，仅提交卡片与 MEMORY 变更。"
  (interactive "s提交总结: ")
  (if (literal/agenote--resolve-executable)
      (progn
        (literal/agenote-call-async 'human "commit" "-m" summary)
        (message "正在提交: %s" summary))
    (message "未找到 agenote 命令")))

;; Agenda 集成

(defun literal/knowledge--setup-agenda ()
  "将经验目录加入 agenda 自定义命令。"
  (add-to-list
   'org-agenda-custom-commands
   '("K" "知识库回顾"
     ((tags "difficulties|lessons"
            ((org-agenda-files (list (expand-file-name "experiences" literal:org-directory)))
             (org-agenda-sorting-strategy '(timestamp-down))))))
   t))

;; 初始化

(with-eval-after-load 'org-agenda
  (literal/knowledge--setup-agenda))



#+end_src

*** 知识库浏览(统一 mode)
#+begin_src emacs-lisp :noweb-ref org/knowledge-browse :tangle no
;; 知识库浏览(Phase 2.3 合并):统一的 special-mode 视图,消费
;; `agenote list --json' 的稳定索引;不再自行解析 index.json / 计算陈旧度桶。
;; 两个 Tab:类别树(按 category 字典序)+ 时间线(按 created 降序)。
;; 域切换:C-u 前缀(默认 human;agenote 子域用 `literal/knowledge-browse-agenote')。
;; 命名约定:literal/knowledge-browse--*(函数)、literal--knowledge-browse-*(私有变量)。

;; 常量与数据层

(defconst literal:knowledge-browse--buffer-name "*knowledge-browse*"
  "知识库浏览 buffer 名称。human / agenote 子域共用,通过 buffer-local
`literal--knowledge-browse-domain' 区分。")

(defun literal/knowledge-browse--load-cards (domain)
  "返回 DOMAIN 域的卡片 plist 列表。
优先调 `agenote --domain DOMAIN list --json' 获取稳定 JSON 输出;
CLI 不可用或不支持 --json 时降级读 index.json,并在 *Messages* 标 TODO。"
  (let ((json-object-type 'plist)
        (json-array-type 'list)
        (json-key-type 'keyword))
    (condition-case err
        (let ((raw (literal/agenote-call-string domain "list" "--json")))
          (if (and raw (string-match-p "\\`[[:space:]]*[\[{]" raw))
              (with-temp-buffer
                (insert raw)
                (goto-char (point-min))
                (let ((parsed (json-read)))
                  ;; 兼容两种返回:{:cards [...]} 或直接 [...]
                  (if (and (listp parsed) (plist-member parsed :cards))
                      (plist-get parsed :cards)
                    parsed)))
            ;; 非降级路径:JSON 不合法时尝试 index.json
            (literal/knowledge-browse--fallback-index domain)))
      (error
       (message "literal/knowledge-browse: --json 不可用 (%s),降级读 index.json"
                (error-message-string err))
       (literal/knowledge-browse--fallback-index domain)))))

(defun literal/knowledge-browse--fallback-index (domain)
  "降级:直接读 DOMAIN 对应的 index.json。PLAN §2.3 TODO:CLI 补 --json 后移除。"
  (let ((file (pcase domain
                ('human (expand-file-name "index.json" literal:org-directory))
                ('agenote literal:agenote-index-file)
                (_ (expand-file-name "index.json" literal:org-directory)))))
    (when (file-readable-p file)
      (let ((json-object-type 'plist)
            (json-array-type 'list)
            (json-key-type 'keyword))
        (with-temp-buffer
          (insert-file-contents file)
          (goto-char (point-min))
          (condition-case nil
              (plist-get (json-read) :cards)
            (error nil)))))))

;; 渲染辅助

(defun literal/knowledge-browse--status-face (status)
  "STATUS 字符串 → 对应 face 符号。未知状态返回 nil。"
  (pcase status
    ("done" 'success)
    ("stable" 'font-lock-type-face)
    ("inprogress" 'warning)
    ("cancelled" 'error)
    (_ nil)))

(defun literal/knowledge-browse--entry-type-face (entry-type)
  "ENTRY_TYPE 字符串 → face 符号。"
  (pcase entry-type
    ("note" 'font-lock-doc-face)
    ("mistake" 'error)
    ("ascended" 'success)
    (_ 'shadow)))

(defun literal/knowledge-browse--format-card-line (card)
  "格式化单张卡片为 propertized 字符串(类别 Tab 与时间线 Tab 共用)。"
  (let* ((title (or (plist-get card :title) "(无标题)"))
         (category (or (plist-get card :category) "unknown"))
         (entry-type (plist-get card :entry_type))
         (status (or (plist-get card :status) ""))
         (source (or (plist-get card :source_agent) ""))
         (created (or (plist-get card :created) ""))
         (id (or (plist-get card :id) ""))
         (file (or (plist-get card :file_path)
                   (plist-get card :path)))
         (keymap (make-sparse-keymap))
         (line))
    (when file
      (define-key keymap (kbd "RET")
                  (lambda () (interactive) (find-file file)))
      (define-key keymap [mouse-1]
                  (lambda () (interactive) (find-file file))))
    (setq line
          (concat
           (propertize (format "%-12s" category)
                       'face 'font-lock-keyword-face)
           "  "
           (propertize (format "%-10s" created)
                       'face 'font-lock-comment-face)
           "  "
           (when entry-type
             (concat (propertize (format "%-9s" entry-type)
                                 'face (literal/knowledge-browse--entry-type-face
                                        entry-type))
                     "  "))
           (when (and status (not (string-empty-p status)))
             (concat (propertize (format "%-11s" status)
                                 'face (literal/knowledge-browse--status-face status))
                     "  "))
           (unless (string-empty-p source)
             (concat (propertize (format "@%-8s" source)
                                 'face 'font-lock-builtin-face)
                     "  "))
           (propertize title 'face 'default)))
    (when file
      (add-text-properties
       0 (length line)
       (list 'keymap keymap
             'mouse-face 'highlight
             'help-echo (format "打开 %s" file)
             'follow-link t)
       line))
    line))

;; buffer-local 状态

(defvar-local literal--knowledge-browse-cards nil
  "当前 buffer 已加载的卡片 plist 列表。")
(defvar-local literal--knowledge-browse-domain 'human
  "当前 buffer 浏览的域:human 或 agenote。")
(defvar-local literal--knowledge-browse-current-tab 1
  "当前 Tab:1=类别树,2=时间线。")

;; 渲染

(defun literal/knowledge-browse--render-tab-category (cards)
  "渲染类别树 Tab。类别字典序,组内 created 降序。"
  (dolist (group (literal/agenote--group-by-category cards))
    (let ((cat (car group))
          (entries (cdr group)))
      (insert (propertize (format "▼ %s (%d)" cat (length entries))
                          'face 'font-lock-keyword-face)
              "\n")
      (dolist (card entries)
        (insert "  " (literal/knowledge-browse--format-card-line card) "\n"))
      (insert "\n"))))

(defun literal/knowledge-browse--render-tab-timeline (cards)
  "渲染时间线 Tab。所有卡片按 created 降序排列。"
  (dolist (card (literal/agenote--sort-by-recency cards))
    (insert (literal/knowledge-browse--format-card-line card) "\n")))

(defun literal/knowledge-browse--header-line ()
  "返回 header-line 字符串,显示域 + 当前 Tab + 卡片总数。"
  (let* ((tab-name (pcase literal--knowledge-browse-current-tab
                     (1 "类别")
                     (2 "时间线")))
         (domain-name (pcase literal--knowledge-browse-domain
                        ('human "人类域")
                        ('agenote "agenote 子域")))
         (count (length literal--knowledge-browse-cards)))
    (format " [%s] %s · %d 张 · [1]类别 [2]时间线 [g]刷新 [o]浏览器"
            domain-name tab-name count)))

(defun literal/knowledge-browse--render-current-tab ()
  "重绘当前 Tab。调用前需已加载 cards。"
  (let ((inhibit-read-only t))
    (erase-buffer)
    (goto-char (point-min))
    (pcase literal--knowledge-browse-current-tab
      (1 (literal/knowledge-browse--render-tab-category
          literal--knowledge-browse-cards))
      (2 (literal/knowledge-browse--render-tab-timeline
          literal--knowledge-browse-cards)))
    (goto-char (point-min))
    (setq header-line-format (literal/knowledge-browse--header-line))))

(defun literal/knowledge-browse--refresh ()
  "重新加载卡片并重绘当前 Tab。"
  (setq literal--knowledge-browse-cards
        (literal/knowledge-browse--load-cards literal--knowledge-browse-domain))
  (literal/knowledge-browse--render-current-tab))

(defun literal/knowledge-browse--switch-tab (n)
  "切换到 Tab N(1 或 2)。"
  (setq literal--knowledge-browse-current-tab n)
  (literal/knowledge-browse--render-current-tab))

(defun literal/knowledge-browse--force-refresh (&rest _)
  "强制刷新(`revert-buffer-function' 与 `g' 键共享)。"
  (interactive)
  (literal/knowledge-browse--refresh))

(defun literal/knowledge-browse--open-browser ()
  "在浏览器打开当前域的 web 可视化(调 `agenote viz --open')。"
  (interactive)
  (if (literal/agenote--resolve-executable)
      (literal/agenote-call-async literal--knowledge-browse-domain "viz" "--open")
    (message "未找到 agenote 命令")))

(defvar literal/knowledge-browse-mode-map
  (let ((m (make-sparse-keymap)))
    (define-key m "1" (lambda () (interactive)
                        (literal/knowledge-browse--switch-tab 1)))
    (define-key m "2" (lambda () (interactive)
                        (literal/knowledge-browse--switch-tab 2)))
    (define-key m "g" #'literal/knowledge-browse--force-refresh)
    (define-key m "o" #'literal/knowledge-browse--open-browser)
    (define-key m "q" #'quit-window)
    m)
  "literal/knowledge-browse-mode 键位。")

(define-derived-mode literal/knowledge-browse-mode special-mode
  "KB-Browse"
  "统一的知识库浏览 mode(Phase 2.3 合并 knowledge-viz + agenote-view)。
两个 Tab:类别树 / 时间线;域通过 buffer-local 变量区分(human / agenote)。
\\{literal/knowledge-browse-mode-map}"
  (setq buffer-read-only t)
  (setq-local revert-buffer-function
              #'literal/knowledge-browse--force-refresh))

;; 交互入口

;;;###autoload
(defun literal/knowledge-browse-human ()
  "打开人类域(experiences/)知识库浏览视图。
PLAN §2.3:替代历史 `literal/knowledge-viz',合并到统一 mode。"
  (interactive)
  (literal/knowledge-browse--open 'human))

;;;###autoload
(defun literal/knowledge-browse-agenote ()
  "打开 agenote 子域知识库浏览视图。
PLAN §2.3:替代历史 `literal/agenote-browse',合并到统一 mode。"
  (interactive)
  (literal/knowledge-browse--open 'agenote))

(defun literal/knowledge-browse--open (domain)
  "内部:打开 DOMAIN 域的浏览 buffer。"
  (let ((buf (get-buffer-create literal:knowledge-browse--buffer-name)))
    (with-current-buffer buf
      (unless (eq major-mode 'literal/knowledge-browse-mode)
        (literal/knowledge-browse-mode))
      (setq literal--knowledge-browse-domain domain)
      (literal/knowledge-browse--refresh))
    (switch-to-buffer buf)))

(defun literal/knowledge-viz-open-browser ()
  "在浏览器中打开知识库可视化(兼容旧入口)。
PLAN §2.3:保留 `C-c o k V' 绑定,默认人类域。"
  (interactive)
  (if (literal/agenote--resolve-executable)
      (literal/agenote-call-async 'human "viz" "--open")
    (message "未找到 agenote 命令")))
#+end_src

#+begin_src emacs-lisp
<<org/knowledge-core>>
<<org/knowledge-capture>>
<<org/knowledge-search>>
<<org/knowledge-cli>>
<<org/knowledge-browse>>
#+end_src

** agenote
:PROPERTIES:
:CUSTOM_ID: agenote
:END:
agenote CLI 集成:在 Emacs 内浏览 =~/Documents/Org/agenote/= 下的子域视图。

*** 数据读取
#+begin_src emacs-lisp :noweb-ref org/agenote-data :tangle no


;; 常量

(defconst literal:agenote-index-file
  (expand-file-name "index.json" literal:agenote-directory)
  "agenote 子域 index.json 路径。")

;; 数据层（纯函数）

(defun literal/agenote--load-index ()
  "读取 agenote/index.json，返回卡片 plist 列表。空或不可读返回 nil。"
  (when (file-readable-p literal:agenote-index-file)
    (let ((json-object-type 'plist)
          (json-array-type 'list)
          (json-key-type 'keyword))
      (with-temp-buffer
        (insert-file-contents literal:agenote-index-file)
        (goto-char (point-min))
        (condition-case nil
            (plist-get (json-read) :cards)
          (error nil))))))

(defun literal/agenote--group-by-category (cards)
  "按 category 分组 CARDS，返回 ((CATEGORY . CARDS-IN-CAT) ...) 列表。
类别按字典序，每组内卡片按 created 降序。"
  (let (groups)
    (dolist (card cards)
      (let ((cat (or (plist-get card :category) "unknown")))
        (if-let* ((pair (assoc cat groups)))
            (push card (cdr pair))
          (push (cons cat (list card)) groups))))
    ;; 每组内按 created 降序
    (dolist (group groups)
      (setcdr group (sort (cdr group)
                          (lambda (a b)
                            (string> (or (plist-get a :created) "")
                                     (or (plist-get b :created) ""))))))
    (sort groups (lambda (a b) (string< (car a) (car b))))))

(defun literal/agenote--sort-by-recency (cards)
  "按 created 降序排列 CARDS（最新在前）。"
  (sort cards
        (lambda (a b)
          (string> (or (plist-get a :created) "")
                   (or (plist-get b :created) "")))))

#+end_src

*** 健康度面板
#+begin_src emacs-lisp :noweb-ref org/agenote-health :tangle no
;; 健康度面板（agenote health，人类域）
;; `agenote health' 输出本身已是结构化人类可读文本（节标题 / 指标行带阈值与
;; ✅），无需重新解析。直接展示在 special-mode buffer，`g' 调
;; `agenote health --quality' 取更全数据（含内容长度 / 质量问题节）。
;; 注意：agenote health 只读人类域（experiences/），agent 子域健康度需另查
;; index.json（见浏览视图）。

(define-derived-mode literal/agenote-health-mode special-mode "agenote-health"
  "展示 `agenote health' 知识库健康度报告（人类域）。
\\{literal/agenote-health-mode-map}")

(defvar literal/agenote-health-mode-map
  (let ((m (make-sparse-keymap)))
    (define-key m "g" #'literal/agenote-health)
    m))

(defun literal/agenote--refresh-health-buffer ()
  "重新调 `agenote --domain human health --quality' 并填充当前 health buffer。"
  (let ((inhibit-read-only t))
    (erase-buffer)
    (insert (or (literal/agenote-call-string 'human "health" "--quality")
                "（agenote 命令不可用）"))
    (goto-char (point-min))))

;;;###autoload
(defun literal/agenote-health ()
  "打开人类知识库健康度面板 *agenote-health*（调 `agenote --domain human health --quality'）。
`g' 刷新。"
  (interactive)
  (if (literal/agenote--resolve-executable)
      (let ((buf (get-buffer-create "*agenote-health*")))
        (with-current-buffer buf
          (unless (eq major-mode 'literal/agenote-health-mode)
            (literal/agenote-health-mode))
          (literal/agenote--refresh-health-buffer))
        (switch-to-buffer buf))
    (message "未找到 agenote 命令")))
#+end_src

#+begin_src emacs-lisp
<<org/agenote-data>>
<<org/agenote-health>>
#+end_src

* 键位与补全
:PROPERTIES:
:CUSTOM_ID: keys-completion
:END:
全局直达键 + 多前缀体系(C-x / C-c / M-s / M-g 等)+ which-key 本体加载 + helpful 增强帮助 + winner 窗口布局撤销 + 补全框架全家桶(Vertico / Corfu / Consult)。

** Which-key 本体与辅助包
:PROPERTIES:
:CUSTOM_ID: which-key-runtime
:END:
加载 which-key 本体并配置显示参数;helpful 增强帮助系统;winner 窗口布局撤销 / 重做。

+ which-key 中文描述注册由 *界面与外观 → 右键菜单与 Which-key 中文化* 域处理( =literal/which-key-apply-descriptions= + =literal/register-pending-wk-descs= )
+ *中文字符对齐修复*: which-key 原生按 =length= (字符数)计算描述补齐宽度,中文双宽字符会让列错位。这里用 =advice-add :override= 替换 =which-key--pad-column= ,改为基于 =string-width= (显示列宽)计算,底部多列布局下固定为等宽分栏(默认 =literal:which-key-grid-columns 3= 列)
+ =which-key-idle-delay 0.3= :延迟显示,避免干扰快速操作
+ =winner-dont-bind-my-keys t= :避免 winner 抢占 =C-c ←= / =C-c →= (本配置在 =C-c w= 体系下手动绑定 winner-undo / redo)

#+begin_src emacs-lisp
;; Which-key(键位提示)

;; ── 中文字符对齐修复 ──────────────────────────────────────────────────────
;; which-key 原生按 `length'(字符数)计算描述补齐宽度,中文是双宽字符会导致列错位。
;; 这里统一改成基于 `string-width'(显示列宽)计算,并在底部多列布局下固定为等宽分栏。
;; 中文菜单默认三列网格;宽屏可升到 4,窄窗口则更适合降到 2。
(defconst literal:which-key-grid-columns 3
  "Which-key 中文菜单默认使用的固定网格列数。")

(defun literal/which-key--fixed-grid-layout-p ()
  "判断当前是否启用固定多列 which-key 网格布局。"
  (and (boundp 'which-key-max-display-columns)
       (integerp which-key-max-display-columns)
       (> which-key-max-display-columns 1)
       (boundp 'which-key-popup-type)
       (eq which-key-popup-type 'side-window)
       (boundp 'which-key-side-window-location)
       (eq which-key-side-window-location 'bottom)))

(defun literal/which-key--target-grid-column-width (available-width)
  "根据 AVAILABLE-WIDTH 计算固定网格布局下每列的目标宽度。"
  (if (literal/which-key--fixed-grid-layout-p)
      (let* ((columns which-key-max-display-columns)
             (gaps (max 0 (1- columns))))
        (/ (max 1 (- available-width gaps)) columns))
    available-width))

(defun literal/which-key--pad-column-fixed-width (col-keys available-width)
  "为 COL-KEYS 生成更适合中文的 which-key 列布局。
AVAILABLE-WIDTH 为当前 which-key 可用宽度。"
  (let* ((grid-layout-p (literal/which-key--fixed-grid-layout-p))
         (col-key-width (+ which-key-add-column-padding
                           (which-key--max-len col-keys 0)))
         (col-sep-width (which-key--max-len col-keys 1))
         (target-col-width (literal/which-key--target-grid-column-width available-width))
         (desc-space (max 1 (- target-col-width col-key-width col-sep-width)))
         (col-desc-width (if grid-layout-p
                             desc-space
                           (max which-key-min-column-description-width desc-space)))
         (col-width (+ col-key-width col-sep-width col-desc-width))
         (col-format (concat "%" (int-to-string col-key-width) "s%s%s")))
    (cons col-width
          (mapcar
           (pcase-lambda (`(,key ,sep ,desc ,_doc))
             (let* ((display-desc (truncate-string-to-width
                                   desc col-desc-width 0 nil which-key-ellipsis))
                    (padding (max 0 (- col-desc-width (string-width display-desc)))))
               (concat
                (format col-format key sep display-desc)
                (make-string padding ?\s))))
           col-keys))))

(use-package which-key
  :defer 0.3
  :config
  (which-key-mode 1)
  (setq which-key-idle-delay 0.3
        which-key-prefix-prefix ""
        which-key-allow-multiple-replacements t
        which-key-max-description-length 28
        ;; 中文条目在终端里更适合稳定的等宽分栏布局。
        ;; 默认三列;超宽屏可升到四列,窄窗口可降回两列。
        which-key-max-display-columns literal:which-key-grid-columns
        which-key-min-column-description-width 16
        which-key-add-column-padding 1
        which-key-min-display-lines 10
        which-key-side-window-max-height 0.38
        which-key-sort-order #'which-key-prefix-then-key-order
        which-key-C-h-map-prompt
        " \\<which-key-C-h-map>\\[which-key-show-next-page-cycle]/\\[which-key-show-previous-page-cycle] 翻页,\\[which-key-show-standard-help] 帮助,\\[which-key-abort] 退出")
  ;; Phase 5.2:用基于 `string-width' 的中文友好版覆盖 which-key 原生
  ;; `which-key--pad-column'(原生版按 `length' 计算补齐,中文双宽字符会
  ;; 导致列错位)。加版本守卫:which-key 3.6.1 仍用 length,补丁生效;
  ;; 上游改用 string-width 后(版本 > 3.6.1 或函数签名变化)自动跳过,
  ;; 不再静默 override 私有函数。完整 override 实现见上方函数定义。
  ;; TODO(Phase 5.2):把此 override 物理移到 compatibility 子树并加 ERT。
  (when (and (fboundp 'which-key--pad-column)
             (boundp 'which-key-version)
             (stringp which-key-version)
             (string-match-p "\\`3\\.6\\." which-key-version))
    (advice-add 'which-key--pad-column :override
                #'literal/which-key--pad-column-fixed-width)))

;; Helpful(增强帮助系统)
(use-package helpful
  :defer t
  :bind (([remap describe-function] . helpful-callable)
         ([remap describe-command]  . helpful-command)
         ([remap describe-variable] . helpful-variable)
         ([remap describe-key]      . helpful-key)))

;; Winner(窗口布局撤销/重做)
(use-package winner
  :custom
  (winner-dont-bind-my-keys t)
  :config
  (setq winner-boring-buffers
        (append winner-boring-buffers
                '("*Completions*" "*Help*" "*Apropos*" "*which-key*")))
  (winner-mode 1))
#+end_src

** 代码操作辅助函数
:PROPERTIES:
:CUSTOM_ID: code-commands
:END:
供快捷键和右键菜单复用的代码操作函数:复制行、移动行、跳转、格式化、块注释、标签切换。

+ 这些函数集中定义在本块,与下面的 *全局直达键* 同源
+ =literal/tabs-next= / =literal/tabs-previous= / =literal/tabs-refresh-context= 由 [[#tab-line][Tab-line]] 提供

#+begin_src emacs-lisp
(defun literal/code--current-line-text ()
  "返回当前行文本,包含行尾换行。"
  (buffer-substring (line-beginning-position)
                    (line-beginning-position 2)))

(defun literal/code-duplicate-line ()
  "复制当前行并在下方粘贴。"
  (interactive)
  (let ((column (current-column))
        (line (literal/code--current-line-text))
        (insert-at (line-beginning-position 2)))
    (goto-char insert-at)
    (insert line)
    (goto-char insert-at)
    (move-to-column column)))

(defun literal/code-move-line-up ()
  "将当前行上移一行。"
  (interactive)
  (transpose-lines 1)
  (forward-line -2))

(defun literal/code-move-line-down ()
  "将当前行下移一行。"
  (interactive)
  (forward-line 1)
  (transpose-lines 1)
  (forward-line -1))

(defun literal/code-goto-definition ()
  "跳转到定义。"
  (interactive)
  (call-interactively #'xref-find-definitions))

(defun literal/code-goto-references ()
  "查找所有引用。"
  (interactive)
  (call-interactively #'xref-find-references))

(defun literal/code-jump-back ()
  "跳回之前的位置。"
  (interactive)
  (if (fboundp 'xref-go-back) (xref-go-back) (pop-mark)))

(defun literal/code-jump-forward ()
  "跳到下一个位置。"
  (interactive)
  (if (fboundp 'xref-go-forward) (xref-go-forward)
    (message "前进功能不可用")))

(defun literal/code-format-dwim ()
  "智能格式化:有选区格式化选区,否则格式化整个缓冲区。"
  (interactive)
  (if (use-region-p)
      (indent-region (region-beginning) (region-end))
    (cond
     ((and (fboundp 'eglot-managed-p) (eglot-managed-p))
      (eglot-format-buffer))
     (t
      (indent-region (point-min) (point-max))))))

(defun literal/code-comment-block ()
  "使用块注释包围选区。"
  (interactive)
  (if (use-region-p)
      (let ((comment-style 'multi-line))
        (comment-region (region-beginning) (region-end)))
    (message "请先选择要块注释的区域")))
#+end_src

** 全局直达键
:PROPERTIES:
:CUSTOM_ID: global-keys
:END:
IDE 风格全局直达键,通过 =literal/set-key= 同源注册键位与 which-key 中文描述。

+ =C-h/j/k/l= 移动光标, =F1= 作为 help prefix 唯一入口(硬约束)
+ 直达键保留 IDE 风格( =C-s= 保存 / =C-p= 找文件 / =C-S-f= 全文搜索 等)
+ =literal/set-key= = 键位绑定 + which-key 描述同步注册(定义见 *启动与基础设施 → 键位辅助函数* )

#+begin_src emacs-lisp
;; 全局直达键(literal/set-key = 绑定 + which-key 描述同源)

;; 光标移动(C-h 已被 backward-char 占用,F1 作为 help prefix 入口)
(literal/set-key "C-h" #'backward-char "左移")
(define-key global-map (kbd "<f1>") help-map)
(literal/set-key "C-j" #'next-line "下移")
(literal/set-key "C-k" #'previous-line "上移")
(literal/set-key "C-l" #'forward-char "右移")

;; 撤销/重做
(literal/set-key "C-z" #'undo "撤销")
(literal/set-key "C-S-z" #'undo-redo "重做")

;; 文件与搜索(IDE 风格直达键)
(literal/set-key "C-s" #'save-buffer "保存")
(literal/set-key "C-f" #'consult-line "缓冲区搜索")
(literal/set-key "C-p" #'project-find-file "项目内查找文件")
(literal/set-key "C-S-f" #'consult-ripgrep "项目全文搜索")
(literal/set-key "C-S-b" #'consult-buffer "切换缓冲区")
(literal/set-key "C-S-o" #'consult-imenu "符号大纲")
(literal/set-key "C-S-j" #'avy-goto-char-timer "屏幕内快速跳转")
(literal/set-key "C-S-p" #'execute-extended-command "命令面板")

;; 标签切换(Emacs 31 的 keymap-global-set 要求修饰键写在 <> 外部)
(literal/set-key "C-<tab>" #'literal/tabs-next "下一个标签")
(literal/set-key "C-<iso-lefttab>" #'literal/tabs-previous "上一个标签")
(literal/set-key "C-S-<tab>" #'literal/tabs-previous "上一个标签")
(literal/set-key "C-<next>" #'literal/tabs-next "下一个标签")
(literal/set-key "C-<prior>" #'literal/tabs-previous "上一个标签")

;; 编辑操作
(literal/set-key "C-a" #'mark-whole-buffer "全选")
(literal/set-key "C-/" #'literal/comment-dwim-stable "注释")
(literal/set-key "C-_" #'literal/comment-dwim-stable "注释")
(literal/set-key "C-S-c" #'literal/copy-dwim "复制")
(literal/set-key "C-S-v" #'yank "粘贴")
(literal/set-key "C-S-x" #'literal/cut-dwim "剪切")
(literal/set-key "C-S-w" #'kill-current-buffer "关闭缓冲区")
(literal/set-key "C-S-t" #'consult-recent-file "最近文件")
(literal/set-key "C-S-d" #'literal/code-duplicate-line "复制当前行")
(literal/set-key "C-S-<up>" #'literal/code-move-line-up "上移当前行")
(literal/set-key "C-S-<down>" #'literal/code-move-line-down "下移当前行")
(literal/set-key "C-S-/" #'literal/code-comment-block "块注释")
(literal/set-key "C-S-s" #'write-file "另存为")
(literal/set-key "C-S-a" #'literal/find-file-in-project "项目文件搜索")

;; 代码导航
(literal/set-key "<f2>" #'literal/rename-dwim "重命名")
(literal/set-key "C-<f12>" #'literal/code-goto-definition "跳转到定义")
(literal/set-key "C-S-<f12>" #'literal/code-goto-references "查找引用")
(literal/set-key "C-S-i" #'literal/code-format-dwim "智能格式化")
(literal/set-key "C-`" #'literal/open-terminal "打开终端")

;; 缩放
(literal/set-key "C-=" #'text-scale-increase "放大")
(literal/set-key "C--" #'text-scale-decrease "缩小")
(literal/set-key "C-0" #'literal/reset-text-scale "重置缩放")

;; 窗口切换(原生 windmove)
(literal/set-key "M-<left>" #'windmove-left "左切窗口")
(literal/set-key "M-<right>" #'windmove-right "右切窗口")
(literal/set-key "M-<up>" #'windmove-up "上切窗口")
(literal/set-key "M-<down>" #'windmove-down "下切窗口")

;; C-c e:多光标 / 修改点跳转(Phase 7.2 新增前缀)
(literal/set-key "C-c e l" #'mc/edit-lines "多光标-每行一个")
(literal/set-key "C-c e n" #'mc/mark-next-like-this "多光标-标记下一个匹配")
(literal/set-key "C-c e p" #'mc/mark-previous-like-this "多光标-标记上一个匹配")
(literal/set-key "C-c e a" #'mc/mark-all-like-this "多光标-标记所有匹配")
(literal/set-key "C-c e d" #'mc/mark-all-like-this-in-defun "多光标-函数内所有匹配")
(literal/set-key "C-c e ." #'goto-last-change "跳到上一处修改")
(literal/set-key "C-c e ," #'goto-last-change-reverse "跳到下一处修改")
#+end_src

** 前缀键体系
:PROPERTIES:
:CUSTOM_ID: keybindings
:END:
Emacs 原生前缀扩展( =C-x= / =M-s= / =M-g= ) + 自定义 =C-c= 前缀分组。

+ 前缀分组: =C-c g= Git / =C-c w= 窗口 / =C-c l= LSP / =C-c o= Org / =C-c a= 应用 / =C-c m= 补全 / =C-c z= 折叠 / =C-c f= 格式化 / =C-c h= 帮助
+ =C-x p= 项目(project.el 配置 + consult-fd 体系见 *项目与导航 → 项目管理 project.el* )
+ Git 操作函数( =literal/git-*= )定义在 [[#git-display][Git 与窗口路由]]

#+begin_src emacs-lisp
;; 前缀键体系(Emacs 原生前缀扩展 + 自定义 C-c 前缀)

;; C-x:文件、缓冲区、标签
(keymap-global-set "C-x C-r" #'consult-recent-file)
(keymap-global-set "C-x b" #'consult-buffer)
(keymap-global-set "C-x B" #'ibuffer)
(keymap-global-set "C-x k" #'kill-buffer-and-window)
(keymap-global-set "C-x K" #'kill-buffer)
(keymap-global-set "C-x t n" #'literal/tabs-next)
(keymap-global-set "C-x t p" #'literal/tabs-previous)
(keymap-global-set "C-x t g" #'literal/tabs-refresh-context)

;; C-x p:项目(project.el 配置 + consult-fd 体系见 *项目与导航* 域)
;; Phase 3 迁移自 projectile:全部走 project.el 公开 API + consult-* 入口。
;; C-x p p = project-switch-project(替代 consult-projectile),
;; C-x p k = project-kill-buffers(替代 projectile-kill-buffers)。
(keymap-global-set "C-x p p" #'project-switch-project)
(keymap-global-set "C-x p f" #'project-find-file)
(keymap-global-set "C-x p s" #'literal/project-search)
(keymap-global-set "C-x p o" #'literal/open-project-folder)
(keymap-global-set "C-x p O" #'literal/open-directory)
(keymap-global-set "C-x p k" #'project-kill-buffers)
(keymap-global-set "C-x p D" #'project-dired)

;; M-s:搜索
(keymap-global-set "M-s l" #'consult-line)
(keymap-global-set "M-s p" #'consult-ripgrep)
(keymap-global-set "M-s b" #'consult-buffer)
(keymap-global-set "M-s e" #'consult-eglot-symbols)
(keymap-global-set "M-s f" #'consult-flymake)

;; M-g:跳转、错误(Flymake 后端)
(keymap-global-set "M-g n" #'flymake-goto-next-error)
(keymap-global-set "M-g p" #'flymake-goto-prev-error)
(keymap-global-set "M-g l" #'literal/flymake-list-errors-dwim)
(keymap-global-set "M-g r" #'literal/code-goto-references)
(keymap-global-set "M-g b" #'literal/code-jump-back)
(keymap-global-set "M-g f" #'literal/code-jump-forward)
(keymap-global-set "M-g j" #'avy-goto-char-timer)
(keymap-global-set "M-," #'literal/code-jump-back)

;; C-c m:补全框架(literal/set-key = 绑定 + which-key 中文描述同源)
(literal/set-key "C-c m R" #'vertico-reverse-mode "补全-反向显示")
(literal/set-key "C-c m j" #'vertico-quick-jump "补全-单键跳转")
(literal/set-key "C-c m s" #'vertico-suspend "补全-挂起会话")
(literal/set-key "C-c m v" #'vertico-repeat "补全-恢复会话")
(literal/set-key "C-c m a" #'prescient-toggle-anchored "补全-切换锚定")
(literal/set-key "C-c m p" #'prescient-toggle-prefix "补全-切换前缀")
(literal/set-key "C-c m f" #'prescient-toggle-fuzzy "补全-切换模糊")
(literal/set-key "C-c m r" #'prescient-toggle-regexp "补全-切换正则")
(literal/set-key "C-c m c" #'cape-interactive "补全-交互capf")
(literal/set-key "C-c m d" #'cape-dabbrev "补全-缩写展开")
(literal/set-key "C-c m F" #'cape-file "补全-文件路径")
(literal/set-key "C-c m h" #'cape-history "补全-历史回填")
(literal/set-key "C-c m k" #'cape-keyword "补全-关键字")
(literal/set-key "C-c m l" #'cape-line "补全-整行近似")

;; C-c l:代码 / LSP
(keymap-global-set "C-c l d" #'literal/code-goto-definition)
(keymap-global-set "C-c l r" #'literal/code-goto-references)
(keymap-global-set "C-c l a" #'eglot-code-actions)
(keymap-global-set "C-c l n" #'eglot-rename)
(keymap-global-set "C-c l c" #'completion-at-point)
(keymap-global-set "C-c l e" #'literal/flymake-list-errors-dwim)
;; C-c l x:LSP 协议扩展(eglot-x)
(keymap-global-set "C-c l x d" #'eglot-find-declaration)
(keymap-global-set "C-c l x i" #'eglot-find-implementation)
(keymap-global-set "C-c l x t" #'eglot-find-typeDefinition)

;; C-c f:格式化
(keymap-global-set "C-c f f" #'literal/code-format-dwim)
(keymap-global-set "C-c f b" #'apheleia-format-buffer)

;; C-c z:折叠(基于 hs-minor-mode)
(add-hook 'prog-mode-hook #'hs-minor-mode)
(add-hook 'conf-mode-hook #'hs-minor-mode)
(keymap-global-set "C-c z a" #'hs-toggle-hiding)
(keymap-global-set "C-c z c" #'hs-hide-block)
(keymap-global-set "C-c z o" #'hs-show-block)
(keymap-global-set "C-c z m" #'hs-hide-all)
(keymap-global-set "C-c z r" #'hs-show-all)
(unless (key-binding (kbd "<backtab>"))
  (keymap-global-set "<backtab>" #'hs-toggle-hiding))

;; C-c g:Git
(keymap-global-set "C-c g s" #'literal/git-status-dwim)
(keymap-global-set "C-c g b" #'literal/git-blame-current-file)
(keymap-global-set "C-c g l" #'literal/git-log-current-file)
(keymap-global-set "C-c g d" #'literal/git-diff-current-file)
(keymap-global-set "C-c g t" #'literal/git-timemachine-toggle)
(keymap-global-set "C-c g S" #'literal/git-stage-current-file)
(keymap-global-set "C-c g D" #'literal/git-discard-current-file)
(keymap-global-set "C-c g P" #'literal/git-push-current-repo)
(keymap-global-set "C-c g F" #'literal/git-pull-current-repo)
(keymap-global-set "C-c g #" #'literal/git-stash-push)
(keymap-global-set "C-c g @" #'literal/git-stash-pop)

;; C-c w:窗口创建
(keymap-global-set "C-c w r" #'ibuffer)
(keymap-global-set "C-c w d" #'literal/open-directory)
(keymap-global-set "C-c w v" #'literal/open-terminal)
(keymap-global-set "C-c w u" #'winner-undo)
(keymap-global-set "C-c w U" #'winner-redo)

;; C-c o:Org 扩展
(keymap-global-set "C-c o a" #'org-agenda)
(keymap-global-set "C-c o i" #'org-insert-structure-template)
(keymap-global-set "C-c o o" #'olivetti-mode)
(keymap-global-set "C-c o d" #'deft)
(keymap-global-set "C-c o n f" #'org-roam-node-find)
(keymap-global-set "C-c o n i" #'org-roam-node-insert)
(keymap-global-set "C-c o n l" #'org-roam-buffer-toggle)

;; C-c c:Capture / Clock
(keymap-global-set "C-c c n" #'literal/note-new)
(keymap-global-set "C-c c i" #'org-clock-in)
(keymap-global-set "C-c c o" #'org-clock-out)
(keymap-global-set "C-c c g" #'org-clock-goto)

;; C-c h:帮助(因为 C-h 已保留给左移)
(keymap-global-set "C-c h f" #'helpful-callable)
(keymap-global-set "C-c h v" #'helpful-variable)
(keymap-global-set "C-c h k" #'helpful-key)
(keymap-global-set "C-c h m" #'describe-mode)
(keymap-global-set "C-c h ?" #'literal/show-help)

;; C-c a:应用(重复按两遍进入主界面原则)
(keymap-global-set "C-c a f f" #'elfeed)
(keymap-global-set "C-c a f u" #'elfeed-update)
(keymap-global-set "C-c a c" #'cfw:open-org-calendar)
(keymap-global-set "C-c a m" #'notmuch)
(keymap-global-set "C-c a t t" #'telega)
(keymap-global-set "C-c a e e" #'ement)
(keymap-global-set "C-c a g t" #'tetris)
(keymap-global-set "C-c a g s" #'snake)
(keymap-global-set "C-c a g m" #'gomoku)
(keymap-global-set "C-c a g p" #'pong)
;; Phase 7.2:PDF Tools + Agent Shell 声明式入口(PLAN §7.2 强约束)
(literal/set-key "C-c a p" #'find-file "应用-打开 PDF")
(literal/set-key "C-c a a" #'agent-shell "应用-Agent Shell 主入口")
(literal/set-key "C-c a A" #'agent-shell-toggle "应用-Agent Shell 切换")

;; C-c o k:知识库
(keymap-global-set "C-c o k c" #'literal/knowledge-capture)
(keymap-global-set "C-c o k s" #'literal/knowledge-search)
(keymap-global-set "C-c o k t" #'literal/knowledge-search-by-tag)
(keymap-global-set "C-c o k I" #'literal/knowledge-open-inbox)
(keymap-global-set "C-c o k S" #'literal/knowledge-stats)
(keymap-global-set "C-c o k v" #'literal/knowledge-browse-human)
(keymap-global-set "C-c o k b" #'literal/knowledge-browse-agenote)
;; 生命周期管理(agenote CRUD)
(keymap-global-set "C-c o k a" #'literal/knowledge-archive-inbox-entry)
(keymap-global-set "C-c o k d" #'literal/knowledge-deduplicate)
(keymap-global-set "C-c o k e" #'literal/knowledge-merge)
(keymap-global-set "C-c o k l" #'literal/knowledge-lint)
(keymap-global-set "C-c o k m" #'literal/knowledge-memory)
(keymap-global-set "C-c o k n" #'literal/knowledge-connect)
(keymap-global-set "C-c o k o" #'literal/knowledge-commit)
(keymap-global-set "C-c o k r" #'literal/knowledge-review)
(keymap-global-set "C-c o k u" #'literal/knowledge-touch)
(keymap-global-set "C-c o k V" #'literal/knowledge-viz-open-browser)

;; Org mode 局部
(with-eval-after-load 'markdown-mode
  (keymap-set markdown-mode-map "C-c p" #'markdown-preview))
#+end_src

** Completion 框架
:PROPERTIES:
:CUSTOM_ID: completion
:END:
Vertico(接管 minibuffer,不再回退到 =*Completions*= buffer / =completion-list-mode= )、Corfu(接管 in-region 代码补全)、Consult / Embark / Orderless / Marginalia / Prescient / Cape 全家桶。

+ 直接复用启动域的 aspell/hunspell 路径缓存与 [[#frame][Frame 生命周期]]
+ *consult-dir* (minibuffer 内目录切换, =C-x C-d= / =C-x C-j= )已移入本模块,与 vertico-map 集成
+ *Org 禁用 corfu-auto* 逻辑已移入本模块的 =literal/completion-setup-cape-org=
+ 补全相关命令的前缀分组键位( =C-c m= )见 *前缀键体系* 块
+ vertico-buffer 形态在 Emacs 31 下的已知兼容性问题(FIXME)见 *实验性与待确认* 块

*** Minibuffer 补全
#+begin_src emacs-lisp :noweb-ref keys/completion-minibuffer :tangle no


(require 'ispell)

;; savehist - 持久化 minibuffer 历史和 vertico 最后查询
;; Phase 5.1:删除顶层重复 (savehist-mode 1);savehist 已在 *启动与基础设施*
;; startup 域 use-package savehist :defer 0.5 + :config (savehist-mode 1) 启用。

;; Vertico - 垂直补全界面

;; Vertico 提供简洁高效的垂直补全界面，接管 minibuffer 的 completing-read。
;; 启用后 Emacs 不再回退到内置 *Completions* buffer（completion-list-mode）。
(use-package vertico
  :custom
  ;; 显示更多候选 + 循环
  (vertico-count 12)
  (vertico-cycle t)
  :config
  (vertico-mode 1))

;; Vertico Multiform - 按命令/补全类别切换 Vertico 形态
;; vertico-multiform-mode 允许按 completing-read 命令或 completion category
;; 切换 Vertico 显示形态（vertical / buffer / flat / grid / unobtrusive 等），
;; 还能附带 keymap、sort function、buffer-local 设置。形态切换见
;; `vertico-multiform-map'：M-B buffer、M-F flat、M-G grid、M-U unobtrusive、
;; M-V vertical。
;; 配置策略（参考 vertico README "Configuration per command"）：
;; - 大纲类（imenu / outline / bookmark）：indexed —— 候选前加 [1] [2] 数字
;;   索引，用 M-<数字> 快速跳转，比 minibuffer 一行行扫强得多
;; - M-x（execute-extended-command）：unobtrusive —— 不喧宾夺主
;; - file category：grid —— 文件名横排成多列，路径补全同时挂 directory-map
;;   的 RET/L 并行补全能力
;; - consult-grep category：indexed —— 搜索结果加数字索引便于跳转
;; 下方 multiform 用 indexed 替代 buffer 的原因见 *系统工具与实验性 → 实验性*:
<<emacs31/vertico-buffer-note>>

(use-package vertico-multiform
  :after vertico
  :config
  ;; 按命令定制形态
  (setq vertico-multiform-commands
        '((consult-imenu indexed)
          (consult-outline indexed)
          (consult-bookmark indexed)
          (consult-theme indexed)
          (execute-extended-command unobtrusive)))

  ;; 按补全类别定制形态
  (setq vertico-multiform-categories
        '((file grid (:keymap . vertico-directory-map))
          (consult-grep indexed)
          (imenu indexed)
          (bookmark indexed)
          (symbol (vertico-sort-function . vertico-sort-alpha))
          (library (vertico-sort-function . vertico-sort-alpha))))

  (vertico-multiform-mode 1))

;; ── Vertico Directory — minibuffer 内目录导航 ──
;; 在 file 补全中：RET 进入子目录、C-l 返回上级、DEL 删字符、M-DEL 删词。
;; vertico-multiform 已将 vertico-directory-map 挂到 grid file 形态，
;; 此处绑定到 vertico-map 覆盖所有文件补全场景。
(use-package vertico-directory
  :after vertico
  :bind (:map vertico-map
              ("RET" . vertico-directory-enter)
              ("DEL" . vertico-directory-delete-char)
              ("M-DEL" . vertico-directory-delete-word)
              ("C-l" . vertico-directory-up))
  :hook (rfn-eshadow-update-overlay . vertico-directory-tidy))

;; ── Vertico Quick — 单键跳转候选 ──
;; M-j 给每个候选分配单字母快捷鍵，一键跳转。
(use-package vertico-quick
  :after vertico
  :bind (:map vertico-map
              ("M-j" . vertico-quick-jump)
              ("M-J" . vertico-quick-exit)))

;; ── Vertico Repeat — 恢复上次补全会话 ──
;; M-R 恢复上次 minibuffer 会话状态。
(use-package vertico-repeat
  :after vertico
  :bind (:map vertico-map
              ("M-R" . vertico-repeat)))

;; Prescient - 基于频率/最近使用的候选排序
;; Prescient 维护一份本地候选历史，按使用频率 + 最近使用排序补全候选。
;; `vertico-prescient' / `corfu-prescient' 分别接入 Vertico minibuffer 与
;; Corfu in-region 补全。`prescient-persist-mode' 把状态存到
;; `prescient-save-file'，跨 session 保留排序记忆。

(use-package prescient
  :config
  (prescient-persist-mode 1))

(use-package vertico-prescient
  :after (vertico prescient)
  :config
  ;; vertico-sort-override-function 由 prescient 接管
  (vertico-prescient-mode 1))

(use-package corfu-prescient
  :after (corfu prescient)
  :config
  (corfu-prescient-mode 1))

;; Marginalia - 补全注释

;; 为补全候选项添加有用的注释信息
;; 例如：命令显示快捷键，文件显示大小和修改时间
(use-package marginalia
  :config
  (marginalia-mode 1))

;; Orderless - 无序补全

;; 支持空格分隔的无序模糊匹配
;; 例如：输入 "buf swi" 可以匹配 "switch-to-buffer"
(use-package orderless
  :custom
  (completion-styles '(orderless basic))
  (completion-category-overrides '((file (styles basic partial-completion)))))

;; Consult - 搜索与导航增强

;; Consult 提供增强的搜索、导航和预览功能。
;; 全局快捷键（consult-line / consult-buffer / consult-ripgrep 等）在
;; emacs.org 全局直达键 / 前缀键体系块注册，本块仅配置 use-package 本体。
(use-package consult
  :defer t
  :custom
  (xref-show-xrefs-function #'consult-xref)
  (xref-show-definitions-function #'consult-xref))

;; Embark - 上下文操作

;; Embark 提供类似右键菜单的上下文操作。
;; 全局键位（C-. embark-act / C-; embark-dwim）在 emacs.org 前缀键体系块注册。
(use-package embark
  :defer t
  :init
  (setq prefix-help-command #'embark-prefix-help-command))

;; Embark 与 Consult 集成
(use-package embark-consult
  :after (embark consult)
  :defer t)

;; Consult-dir - Minibuffer 内目录切换

;; 在 find-file、write-file 等 minibuffer 场景中用 Consult 选择最近目录、
;; 项目目录或书签目录，减少手动输入路径。
;; 与 vertico 集成，:map vertico-map 挂 minibuffer 内目录跳转。
(use-package consult-dir
  :after consult
  :defer t
  :commands (consult-dir consult-dir-jump-file)
  :bind (("C-x C-d" . consult-dir)
         :map vertico-map
         ("C-x C-d" . consult-dir)
         ("C-x C-j" . consult-dir-jump-file)))

;; Wgrep - 可编辑搜索结果

;; Consult + Embark 导出的 grep buffer 可以进入 wgrep，像编辑普通 buffer 一样
;; 批量修改搜索结果，再统一写回文件。
(use-package wgrep
  :defer t
  :commands (wgrep-change-to-wgrep-mode wgrep-finish-edit
                                        wgrep-abort-changes wgrep-save-all-buffers)
  :custom
  (wgrep-auto-save-buffer t)
  (wgrep-change-readonly-file t))

(with-eval-after-load 'grep
  (require 'wgrep)
  (keymap-set grep-mode-map "e" #'wgrep-change-to-wgrep-mode)
  (keymap-set grep-mode-map "C-c C-e" #'wgrep-change-to-wgrep-mode)
  ;; grep 输出按文件分节（Emacs 30+，与 rg --heading 风格一致）
  (when (boundp 'grep-use-headings)
    (setq grep-use-headings t)))

#+end_src

*** In-region 补全
#+begin_src emacs-lisp :noweb-ref keys/completion-region :tangle no
;; Corfu - 代码补全弹窗

;; ispell 词表发现(aspell/hunspell 使用启动域缓存)
(defconst literal/completion:ispell-word-list-candidates
  (delq nil
        (list (and (getenv "GUIX_PROFILE")
                   (expand-file-name "share/dict/words" (getenv "GUIX_PROFILE")))
              (expand-file-name "~/.guix-home/profile/share/dict/words")
              (expand-file-name "~/.guix-profile/share/dict/words")
              "/run/current-system/profile/share/dict/words"
              "/usr/share/dict/words"
              "/usr/share/dict/web2"))
  "候选 plain word list 路径。`miscfiles' 通常提供 share/dict/words。")

(defvar literal/completion--ispell-word-list nil
  "已发现的 plain word list 文件。nil 表示尚未找到。")

(defun literal/completion-ispell-find-word-list ()
  "返回可用于 `ispell-alternate-dictionary' 的 plain word list 文件。"
  (or literal/completion--ispell-word-list
      (setq literal/completion--ispell-word-list
            (seq-find #'file-readable-p literal/completion:ispell-word-list-candidates))))

(defun literal/completion-setup-ispell-program ()
  "配置可用的 ispell 后端命令。
两者均不可用时不修改 ispell 配置。"
  (when-let* ((program (or literal:executable-aspell
                           literal:executable-hunspell)))
    (setq ispell-program-name program)
    (when literal:executable-aspell
      (setq ispell-extra-args '("--sug-mode=ultra")))))

(defun literal/completion-remove-ispell-completion-at-point ()
  "从当前 buffer 的 CAPF 列表移除 `ispell-completion-at-point'。"
  (setq-local completion-at-point-functions
              (remove 'ispell-completion-at-point
                      (remove #'ispell-completion-at-point
                              completion-at-point-functions))))

(defun literal/completion-setup-ispell-word-list ()
  "为当前 buffer 配置单词补全词表。

若找不到 plain word list，则移除当前 buffer 的
`ispell-completion-at-point'，避免 Corfu 自动补全触发缺词表错误。"
  (let ((word-list (literal/completion-ispell-find-word-list)))
    (if word-list
        (setq-local ispell-alternate-dictionary word-list)
      (literal/completion-remove-ispell-completion-at-point))))

(literal/completion-setup-ispell-program)

(add-hook 'text-mode-hook #'literal/completion-setup-ispell-word-list)
(add-hook 'org-mode-hook #'literal/completion-setup-ispell-word-list)
(add-hook 'git-commit-setup-hook #'literal/completion-setup-ispell-word-list)

;; ── Corfu 本体 ──
;; Corfu 提供轻量级的代码补全界面，接管 in-region 补全（替代 *Completions* 回退）。
;; 延迟 0.2 秒加载以加速启动
;; 配置说明：
;; - corfu-auto: 自动弹出补全
;; - corfu-auto-prefix: 输入几个字符后触发补全
;; - corfu-quit-no-match: 无匹配时的行为
;; - corfu-cycle: 循环选择候选项
(use-package corfu
  :defer 0.2
  :bind (:map corfu-map
              ("TAB" . corfu-insert)
              ("<tab>" . corfu-insert))
  :custom
  (corfu-auto t)                      ; 自动补全
  (corfu-auto-prefix 2)               ; 输入 2 个字符后触发
  (corfu-quit-no-match 'separator)    ; 无匹配时保留输入
  (corfu-cycle t)                     ; 循环选择
  :config
  (global-corfu-mode 1))

;; Cape - 为 CAPF 增强路径补全

;; Cape 提供 Completion-At-Point 扩展后端（CAPF），与 Corfu 的 in-region 补全
;; 完全兼容。各 capf 通过 buffer-local 追加到 `completion-at-point-functions'，
;; 排在已有 major-mode / LSP capf 之后兜底。
;; 可用 capf 清单（cape 2.7）：
;; - cape-dabbrev: 当前 buffer 内动态缩写展开（覆盖旧 cape-symbol 功能）
;; - cape-file:    字符串内路径补全
;; - cape-history: 当前 minibuffer/region 历史回填
;; - cape-keyword: 编程语言关键字补全（仅 prog-mode 有意义）
;; - cape-line:    当前 buffer 内整行近似补全（性能略重，单独控制）
;; - cape-tex:     LaTeX 数学符号补全（按需）
;; 按模式分组配置 capf，避免 LSP 管理的 prog-mode 被无关 capf 干扰。

(use-package cape
  :defer t
  :commands (cape-dabbrev cape-file cape-history cape-keyword cape-line))

(defun literal/completion-cape-add-capfs (capfs)
  "把 CAPFS（符号列表）追加到当前 buffer 的 `completion-at-point-functions'。
每个 capf 仅在已 fboundp 时追加，避免 cape 版本差异导致报错。"
  (dolist (capf capfs)
    (when (fboundp capf)
      (add-hook 'completion-at-point-functions capf t t))))

(defun literal/completion-setup-cape-org ()
  "Org mode 的 cape capf 组合：只保留 file（路径补全）。
由于 org-mode 是 text-mode 的衍生模式，`text-mode-hook' 会先运行并插入
cape-dabbrev / cape-line；因此本函数先移除这两个 capf，再追加 cape-file，
避免普通写作中弹出大量无关缩写和整行候选。org 自身关键字 / 链接 / roam
补全仍保留。

同时关闭 Org buffer 的 corfu-auto（手动 M-Tab 仍可用），减少写作干扰。"
  (setq-local completion-at-point-functions
              (remove #'cape-line
                      (remove 'cape-line
                              (remove #'cape-dabbrev
                                      (remove 'cape-dabbrev
                                              completion-at-point-functions)))))
  (literal/completion-cape-add-capfs '(cape-file))
  (when (boundp 'corfu-auto)
    (setq-local corfu-auto nil)))

(defun literal/completion-setup-cape-text ()
  "文本类 buffer（text/conf）的 cape capf 组合：
dabbrev（缩写展开）+ file（路径）+ line（行近似）。
ispell 拼写补全由 `literal/completion-setup-ispell-word-list' 单独管理
（在找到 word list 时保留 Emacs 自带的 `ispell-completion-at-point'）。"
  (literal/completion-cape-add-capfs '(cape-dabbrev cape-file cape-line)))

(defun literal/completion-setup-cape-prog ()
  "编程类 buffer 的 cape capf 组合：
dabbrev + keyword（关键字）+ file（路径）。
不挂 cape-line，避免大 buffer 拖慢 LSP 补全响应。"
  (literal/completion-cape-add-capfs '(cape-dabbrev cape-keyword cape-file)))

(add-hook 'prog-mode-hook #'literal/completion-setup-cape-prog)
(add-hook 'conf-mode-hook #'literal/completion-setup-cape-prog)
(add-hook 'text-mode-hook #'literal/completion-setup-cape-text)
(add-hook 'org-mode-hook #'literal/completion-setup-cape-org)

(with-eval-after-load 'eglot
  (add-hook 'eglot-managed-mode-hook #'literal/completion-setup-cape-prog))

;; Corfu 文档预览 - 在补全弹窗旁边显示候选文档

;; corfu-popupinfo 在补全弹窗旁显示文档详细信息
;; 默认不自动显示文档；仅通过显式动作（如 M-d）打开
(use-package corfu-popupinfo
  :after corfu
  :bind (:map corfu-map
              ("M-d" . corfu-popupinfo-toggle))
  :custom
  (corfu-popupinfo-delay 0.5)
  :config
  ;; JetBrains 风格：默认仅显示候选列表，文档按需手动打开
  (corfu-popupinfo-mode -1))

;; Kind-icon - 补全图标

;; 为补全候选项添加图标（仅 GUI 模式）
;; 图标可以直观显示候选项类型（函数、变量、类等）
(use-package kind-icon
  :commands (kind-icon-margin-formatter)
  :init
  (setq kind-icon-default-face 'corfu-default))

;; 终端模式下使用 Nerd Font 文字图标作为补全类型标记
;; 定义部分（无 display 依赖，可安全放在顶层）
(defvar literal/completion--corfu-terminal-kind-icons
  '((file . "󰈔") (folder . "󰉋") (function . "󰊕") (variable . "󰀫")
    (module . "󰏗") (interface . "󰡧") (keyword . "󰌋") (method . "󰊕")
    (property . "󰜢") (unit . "󰑘") (value . "󰎠") (enum . "󰕘")
    (operator . "󰆕") (class . "󰌗") (struct . "󰌗") (type-parameter . "󰊄")
    (snippet . "󰘍") (color . "󰏘") (reference . "󰈇") (text . "󰉿")
    (event . "󰉁") (constant . "󰏿") (enum-member . "󰀬")
    (t . "󰀫"))
  "补全类型到 Nerd Font 图标的映射表。")

(defun literal/completion--corfu-terminal-margin-formatter (metadata)
  "终端模式下使用 Nerd Font 图标格式化 Corfu 边距。"
  (let* ((kind (or (completion-metadata-get metadata 'company-kind)
                   (completion-metadata-get metadata 'kind)))
         (icons literal/completion--corfu-terminal-kind-icons))
    (lambda (candidate)
      (let* ((kind-sym (and kind (funcall kind candidate)))
             (kind-item (or kind-sym t))
             (icon (or (cdr (assq kind-item icons)) (cdr (assq t icons)))))
        (propertize (concat " " icon " ") 'face 'corfu-default)))))

;; Phase 5.1:删除 corfu-doc / corfu-doc-terminal use-package。
;; corfu-popupinfo 是 Corfu 2.10+ 官方推荐的文档显示入口,在 GUI 和 TTY
;; 上行为一致(原 corfu-doc 的 M-d 绑定已由 corfu-popupinfo 接管)。
;; PLAN §5.1:只保留 corfu-popupinfo 作为唯一文档入口。

;; Consult-eglot - Eglot 符号搜索

;; 通过 Consult 界面搜索 Eglot/LSP 管理的项目符号
(use-package consult-eglot
  :defer t
  :commands (consult-eglot-symbols))

;; Consult-flymake - Flymake 错误浏览(Emacs 29+ 内置,无独立包)
;; consult 内置 consult-flymake 命令,通过 Consult 界面浏览和跳转 Flymake 诊断。
;; M-s f 在前缀键体系绑定到 consult-flymake。

#+end_src

*** Frame 显示适配
#+begin_src emacs-lisp :noweb-ref keys/completion-display :tangle no
;; Per-frame 补全显示设置(daemon 安全)

;; Phase 5.1:单一 margin formatter,按当前 frame 的 display 类型选择 GUI
;; kind-icon 或 TTY Nerd Font 图标。不再由 frame hook 全局 setq
;; `corfu-margin-formatters'(PLAN §5.1:GUI/TTY 差异必须 frame-local,
;; 禁止新 frame 修改全局变量后影响旧 frame)。
(defun literal/completion--corfu-margin-formatter (metadata)
  "根据当前 frame 的 display 类型选择 GUI / TTY 图标 formatter。"
  (if (display-graphic-p)
      (when (fboundp 'kind-icon-margin-formatter)
        (funcall 'kind-icon-margin-formatter metadata))
    (funcall 'literal/completion--corfu-terminal-margin-formatter metadata)))

(defun literal/completion-setup-display (&optional frame)
  "根据 FRAME 的 display 类型设置补全框架的显示相关配置。
Phase 5.1:Corfu 2.10+ 在 Emacs 31 原生支持 TTY childframe,删除
corfu-terminal 依赖。GUI 模式仅保留 x-gtk-resize-child-frames 修复;
margin formatter 由 \`literal/completion--corfu-margin-formatter\` 按
当前 frame 动态选择,不再全局 setq。"
  (with-selected-frame (or frame (selected-frame))
    (with-eval-after-load 'corfu
      ;; 单一 formatter 注册一次:内部按 (display-graphic-p) 分发
      (setq corfu-margin-formatters
            '(literal/completion--corfu-margin-formatter))
      (when (display-graphic-p)
        ;; GUI: PGTK/Wayland 修复 — childframe 位置偏移
        (when (boundp 'x-gtk-resize-child-frames)
          (setq x-gtk-resize-child-frames 'resize-mode))))))

;; 注册到 frame 生命周期（add-frame-hook 由 Frame 块提供）
(literal/add-frame-hook #'literal/completion-setup-display)
#+end_src

#+begin_src emacs-lisp
<<keys/completion-minibuffer>>
<<keys/completion-region>>
<<keys/completion-display>>
#+end_src

* 系统工具与实验性
:PROPERTIES:
:CUSTOM_ID: system-tools
:END:
Guix 环境检测、daemon 预热、Dashboard(延后加载)、Emacs 31 兼容性 hack、需要决策的配置。

** Guix 集成与 daemon 预热
:PROPERTIES:
:CUSTOM_ID: guix-daemon
:END:
Guix 环境检测、shepherd daemon 集成、外部工具加速范式、daemon 模式后台预热常用 feature。

+ =literal:in-guix-environment-p= :检测 Guix 环境
+ =exec-path= 在 early-init.el 已扩充(Guix profile bin + ~/.local/bin)
+ 所有外部命令路径启动期缓存(见 *启动与基础设施 → 路径常量与外部命令缓存* )
+ daemon 由 shepherd 的 =emacs-daemon= 服务管理( =emacs --fg-daemon= ),init.el 不重复启用 =server-mode= (仅 standalone 时启用)
+ daemon 模式下后台预热常用 feature(减少首帧延迟)

#+begin_src emacs-lisp
;; Guix 环境检测(启动期一次性 message)
(when literal:in-guix-environment-p
  (message "[literal-config] Guix 环境: %s" literal:guix-profile))

;; daemon 模式下后台预热常用 feature(减少首帧延迟)
;;
;; Phase 7.1 决策(保留):PLAN §7.1 原本建议"删 0.5s 全 require(与 defer
;; 矛盾)",但实测删除后首次 `emacsclient -c' 创建 frame 时,Vertico/Consult/
;; Corfu 等首次调用要现场 require,首帧延迟可感知(200-500ms,在繁忙系统
;; 上更明显)。保留 preload 的成本是 daemon 启动期多花 ~100ms 加载这些包,
;; 换来后续所有 emacsclient 的瞬时响应。
;;
;; 白名单维护原则:只列"用户首帧高概率用到 + autoload 链复杂"的包。
;; 简单包(如 which-key)即使延迟加载感知也不强,但既然也在白名单里,移除
;; 它的收益微乎其微,留着保持一致。
(when (literal/daemon-runtime-p)
  (run-with-idle-timer
   0.5 nil
   (lambda ()
     (dolist (feature '(savehist recentf bookmark consult embark
                                 embark-consult which-key helpful corfu
                                 corfu-popupinfo ef-themes))
       (unless (featurep feature)
         (condition-case err
             (require feature)
           (error
            (message "[daemon-preload] %s 加载失败: %s"
                     feature (error-message-string err)))))))))

(message "[literal-config] 启动完成")
#+end_src

** Dashboard
:PROPERTIES:
:CUSTOM_ID: dashboard
:END:
启动仪表盘:集成待办列表、文件管理器、前缀键概览和鼠标工作流提示。

Dashboard 依赖知识库、帮助、颜色方案和 frame 接口,因此固定在最后一个配置域加载。跨域调用保留 =fboundp= 守卫,但正常路径由文档顺序保证依赖已就绪。

+ Dashboard 调用的跨域函数:
  - =literal/knowledge-collect-org-files= / =literal/knowledge-open-file= (知识库卡片)
  - =literal/help--extract-dashboard-bindings= (快捷键卡片)
  - =literal/register-buffer-refresh!= (主题切换刷新)
  - =literal/add-frame-hook= (daemon/client frame 生命周期)

*** 状态与缓存
#+begin_src emacs-lisp :noweb-ref system/dashboard-core :tangle no


(require 'calendar)

(defface dashboard-card
  '((((background light)) :background "#f0f0f0")
    (((background dark))  :background "#1e2030"))
  "Dashboard 卡片背景。默认使用 light/dark 双套回退颜色，
主题加载后由 `literal/color-scheme-apply-custom-face-colors' 覆盖为 `bg-alt'。")

(defface dashboard-card-title
  '((t :inherit font-lock-keyword-face :weight bold))
  "Dashboard 卡片标题的 face。从 `font-lock-keyword-face' 继承强调色。")

(defface dashboard-todo-title
  '((t :inherit default))
  "Dashboard TODO 标题的 face。背景色由卡片行添加。")

(defconst literal:dashboard-card-inner-padding 2
  "Dashboard 卡片内容区左右统一保留的内边距列数。")

(defconst literal:dashboard-shortcuts-column-gap 4
  "Dashboard 快捷键双列之间的固定间隔列数。")

(defconst literal:dashboard-dual-gap 3
  "双列布局左右卡片之间的间隔列数。")

(defvar-local literal/dashboard--rendered-width nil
  "当前 dashboard buffer 最近一次渲染所使用的窗口宽度。")

(defvar literal/dashboard--refresh-in-progress nil
  "非 nil 表示当前正在刷新 dashboard，避免重复重入。")

(defvar literal/dashboard--cached-recentf-list nil
  "dashboard 渲染前保存的 recentf-list 快照，绕过渲染期间的临时清空。")

(defconst literal:dashboard-cache-ttl 60
  "Dashboard 数据源缓存的有效期（秒）。
超过此时间则视为过期，下次取用时重新计算。")

(defvar literal/dashboard--todo-cache nil
  "TODO 项缓存，结构为 (KEY TIME . DATA)，KEY 为 MAX-ITEMS。")

(defvar literal/dashboard--knowledge-cache nil
  "知识库条目缓存，结构为 (KEY TIME . DATA)，KEY 为 MAX-ITEMS。")

(defvar literal/dashboard--projects-cache nil
  "项目列表缓存，结构为 (KEY TIME . DATA)，KEY 恒为 nil（无查询参数）。")

(defun literal/dashboard--cache-valid-p (cache key)
  "判断 CACHE (KEY TIME . DATA) 是否对 KEY 在 TTL 内有效。"
  (and (consp cache)
       (eq (car cache) key)
       (let ((time (cadr cache)))
         (and time
              (<= (float-time (time-since time))
                  literal:dashboard-cache-ttl)))))

(defun literal/dashboard--cached (cache-var key compute-fn)
  "返回 CACHE-VAR 中 KEY 对应的缓存数据；失效则调用 COMPUTE-FN 重算并缓存。
CACHE-VAR 是缓存变量符号，缓存结构统一为 (KEY TIME . DATA)。
COMPUTE-FN 为无参函数或函数符号，返回要缓存的数据。"
  (let ((cache (symbol-value cache-var)))
    (if (literal/dashboard--cache-valid-p cache key)
        (cddr cache)
      (let ((data (funcall compute-fn)))
        (set cache-var (cons key (cons (current-time) data)))
        data))))

(defun literal/dashboard-invalidate-cache ()
  "清除 dashboard 所有数据源缓存。可在 Org 保存、Magit 操作等
改变底层数据后调用，确保下次刷新读取最新结果。"
  (setq literal/dashboard--todo-cache nil
        literal/dashboard--knowledge-cache nil
        literal/dashboard--projects-cache nil))

;; Phase 4.3:删除 literal/dashboard--snapshot-recentf + :before advice
;; (PLAN §4.3)。原绕过 dashboard-refresh-buffer 内部清空 recentf-list 的
;; workaround 由 dashboard-items 标准生成器接收 list-size 参数解决。

#+end_src

*** 卡片布局工具
#+begin_src emacs-lisp :noweb-ref system/dashboard-cards :tangle no
;; 卡片工具函数

(defvar literal/dashboard--forced-width nil
  "非 nil 时强制覆盖 card-width 计算，用于双列布局。")

(defun literal/dashboard--card-width (margin)
  "返回当前窗口下卡片的可用内容宽度。
如果 `literal/dashboard--forced-width' 非 nil，直接返回该值。"
  (or literal/dashboard--forced-width
      (max 1 (- (window-body-width) (* 2 margin)))))

(defun literal/dashboard--apply-fixed-pitch ()
  "强制 dashboard buffer 使用固定宽度的默认 face。"
  (setq-local face-remapping-alist
              (cons '(default fixed-pitch)
                    (assq-delete-all 'default face-remapping-alist)))
  (setq-local truncate-lines t))

(defun literal/dashboard--valid-color (val)
  "返回可用的颜色值 VAL；过滤 `face-foreground'/`face-background' 在 TTY
frame 下属性未指定时返回的 `unspecified-fg'/`unspecified-bg' 符号。
这些符号直接传给 `add-face-text-property' 的 :foreground/:background
会触发 \"Unable to load color\" 报错。"
  (and val
       (let ((s (if (symbolp val) (symbol-name val) val)))
         (and (not (string-prefix-p "unspecified" s))
              val))))

(defun literal/dashboard--card-row (content width margin &optional face)
  "插入 CONTENT 填充至 WIDTH 宽度的卡片行，背景使用 FACE。
MARGIN 两侧的空额无背景色。
CONTENT 可以是字符串或需要拼接的属性字符串/按钮列表。
超出 WIDTH 的内容用省略号截断。
当 FACE 不是 `dashboard-card' 时，同时将前景色应用到内容。"
  (let* ((parts (if (listp content) content (list content)))
         (content-str (apply #'concat parts))
         (content-width (string-width content-str))
         (face (or face 'dashboard-card))
         (bg (or (literal/dashboard--valid-color (face-background face)) 'unspecified))
         (fg (and (not (eq face 'dashboard-card))
                  (literal/dashboard--valid-color (face-foreground face nil t))))
         (margin-str (make-string margin ?\s)))
    ;; 超出宽度时截断并加 "..."，末尾精确保留 2 列呼吸空间
    (when (> content-width width)
      (setq content-str (truncate-string-to-width content-str (- width 2) 0 nil "...")
            content-width (string-width content-str)))
    (let ((pad-len (max 0 (- width content-width))))
      ;; 给内容补背景色；非默认 face 时同时叠加前景色
      (add-face-text-property
       0 (length content-str)
       (if fg
           (list :background bg :foreground fg)
         (list :background bg))
       t content-str)
      (insert margin-str
              content-str
              (propertize (make-string pad-len ?\s) 'face face)
              margin-str
              "\n"))))

(defun literal/dashboard--make-title-face ()
  "返回卡片标题的 face 符号。
动态设置 `dashboard-card-title' 背景以匹配 `dashboard-card'，
前景色继承 `font-lock-keyword-face' 以实现可靠的主题化。
`face-background' 在未设置时返回 nil，需要回退为 `unspecified'。"
  (let ((bg (or (literal/dashboard--valid-color (face-background 'dashboard-card)) 'unspecified)))
    (face-spec-set 'dashboard-card-title
                   `((t :inherit font-lock-keyword-face
                        :background ,bg
                        :weight bold))))
  'dashboard-card-title)

(defun literal/dashboard--compose-aligned-row (left right width)
  "Compose a WIDTH-wide card row with LEFT and RIGHT content.
Both sides keep `literal:dashboard-card-inner-padding' columns of inner padding."
  (let* ((left (or left ""))
         (right (or right ""))
         (inner-padding (make-string literal:dashboard-card-inner-padding ?\s))
         (gap-len (max 0 (- width
                            (* 2 literal:dashboard-card-inner-padding)
                            (string-width left)
                            (string-width right)))))
    (concat inner-padding left (make-string gap-len ?\s) right inner-padding)))

(defun literal/dashboard--format-shortcut-column (binding width)
  "Format a dashboard shortcut BINDING to fit within WIDTH columns."
  (if (not binding)
      (make-string width ?\s)
    (let* ((key (car binding))
           (desc (cdr binding))
           (key-gap "  ")
           (desc-width (max 0 (- width
                                 (string-width key)
                                 (string-width key-gap))))
           (display-desc (truncate-string-to-width desc desc-width 0 nil "..."))
           (pad-len (max 0 (- width
                              (string-width key)
                              (string-width key-gap)
                              (string-width display-desc)))))
      (concat (propertize key 'face 'font-lock-keyword-face)
              key-gap
              display-desc
              (make-string pad-len ?\s)))))

(defvar literal/dashboard--override-margin nil
  "非 nil 时强制覆盖 card-section 中的 margin 参数，用于双列等特殊布局。")

(defun literal/dashboard--card-section (title icon-key rows margin)
  "Insert a card section with TITLE line and ROWS.
TITLE is the section title string.
ICON-KEY is a cons (icon . key) where icon is a string and key is the shortcut hint.
ROWS is a list; each element is either a string or a list of string parts to concat.
MARGIN is the number of space columns on each side."
  (let* ((margin (or literal/dashboard--override-margin margin))
         (card-width (literal/dashboard--card-width margin))
         (icon (car icon-key))
         (key (cdr icon-key))
         (title-line (concat icon " " title))
         (title-content (literal/dashboard--compose-aligned-row
                         title-line key card-width)))
    ;; 顶部内边距
    (literal/dashboard--card-row "" card-width margin)
    ;; 标题行
    (literal/dashboard--card-row title-content card-width margin
                                 (literal/dashboard--make-title-face))
    ;; 标题与内容的分隔行
    (literal/dashboard--card-row "" card-width margin)
    ;; 数据行
    (dolist (row rows)
      (if (listp row)
          (literal/dashboard--card-row (cons "  " row) card-width margin)
        (literal/dashboard--card-row (concat "  " row) card-width margin)))
    ;; 底部内边距
    (literal/dashboard--card-row "" card-width margin)
    (insert "\n")))

(defun literal/dashboard--open-path (path &optional opener)
  "打开 PATH。OPENER 非 nil 时优先调用 OPENER。"
  (if opener
      (funcall opener path)
    (find-file path)))

(defun literal/dashboard--make-item-button (display path &optional opener)
  "返回一个带局部 keymap 的字符串，RET/mouse-1 时打开 PATH。
OPENER 非 nil 时作为打开函数，接收 PATH 一个参数。"
  (let ((button-str (copy-sequence display))
        (keymap (make-sparse-keymap)))
    (set-text-properties 0 (length button-str) nil button-str)
    (define-key keymap (kbd "RET")
                (lambda () (interactive)
                  (literal/dashboard--open-path path opener)))
    (define-key keymap [mouse-1]
                (lambda () (interactive)
                  (literal/dashboard--open-path path opener)))
    (add-text-properties
     0 (length button-str)
     (list 'keymap keymap
           'face 'dashboard-items-face
           'mouse-face 'highlight
           'help-echo (format "打开 %s" path))
     button-str)
    button-str))

(defun literal/dashboard--compact-path (path)
  "将 PATH 中间目录压缩为首字母，首尾保留完整。
隐藏目录（以 . 开头）取其第二个字符，避免 ~/./e 这种怪异的路径。"
  (let* ((parts (split-string path "/" t))
         (len (length parts)))
    (if (<= len 3)
        path
      (let* ((head (car parts))
             (file (car (last parts)))
             (middle (cl-subseq parts 1 (1- len)))
             (compressed (mapcar (lambda (d)
                                   (cond ((string-empty-p d) d)
                                         ((string-prefix-p "." d)
                                          (if (> (length d) 1)
                                              (concat "." (substring d 1 2))
                                            d))
                                         (t (substring d 0 1))))
                                 middle)))
        (concat head "/"
                (mapconcat #'identity compressed "/")
                "/" file)))))

(defun literal/dashboard--abbreviate-path (path max-width)
  "将 PATH 缩写以适应 MAX-WIDTH 列宽。
先压缩中间目录为首字母，仍超宽则从右侧截断并加 \"…\"。"
  (let* ((compact (literal/dashboard--compact-path path))
         (cw (string-width compact)))
    (if (<= cw max-width)
        compact
      (truncate-string-to-width compact max-width 0 nil "..."))))

(defun literal/dashboard--todo-state-face (todo-state)
  "返回 TODO-STATE 在 dashboard 待办卡片中使用的标签 face。"
  (pcase todo-state
    ("TODO" 'font-lock-warning-face)
    ("NEXT" 'font-lock-type-face)
    ("INPROGRESS" 'font-lock-constant-face)
    ("WAITING" 'font-lock-comment-face)
    ("DONE" 'success)
    ("CANCELLED" 'shadow)
    (_ 'font-lock-variable-name-face)))

(defun literal/dashboard--todo-title (title)
  "返回 dashboard 待办标题 TITLE。
标题显式使用普通前景色，只继承卡片背景；状态和日期各自独立上色。"
  (let ((fg (literal/dashboard--valid-color (face-foreground 'default nil t))))
    (face-spec-set 'dashboard-todo-title
                   `((t ,@(when fg (list :foreground fg))))))
  (propertize (or title "") 'face 'dashboard-todo-title))

(defun literal/dashboard--todo-date-info (today-abs deadline-str scheduled-str
                                                    deadline-abs scheduled-abs)
  "返回 TODO 项的日期信息字符串，用于 dashboard 待办卡片。"
  (cond
   ((and deadline-abs (< deadline-abs today-abs))
    (propertize (format "  󰥕 过期%d天" (- today-abs deadline-abs))
                'face 'error))
   ((and deadline-abs (= deadline-abs today-abs))
    (propertize "  󰥕 今天截止" 'face 'error))
   (deadline-str
    (let ((diff (- deadline-abs today-abs)))
      (propertize
       (cond ((= diff 1) "  󰥕 明天截止")
             ((<= diff 3) (format "  󰥕 %d天后截止" diff))
             ((<= diff 7) (format "  󰥕 %d天后截止" diff))
             (t (format "  󰥕 %s"
                        (substring deadline-str 1 11))))
       'face (cond ((<= diff 1) 'error)
                   ((<= diff 3) 'warning)
                   ((<= diff 7) 'font-lock-variable-name-face)
                   (t 'font-lock-keyword-face)))))
   ((and scheduled-abs (< scheduled-abs today-abs))
    (propertize (format "  󰔠 过期%d天" (- today-abs scheduled-abs))
                'face 'error))
   ((and scheduled-abs (= scheduled-abs today-abs))
    (propertize (format "  󰔠 %s" (substring scheduled-str 1 11))
                'face 'error))
   (scheduled-str
    (propertize (format "  󰔠 %s" (substring scheduled-str 1 11))
                'face 'font-lock-keyword-face))
   (t "")))

;; 双列布局工具函数

(defun literal/dashboard--call-with-card-width (card-width thunk)
  "以 CARD-WIDTH 覆盖卡片宽度后调用 THUNK，执行完恢复原值。"
  (let ((saved-width literal/dashboard--forced-width))
    (setq literal/dashboard--forced-width card-width)
    (unwind-protect (funcall thunk)
      (setq literal/dashboard--forced-width saved-width))))

(defun literal/dashboard--render-one-card (thunk card-width)
  "在临时 buffer 中以 CARD-WIDTH 渲染单个卡片，返回行列表。"
  (let ((it-lines nil)
        (it-buf (generate-new-buffer " *dual-card*")))
    (unwind-protect
        (progn
          (with-current-buffer it-buf
            (let ((literal/dashboard--forced-width card-width)
                  (literal/dashboard--override-margin 0))
              (funcall thunk))
            (goto-char (point-min))
            (while (not (eobp))
              (push (buffer-substring (line-beginning-position) (line-end-position))
                    it-lines)
              (forward-line 1))))
      (kill-buffer it-buf))
    (nreverse it-lines)))

(defun literal/dashboard--pad-to-width (str width face)
  "将 STR 填充至恰好 WIDTH 列，仅对填充部分应用 FACE。"
  (let* ((pad-len (max 0 (- width (string-width str)))))
    (if (> pad-len 0)
        (concat str (propertize (make-string pad-len ?\s) 'face face))
      str)))

(defun literal/dashboard--insert-card-pair (left-thunk right-thunk
                                                       left-width right-width
                                                       margin gap)
  "并排插入左右两张卡片。
每张卡片以零内边距渲染到临时 buffer，再按行交错拼接。
MARGIN 为页面左右外边距，GAP 为两卡片间距。"
  (let* ((literal/dashboard--override-margin 0)
         (left-lines (literal/dashboard--render-one-card left-thunk left-width))
         (right-lines (literal/dashboard--render-one-card right-thunk right-width))
         (max-lines (max (length left-lines) (length right-lines)))
         (margin-str (make-string margin ?\s))
         (gap-str (make-string gap ?\s)))
    (dotimes (i max-lines)
      (let ((left (if (< i (length left-lines)) (nth i left-lines) ""))
            (right (if (< i (length right-lines)) (nth i right-lines) "")))
        (insert margin-str
                (literal/dashboard--pad-to-width left left-width 'dashboard-card)
                gap-str
                (literal/dashboard--pad-to-width right right-width 'dashboard-card)
                margin-str
                "\n"))))
  (insert "\n"))

#+end_src

*** 内容数据源
#+begin_src emacs-lisp :noweb-ref system/dashboard-content :tangle no
;; 自定义 ASCII Banner（Doom 风格）

(defvar literal/dashboard-ascii-banner
  '(
    ""
    "███████╗ ██╗   ██╗ ███████╗ ██╗  ██████╗  ███╗   ██╗      ███████╗ ███╗   ███╗  █████╗   ██████╗ ███████╗"
    "██╔════╝ ██║   ██║ ██╔════╝ ██║ ██╔═══██╗ ████╗  ██║      ██╔════╝ ████╗ ████║ ██╔══██╗ ██╔════╝ ██╔════╝"
    "█████╗   ██║   ██║ ███████╗ ██║ ██║   ██║ ██╔██╗ ██║      █████╗   ██╔████╔██║ ███████║ ██║      ███████╗"
    "██╔══╝   ██║   ██║ ╚════██║ ██║ ██║   ██║ ██║╚██╗██║      ██╔══╝   ██║╚██╔╝██║ ██╔══██║ ██║      ╚════██║"
    "██║      ╚██████╔╝ ███████║ ██║ ╚██████╔╝ ██║ ╚████║      ███████╗ ██║ ╚═╝ ██║ ██║  ██║ ╚██████╗ ███████║"
    "╚═╝       ╚═════╝  ╚══════╝ ╚═╝  ╚═════╝  ╚═╝  ╚═══╝      ╚══════╝ ╚═╝     ╚═╝ ╚═╝  ╚═╝  ╚═════╝ ╚══════╝"
    ""
    "")
  "Doom 风格 ASCII art banner。")

(defun literal/dashboard-insert-ascii-banner ()
  "插入居中的自定义 ASCII banner，并在 banner 后插入 footer 引言。"
  (let* ((max-len (apply #'max (mapcar #'length literal/dashboard-ascii-banner)))
         (padding (max 0 (/ (- (window-width) max-len) 2)))
         (pad-str (make-string padding ?\s))
         (banner-face (if (facep 'dashboard-banner) 'dashboard-banner 'font-lock-keyword-face)))
    (insert "\n")
    (dolist (line literal/dashboard-ascii-banner)
      (insert pad-str (propertize line 'face banner-face) "\n"))
    ;; 在 banner 后立即插入 footer 引言
    (when (and (boundp 'dashboard-set-footer) dashboard-set-footer
               (boundp 'dashboard-footer) (stringp dashboard-footer))
      (let* ((footer-text dashboard-footer)
             (footer-pad (max 0 (/ (- (window-width) (string-width footer-text)) 2))))
        (insert "\n")
        (insert (make-string footer-pad ?\s)
                (propertize footer-text 'face 'dashboard-footer)
                "\n")))
    (insert "\n")))

;; 卡片风格列表显示

(defun literal/dashboard-insert-centered-list (title list list-size &optional icon key)
  "插入一个卡片区块，包含 TITLE，显示 LIST 中的条目。
LIST 中的每个元素是一个 (path . display-string) 构造。
ICON 是 Nerd Font 图标字符串。KEY 是快捷键提示字符串。"
  (when (car list)
    (let* ((card-available (- (literal/dashboard--card-width 4)
                              (* 2 literal:dashboard-card-inner-padding)))
           (items (seq-take list list-size))
           (rows (mapcar (lambda (el)
                           (let ((abbreviated
                                  (literal/dashboard--abbreviate-path
                                   (cdr el) card-available)))
                             (list (literal/dashboard--make-item-button
                                    abbreviated (car el)))))
                         items))
           (margin 4))
      (literal/dashboard--card-section title (cons (or icon "") key) rows margin))))

;; 自定义项目生成器

(defun literal/dashboard-insert-recents (list-size)
  "居中插入最近文件列表，无数据时显示 fallback 卡片。"
  (if (bound-and-true-p recentf-mode)
      (let ((items (or literal/dashboard--cached-recentf-list
                       (and (boundp 'recentf-list) recentf-list)
                       (default-value 'recentf-list))))
        (if items
            (literal/dashboard-insert-centered-list
             "最近文件"
             (mapcar (lambda (f) (cons f f))
                     items)
             list-size
             "󰋚" "C-x C-r")
          (literal/dashboard--card-section
           "最近文件" '("󰋚" . "C-x C-r") '(("暂无最近文件")) 4)))
    (literal/dashboard--card-section
     "最近文件" '("󰋚" . "C-x C-r") '(("recentf 未启用")) 4)))

(defun literal/dashboard--compute-known-projects ()
  "实际计算 dashboard 可展示的项目列表（无缓存）。
Phase 3 迁移自 Projectile:统一用 `project-known-project-roots'。"
  (delete-dups
   (seq-filter #'file-directory-p
               (mapcar #'file-name-as-directory
                       (project-known-project-roots)))))

(defun literal/dashboard--known-projects ()
  "返回 dashboard 可展示的项目列表。

优先读取 Projectile 的已知项目缓存（仅当 Projectile 已加载），
回落到 `project.el'。结果带 TTL 缓存（见 `literal:dashboard-cache-ttl'），
可通过 `literal/dashboard-invalidate-cache' 失效。"
  (literal/dashboard--cached
   'literal/dashboard--projects-cache
   nil
   #'literal/dashboard--compute-known-projects))

(defun literal/dashboard-insert-projects (list-size)
  "居中插入项目列表，无数据时显示 fallback 占位卡片。"
  (let ((projects (literal/dashboard--known-projects)))
    (if projects
        (literal/dashboard-insert-centered-list
         "项目"
         (mapcar (lambda (p) (cons p p))
                 projects)
         list-size
         "󰝰" "C-x p p")
      (literal/dashboard--card-section
       "项目" '("󰝰" . "C-x p p") '(("暂无已知项目（C-x p o 添加）")) 4))))

(defun literal/dashboard-insert-bookmarks (list-size)
  "居中插入书签列表，无数据时显示 fallback 占位卡片。"
  (require 'bookmark)
  (let ((bookmarks (bookmark-all-names)))
    (if bookmarks
        (literal/dashboard-insert-centered-list
         "书签"
         (mapcar (lambda (b)
                   (cons (bookmark-get-filename b) b))
                 bookmarks)
         list-size
         "󰉋" "C-x r b")
      (literal/dashboard--card-section
       "书签" '("󰉋" . "C-x r b") '(("暂无书签")) 4))))

(defun literal/dashboard--compute-todo-items (max-items)
  "实际扫描 `org-agenda-files' 计算未完成 TODO 项（无缓存）。
P0 #3 修复(Phase 1.3):原 `(when (featurep 'org) ...)' 在 fresh daemon
(org 未加载)下静默返回 nil,Dashboard 显示"暂无待办事项"假状态。改为
显式 (require 'org-agenda) + (require 'org)(轻量 autoload 触发),让
扫描在 fresh daemon 首次渲染时就拿到真实任务;org-agenda-files 真的为
空时才返回 nil(真实空,而非"未加载"假空)。"
  (require 'org-agenda)
  (require 'org)
  (let ((today-abs (calendar-absolute-from-gregorian (calendar-current-date)))
        (count 0)
        raw-items)
    (when (and (fboundp 'org-agenda-files)
               (ignore-errors (org-agenda-files)))
      (dolist (file (org-agenda-files))
        (when (and (< count (* max-items 2)) (file-exists-p file))
          (with-current-buffer (let ((ask-user-about-supersession-threat
                                      (lambda (&rest _) nil)))
                                 (find-file-noselect file))
            (org-with-wide-buffer
             (goto-char (point-min))
             (while (and (< count (* max-items 2))
                         (re-search-forward org-heading-regexp nil t))
               (let* ((components (org-heading-components))
                      (todo-state (nth 2 components))
                      (title (nth 4 components))
                      (scheduled (org-entry-get (point) "SCHEDULED"))
                      (deadline (org-entry-get (point) "DEADLINE")))
                 (when (and todo-state
                            (not (member todo-state '("DONE" "CANCELLED"))))
                   (let* ((deadline-abs (and deadline
                                             (org-time-string-to-absolute deadline)))
                          (scheduled-abs (and scheduled
                                              (org-time-string-to-absolute scheduled)))
                          (urgency
                           (cond
                            ((and deadline-abs (< deadline-abs today-abs)) 0)
                            ((and deadline-abs (= deadline-abs today-abs)) 1)
                            ((and scheduled-abs (< scheduled-abs today-abs)) 2)
                            (deadline-abs 3)
                            (scheduled-abs 4)
                            (t 5))))
                     (push (list urgency todo-state title deadline
                                 scheduled deadline-abs scheduled-abs)
                           raw-items)
                     (setq count (1+ count)))))))))))
    (setq raw-items
          (sort raw-items
                (lambda (a b)
                  (or (< (car a) (car b))
                      (and (= (car a) (car b))
                           (and (nth 5 a) (nth 5 b)
                                (< (nth 5 a) (nth 5 b))))))))
    (let ((result nil)
          (count 0))
      (dolist (item raw-items)
        (when (< count max-items)
          (let* ((todo-state (nth 1 item))
                 (title (nth 2 item))
                 (deadline-str (nth 3 item))
                 (scheduled-str (nth 4 item))
                 (deadline-abs (nth 5 item))
                 (scheduled-abs (nth 6 item))
                 (state-face (literal/dashboard--todo-state-face todo-state))
                 (date-info (literal/dashboard--todo-date-info
                             today-abs deadline-str scheduled-str
                             deadline-abs scheduled-abs)))
            (push (list (propertize (concat todo-state "  ") 'face state-face)
                        (literal/dashboard--todo-title title)
                        date-info)
                  result)
            (setq count (1+ count)))))
      (nreverse result))))

(defun literal/dashboard--todo-items (max-items)
  "返回所有未完成的 Org TODO 项作为带属性的字符串。
扫描 `org-agenda-files' 中状态为 TODO 的条目（TODO, NEXT, INPROGRESS, WAITING）。
按紧急度排序：过期 DEADLINE → 今日 DEADLINE → 未来 DEADLINE → SCHEDULED → 无日期。
MAX-ITEMS 限制返回的条目数量。
结果按 MAX-ITEMS 带 TTL 缓存（见 `literal:dashboard-cache-ttl'），
可通过 `literal/dashboard-invalidate-cache' 失效。"
  (literal/dashboard--cached
   'literal/dashboard--todo-cache
   max-items
   (lambda () (literal/dashboard--compute-todo-items max-items))))

(defun literal/dashboard--command-button (label command)
  "返回一个调用 COMMAND 的按钮字符串(Phase 2.2)。
用于 Dashboard 卡片底部"更多"跳转;RET/mouse-1 调用 COMMAND。"
  (let ((btn (copy-sequence label))
        (keymap (make-sparse-keymap)))
    (define-key keymap (kbd "RET")
                (lambda () (interactive) (call-interactively command)))
    (define-key keymap [mouse-1]
                (lambda () (interactive) (call-interactively command)))
    (add-text-properties
     0 (length btn)
     (list 'keymap keymap
           'face 'dashboard-items-face
           'mouse-face 'highlight
           'help-echo (format "运行 %s" command))
     btn)
    btn))

(defun literal/dashboard-insert-agenda (list-size)
  "将所有未完成的 TODO 项作为 dashboard 卡片插入。"
  (let ((items (literal/dashboard--todo-items list-size)))
    (if items
        (literal/dashboard--card-section
         "待办" '("󰃭" . "C-c o a")
         (append items
                 (list (list (literal/dashboard--command-button
                              "󰑃 查看 Agenda…"
                              #'org-agenda))))
         4)
      (literal/dashboard--card-section
       "待办" '("󰃭" . "C-c o a")
       (list (list (literal/dashboard--command-button
                    "󰑃 打开 Agenda"
                    #'org-agenda)))
       4))))

(defun literal/dashboard-insert-clock (_list-size)
  "将 Org 计时状态作为 dashboard 卡片插入。"
  ;; org 已 :defer t:org-clocking-p / org-clock-sum-today 虽 autoload,
  ;; 但调用会触发 org-clock 加载,抵消 defer。org 未加载时显示静态卡片。
  (if (not (featurep 'org))
      (literal/dashboard--card-section
       "计时" '("󱎫" . "C-c c i / o")
       '(("打开 org 文件后显示计时")) 4)
    (let* ((clocking-p (and (fboundp 'org-clocking-p) (org-clocking-p)))
           (today-min (if (fboundp 'org-clock-sum-today) (org-clock-sum-today) 0))
           (today-str (format "%dh %dm" (/ today-min 60) (% today-min 60)))
           (rows
            (if clocking-p
                (let* ((clock-string (substring-no-properties (org-clock-get-clock-string)))
                       (task-name (if (string-match org-ts-regexp-both clock-string)
                                      (string-trim (substring clock-string (match-end 0)))
                                    clock-string)))
                  (list (list (propertize "▶ 计时中: " 'face 'success)
                              task-name
                              "  " (propertize today-str 'face 'font-lock-type-face))
                        (list "今日累计: "
                              (propertize today-str 'face 'font-lock-keyword-face))))
              (list "当前没有计时的任务"
                    (list "今日累计: "
                          (propertize today-str 'face 'font-lock-comment-face))))))
      ;; Phase 2.2:计时中时跳转 org-clock-goto,否则跳转 org-clock-in。
      (let ((jump-cmd (if clocking-p #'org-clock-goto #'org-clock-in)))
        (literal/dashboard--card-section
         "计时" '("󱎫" . "C-c c i / o")
         (append rows
                 (list (list (literal/dashboard--command-button
                              (if clocking-p "󰑂 跳转到计时任务…"
                                "󰑂 开始计时…")
                              jump-cmd))))
         4)))))

(defun literal/dashboard--compute-recent-knowledge-entries (max-items)
  "消费 `agenote --domain human list --json' 稳定索引计算最近条目(无缓存)。
PLAN §2.3:不再递归扫描 experiences/ + 自行解析标题;改为消费 CLI 的稳定
JSON 索引。CLI 不可用时降级读 index.json(Phase 2.3 TODO:CLI 补 --json)。"
  (let ((cards (literal/knowledge-browse--load-cards 'human)))
    (cl-loop
     for card in (literal/agenote--sort-by-recency cards)
     for i below max-items
     for file = (or (plist-get card :file_path)
                    (plist-get card :path))
     for title = (or (plist-get card :title)
                     (and file (file-name-base file))
                     "(无标题)")
     when file
     collect (cons file title))))

(defun literal/dashboard--recent-knowledge-entries (max-items)
  "返回最近的知识库条目作为 (path . title) 对列表。
递归扫描 experiences/ 子目录。
结果按 MAX-ITEMS 带 TTL 缓存（见 `literal:dashboard-cache-ttl'），
可通过 `literal/dashboard-invalidate-cache' 失效。"
  (literal/dashboard--cached
   'literal/dashboard--knowledge-cache
   max-items
   (lambda () (literal/dashboard--compute-recent-knowledge-entries max-items))))

(defun literal/dashboard--org-file-title (file)
  "从 Org FILE 中提取 #+title。"
  (when (file-exists-p file)
    (with-temp-buffer
      (insert-file-contents file nil 0 2048)
      (goto-char (point-min))
      (when (re-search-forward "^#\\+title:[ \t]+\\(.+\\)$" nil t)
        (string-trim (match-string 1))))))

(defun literal/dashboard-insert-knowledge (list-size)
  "将最近的知识库条目作为 dashboard 卡片插入。"
  (let* ((entries (literal/dashboard--recent-knowledge-entries list-size))
         (rows (if entries
                   (mapcar (lambda (e)
                             (let* ((path (car e))
                                    (base (cdr e))
                                    (dir (file-name-directory path))
                                    (prefix (concat (file-name-base
                                                     (directory-file-name dir)) "/"))
                                    (display (concat prefix base)))
                               (list (literal/dashboard--make-item-button
                                      display path
                                      (or (when (fboundp 'literal/knowledge-open-file)
                                            #'literal/knowledge-open-file)
                                          #'find-file)))))
                           entries)
                 '(("知识库暂无条目")))))
    (literal/dashboard--card-section
     "知识库" '("󰧑" . "C-c o k I") rows 4)))

;; 快捷键帮助内容（动态提取）

(defun literal/dashboard--extract-top-level-bindings ()
  "提取 dashboard 需要展示的关键顶层快捷键。"
  (when (fboundp 'literal/help--extract-dashboard-bindings)
    (funcall #'literal/help--extract-dashboard-bindings)))

(defun literal/dashboard-insert-shortcuts (_list-size)
  "将键盘快捷键作为双列卡片插入。"
  (let* ((prefix-bindings (literal/dashboard--extract-top-level-bindings))
         (col-count 2)
         (margin 4)
         (card-width (literal/dashboard--card-width margin))
         ;; card-section 对字符串行追加 "  " 缩进，需预留 2 列
         (available-width (- card-width 2))
         (col-gap literal:dashboard-shortcuts-column-gap)
         (col-width (/ (max 1 (- available-width col-gap)) col-count))
         (total (length prefix-bindings))
         (rows-count (ceiling total (float col-count)))
         (row-lines nil))
    (dotimes (i rows-count)
      (let* ((left-idx i)
             (right-idx (+ i rows-count))
             (left (nth left-idx prefix-bindings))
             (right (when (< right-idx total) (nth right-idx prefix-bindings)))
             (left-col (literal/dashboard--format-shortcut-column left col-width))
             (right-col (literal/dashboard--format-shortcut-column right col-width)))
        (push (concat left-col
                      (make-string col-gap ?\s)
                      right-col)
              row-lines)))
    (setq row-lines (nreverse row-lines))
    (literal/dashboard--card-section
     "快捷键" '("󰌌" . "F1 ?") row-lines 4)))

;; 双列布局主生成器

(defconst literal:dashboard-dual-margin 4
  "双列布局左右页面边距列数。")

(defun literal/dashboard-insert-dual-layout (_list-size)
  "双列布局 dashboard 主生成器。
Banner 在上方全宽居中，中间卡片对并排，快捷键在下方全宽。"
  (let* ((total-width (window-body-width))
         (margin literal:dashboard-dual-margin)
         (gap literal:dashboard-dual-gap)
         (pair-available (- total-width (* 2 margin) gap))
         (left-card-width (/ pair-available 2))
         (right-card-width (- pair-available left-card-width))
         (full-card-width (- total-width (* 2 margin))))
    ;; Row 1: 待办 | 计时
    (literal/dashboard--insert-card-pair
     (lambda () (literal/dashboard-insert-agenda 5))
     (lambda () (literal/dashboard-insert-clock 1))
     left-card-width right-card-width margin gap)
    ;; Row 2: 最近文件 | 项目
    (literal/dashboard--insert-card-pair
     (lambda () (literal/dashboard-insert-recents 6))
     (lambda () (literal/dashboard-insert-projects 5))
     left-card-width right-card-width margin gap)
    ;; Row 3: 知识库 | 书签
    (literal/dashboard--insert-card-pair
     (lambda () (literal/dashboard-insert-knowledge 5))
     (lambda () (literal/dashboard-insert-bookmarks 4))
     left-card-width right-card-width margin gap)
    ;; Row 4: 快捷键（全宽单卡片）
    (literal/dashboard--call-with-card-width
     full-card-width
     (lambda () (literal/dashboard-insert-shortcuts 1)))))

#+end_src

*** Dashboard 配置与 Frame 生命周期
#+begin_src emacs-lisp :noweb-ref system/dashboard-lifecycle :tangle no
;; Dashboard 配置

(use-package dashboard
  :demand t
  :custom
  (dashboard-startup-banner nil)
  (dashboard-set-navigator t)
  (dashboard-items '((dual . 1)))
  (dashboard-set-heading-icons nil)   ; 由 literal/apply-frame-appearance 按 display 类型覆盖
  (dashboard-set-file-icons nil)      ; 由 literal/apply-frame-appearance 按 display 类型覆盖
  :config
  ;; Phase 4.3:删除全局 widget-button--check-and-call-button advice
  ;; (PLAN §4.3:该 advice 影响所有 non-dashboard widget buffer,副作用过大;
  ;; 原 wrong-type-argument 'overlayp 是 dashboard 包内部 bug,应在上游修复
  ;; 或局部 condition-case,不是全局 advice)。
  ;; Phase 4.3:删除 dashboard-refresh-buffer :before recentf snapshot advice
  ;; (与 literal/dashboard--snapshot-recentf 一同退役)。
  ;; 插入自定义居中 ASCII banner(内含 footer 引言)
  (advice-add 'dashboard-insert-banner :override #'literal/dashboard-insert-ascii-banner)
  ;; 抑制包原生的 footer 插入(已移至 banner 内部)
  (advice-add 'dashboard-insert-footer :override #'ignore)
  ;; dashboard 使用等宽字,避免 variable-pitch 造成标题和快捷键错位
  (add-hook 'dashboard-mode-hook #'literal/dashboard--apply-fixed-pitch)
  ;; dashboard 在不同宽度的 frame/window 中显示时需要按当前窗口重新渲染
  (add-hook 'window-size-change-functions #'literal/dashboard--maybe-refresh-visible)
  ;; 注册双列布局主生成器（统一渲染所有板块）
  (add-to-list 'dashboard-item-generators '(dual . literal/dashboard-insert-dual-layout))
  ;; 注册到主题切换刷新链：dashboard-mode buffer 在主题切换时主动 re-render，
  ;; 读取新的 face 值烘焙到文本 `:background' 属性。
  (when (fboundp 'literal/register-buffer-refresh!)
    (literal/register-buffer-refresh!
     'dashboard-mode #'dashboard-refresh-buffer))
  (dashboard-setup-startup-hook))

(defconst literal:dashboard-placeholder-buffer-names
  '(" *server*" "*scratch*" "*Messages*")
  "允许 dashboard 自动接管的 Emacs 初始占位 buffer 名称。")

(defun literal/dashboard--placeholder-buffer-p (buffer)
  "判断 BUFFER 是否仍是 client frame 的占位缓冲区。"
  (when (buffer-live-p buffer)
    (with-current-buffer buffer
      (and (not (minibufferp buffer))
           (not buffer-file-name)
           (or (member (buffer-name buffer)
                       literal:dashboard-placeholder-buffer-names)
               (and (eq major-mode 'fundamental-mode)
                    (string-prefix-p " " (buffer-name buffer))))))))

(defun literal/dashboard--server-edit-buffer-p (buffer)
  "判断 BUFFER 是否正在服务 emacsclient / with-editor 编辑请求。"
  (when (buffer-live-p buffer)
    (with-current-buffer buffer
      (or (bound-and-true-p server-buffer-clients)
          (bound-and-true-p with-editor-mode)
          (derived-mode-p 'git-commit-mode)))))

(defun literal/dashboard--single-window-frame-p (frame)
  "判断 FRAME 是否仍只有一个普通窗口。
Dashboard 只接管这种新 client frame 的空白启动状态；若 Magit、提交编辑器
或其他命令已经分裂窗口，则保留现有窗口配置。"
  (and (frame-live-p frame)
       (= (length (window-list frame 'no-minibuf)) 1)))

(defun literal/dashboard--refresh-buffer-in-window (window)
  "在 WINDOW 中按当前宽度重新渲染 dashboard。"
  (when (and (window-live-p window)
             (not literal/dashboard--refresh-in-progress))
    (let ((buffer (window-buffer window)))
      (when (buffer-live-p buffer)
        (with-current-buffer buffer
          (when (derived-mode-p 'dashboard-mode)
            (let ((literal/dashboard--refresh-in-progress t))
              (with-selected-window window
                (dashboard-refresh-buffer)
                (setq-local literal/dashboard--rendered-width
                            (window-body-width window))))))))))

(defconst literal:dashboard-refresh-debounce 0.3
  "window-size-change 触发 dashboard 重新渲染的 idle 延迟（秒）。
连续窗口尺寸变化会被合并为一次刷新。")

(defvar literal/dashboard--refresh-timer nil
  "dashboard 可见性刷新的 pending idle timer。")

(defun literal/dashboard--refresh-visible-now ()
  "立即检查所有可见 dashboard 窗口，宽度变化者按当前窗口重新渲染。"
  (when (timerp literal/dashboard--refresh-timer)
    (cancel-timer literal/dashboard--refresh-timer))
  (setq literal/dashboard--refresh-timer nil)
  (dolist (window (window-list nil 'no-minibuf))
    (let ((buffer (window-buffer window)))
      (when (buffer-live-p buffer)
        (with-current-buffer buffer
          (when (and (derived-mode-p 'dashboard-mode)
                     (/= (window-body-width window)
                         (or literal/dashboard--rendered-width -1)))
            (literal/dashboard--refresh-buffer-in-window window)))))))

(defun literal/dashboard--maybe-refresh-visible (_frame)
  "若可见 dashboard 的窗口宽度已变化，则延迟重新渲染（合并连续变化）。"
  (when (timerp literal/dashboard--refresh-timer)
    (cancel-timer literal/dashboard--refresh-timer))
  (setq literal/dashboard--refresh-timer
        (run-with-idle-timer literal:dashboard-refresh-debounce
                             nil
                             #'literal/dashboard--refresh-visible-now)))

(defun literal/dashboard--display-in-frame (frame)
  "在 FRAME 中显示并按需刷新 dashboard。"
  (when (and (frame-live-p frame)
             (literal/dashboard--single-window-frame-p frame))
    (with-selected-frame frame
      (when (fboundp 'dashboard-open)
        (dashboard-open)
        (when-let* ((dashboard-buffer (get-buffer "*dashboard*")))
          (when-let* ((window (frame-selected-window frame)))
            (set-window-buffer window dashboard-buffer)
            (literal/dashboard--refresh-buffer-in-window window)))))))

(defconst literal:dashboard-open-idle-delay 0.05
  "新建 client frame 后等待 dashboard 判定的 idle 秒数。")

(defconst literal:dashboard-open-retries 2
  "若首次判定过早，dashboard 允许追加重试的次数。")

(defun literal/dashboard--frame-startup-state (frame)
  "返回 FRAME 当前的启动缓冲区状态。"
  (cond
   ((not (literal/dashboard--single-window-frame-p frame)) 'busy)
   (t
    (if-let* ((window (and (frame-live-p frame)
                           (frame-selected-window frame)))
              (buffer (window-buffer window)))
        (with-current-buffer buffer
          (cond
           ((derived-mode-p 'dashboard-mode) 'dashboard)
           ((literal/dashboard--server-edit-buffer-p buffer) 'server-edit)
           ((buffer-file-name) 'file)
           ((literal/dashboard--placeholder-buffer-p buffer) 'placeholder)
           (t 'busy)))
      'pending))))

(defun literal/dashboard--run-open-check (frame retries-left)
  "根据 FRAME 当前状态决定是否显示或重试 dashboard。"
  (when (frame-live-p frame)
    (set-frame-parameter frame 'literal-dashboard-open-scheduled nil)
    (pcase (literal/dashboard--frame-startup-state frame)
      ('placeholder
       (literal/dashboard--display-in-frame frame))
      ('dashboard
       (when-let* ((window (frame-selected-window frame)))
         (literal/dashboard--refresh-buffer-in-window window)))
      ('file
       nil)
      ('server-edit
       nil)
      ('busy
       nil)
      (_
       (when (> retries-left 0)
         (literal/dashboard--schedule-open-for-frame
          frame
          (1- retries-left)))))))

(defun literal/dashboard--schedule-open-for-frame (frame retries-left)
  "为 FRAME 安排一次 dashboard 打开检查。RETRIES-LEFT 为剩余重试次数。"
  (when (and (frame-live-p frame)
             (not (frame-parameter frame 'literal-dashboard-open-scheduled)))
    ;; daemon 场景下 after-make-frame 与 server-after-make-frame 可能对同一 frame 连续触发。
    ;; 用 frame 参数避免并发重复调度，但在每轮检查结束后允许重新调度。
    (set-frame-parameter frame 'literal-dashboard-open-scheduled t)
    (run-with-idle-timer
     literal:dashboard-open-idle-delay nil
     (lambda (idle-frame idle-retries-left)
       (when (frame-live-p idle-frame)
         (literal/dashboard--run-open-check idle-frame idle-retries-left)))
     frame retries-left)))

(defun literal/dashboard-open-for-client-frame (&optional frame)
  "在 daemon 创建的 client FRAME 中打开 dashboard。
仅当该 frame 仍停留在占位缓冲区时接管，避免覆盖 `emacsclient FILE`。"
  (literal/dashboard--schedule-open-for-frame
   (or frame (selected-frame))
   literal:dashboard-open-retries))

(literal/add-frame-hook #'literal/dashboard-open-for-client-frame)
#+end_src

#+begin_src emacs-lisp
<<system/dashboard-core>>
<<system/dashboard-cards>>
<<system/dashboard-content>>
<<system/dashboard-lifecycle>>
#+end_src

** 实验性与待确认
:PROPERTIES:
:CUSTOM_ID: compatibility
:END:
*** 实验性
本节放置所有因为版本问题而添加的临时workaround。

以下 noweb 片段(均为 =:tangle no= )定义 Emacs 31 兼容性 shim,由使用方在文档更靠前的位置前向引用(=<<emacs31/...>>= )展开到 main.el。tangle 后代码插入到使用点,加载顺序不变;定义集中在此处是为了可发现性与未来上游修复后统一清理。

**** arei .elc 与 buffer-local-set-state 不兼容(bug#80812)
arei 0.9.7 的 .elc(Guix 用旧版 Emacs byte-compile)与 Emacs 31 的 =buffer-local-set-state= 宏不兼容:arei-mode 第 356 行调用该宏,但旧 .elc 把它当函数调用,参数 =(sesman-system 'Arei)= 被整体传给 =buffer-local-set-state--get= → =wrong-type-argument (symbolp 'Arei)= 。此错误在 =scheme-mode-hook= 里抛出,会中断整个 scheme-mode 初始化,导致 font-lock 失效 → Scheme 代码不染色。

#+begin_src emacs-lisp :noweb-ref emacs31/arei-compat :tangle no
;; arei 兼容性 shim:从 .el 源加载绕过坏 .elc + condition-case 守卫。
;; 被 *编程与开发 → 各语言 major mode* 的 arei use-package 前向引用。
(defun literal--load-arei-from-source ()
  "从 .el 源强制加载 arei,绕过与 Emacs 31 不兼容的 .elc。
GNU store 只读无法删 .elc,且 .el/.elc 时间戳相同(均为 epoch 1),
`load-prefer-newer' 无效,故需显式从 .el 加载。返回 t 若成功。"
  (condition-case err
      (let* ((found (locate-library "arei"))
             (arei-el (when found
                        (concat (file-name-sans-extension found) ".el"))))
        (when (and arei-el (file-exists-p arei-el))
          (load arei-el nil t t)
          t))
    (error
     (message "[literal] arei 从源加载失败: %S" err)
     nil)))

(defun literal--maybe-enable-arei-mode ()
  "在真实 Scheme 编辑 buffer 中启用 `arei-mode'(跳过 org src 临时 buffer)。

对 `arei-mode' 调用包裹 condition-case:minor-mode 的故障不应中断
`scheme-mode-hook' 的后续步骤(尤其 font-lock 初始化)。"
  (unless (string-prefix-p " *org-src-fontification:" (buffer-name))
    (condition-case err
        (arei-mode 1)
      (error
       (message "[literal] arei-mode 启用失败,Scheme 染色不受影响: %S" err)))))
#+end_src

**** vertico-buffer 形态降级(bug#80812,同根因)
vertico-buffer 形态在 Emacs 31 master + Guix Emacs 30 编译的 vertico elc 下报 =vertico-buffer--setup: Wrong type argument: symbolp= 。根因与 arei 相同:Emacs 31 重构了 =setq-local= (基于新 =set-local= 函数,bug#80812),而 vertico-buffer 内部用的 =buffer-local-set-state= 宏在 30 编译的 elc 里展开不兼容。上游追踪:minad/vertico#670。修复前所有原本想用 =buffer= 的形态临时降级为 =indexed= ;vertico 上游修复或 Guix 用 Emacs 31 重编译后,在 =keys/completion= 数据块的 =vertico-multiform-categories= 里把对应条目从 =indexed= 恢复为 =buffer= (如 =(consult-imenu indexed)= → =(consult-imenu buffer)= )。

#+begin_src emacs-lisp :noweb-ref emacs31/vertico-buffer-note :tangle no
;; FIXME(Emacs 31 兼容性): vertico-buffer 形态在 Emacs 31 master +
;; Guix Emacs 30 编译的 vertico elc 下报
;;   `vertico-buffer--setup: Wrong type argument: symbolp'
;; 因为 Emacs 31 重构了 setq-local（基于新 set-local 函数，bug#80812），
;; 而 vertico-buffer 内部用的 buffer-local-set-state 宏在 30 编译的 elc
;; 里展开不兼容。上游追踪：minad/vertico#670。修复前所有原本想用 buffer
;; 的形态临时降级为 indexed。vertico 上游修复或 Guix 用 Emacs 31 重编译后
;; 可恢复 buffer 形态。
#+end_src

**** byte-compile-root-dir(elfeed-3.4.2 tangle 崩溃)
=tangle= 阶段 =elfeed-link.el= 注册的 =elfeed= org link 被 =org-babel-tangle--unbracketed-link= 经 =org-store-link= 试探,触发 autoload → =require 'elfeed-show= → =require 'elfeed= 。elfeed.el 内 =(cl-eval-when (load eval) (unless byte-compile-root-dir ...))= 这段 byte-code 假设变量已 bound,但 elfeed-3.4.2 不可绕开,Emacs 31 下报 void-variable。

+ *shim 位置* :: =init.el= (非 emacs.org tangle 产物;bootstrap 阶段 =org-babel-tangle-file= 之前 =(defvar byte-compile-root-dir nil)= )
+ *根因* :: Emacs 31 仍 defvar =byte-compile-root-dir= (见 =bytecomp.el:1232= ),但 bytecomp.el 是 lazy-load,默认不在初始环境
+ *根治* :: 升级到 elfeed-4.0.1

* init.el ends here

#+begin_src emacs-lisp
(provide 'main)
;;; main.el ends here
#+end_src
