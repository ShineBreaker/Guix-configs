#!/usr/bin/env bash
set -euo pipefail

resolve_script_path() {
  local source="${BASH_SOURCE[0]}"
  while [[ -L "${source}" ]]; do
    local dir
    dir="$(cd -P "$(dirname "${source}")" && pwd)"
    source="$(readlink "${source}")"
    [[ "${source}" != /* ]] && source="${dir}/${source}"
  done
  cd -P "$(dirname "${source}")" && pwd
}

ensure_installed() {
  local xdg_data_home="${XDG_DATA_HOME:-${HOME}/.local/share}"
  local pi_root="${PI_LOCAL_ROOT:-${xdg_data_home}/pi}"

  if [[ -d "${pi_root}/agent" && -x "${pi_root}/node_modules/.bin/pi" ]]; then
    cd "${pi_root}" && pwd
    return 0
  fi

  if ! command -v bun &>/dev/null; then
    echo "bun is not installed. Please install bun first." >&2
    return 1
  fi

  if [[ ! -d "${pi_root}" ]]; then
    echo "Pi local root not found at ${pi_root}." >&2
    return 1
  fi

  echo "Installing Pi dependencies..." >&2
  cd "${pi_root}" && bun install

  local npm_dir="${pi_root}/agent/npm"
  if [[ -f "${npm_dir}/package.json" ]]; then
    echo "Installing agent plugins..." >&2
    cd "${npm_dir}" && bun install
  fi

  cd "${pi_root}" && pwd
}

SCRIPT_DIR="$(resolve_script_path)"
ROOT_DIR="$(ensure_installed)"

cd "${ROOT_DIR}"

echo "=========================================="
echo "Pi Agent 更新"
echo "位置: ${ROOT_DIR}"
echo "=========================================="

echo ""
echo "[1/3] 当前版本:"
"${ROOT_DIR}/node_modules/.bin/pi" --version 2>/dev/null || echo "  无法获取版本号"

echo ""
echo "[2/3] 更新依赖包..."
deps=$(node -e "console.log(Object.keys(require('./package.json').dependencies).join(' '))")
for dep in ${deps}; do
  echo "  更新 ${dep}..."
  bun install "${dep}@latest"
done

echo ""
echo "[3/3] 验证更新:"
echo "  pi 版本: $("${ROOT_DIR}/node_modules/.bin/pi" --version 2>/dev/null || echo '未知')"

echo ""
echo "Agent 目录状态:"
echo "  扩展: $(ls -1 "${ROOT_DIR}/agent/extensions/" 2>/dev/null | wc -l) 个"
echo "  技能: $(ls -1 "${ROOT_DIR}/agent/skills/" 2>/dev/null | wc -l) 个"
echo "  子代理: $(ls -1 "${ROOT_DIR}/agent/agents/" 2>/dev/null | wc -l) 个"

echo ""
echo "=========================================="
echo "更新完成"
echo "=========================================="
