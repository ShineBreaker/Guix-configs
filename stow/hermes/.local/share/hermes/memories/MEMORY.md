[hermes 部署拓扑] hermes-agent 通过Guix-configs 内部的 Nix 配置部署:$HERMES_HOME=$HOME/.local/share/hermes(非 ~/.hermes/),Nix flake 在 source/nix/flake.nix,模块在 source/nix/configuration/programs/hermes.nix,$HERMES_HOME/config.yaml 是软链 → Guix-configs/stow/hermes/。
§
[age 加密入仓 (Guix-configs)] 私钥 stow/secrets/.keys/age (mode 600, 走 stow 软链到 ~/.keys/age, 不进 git) | 公钥 dotfiles/secrets/.keys/age.pub (进 git) | 密文 dotfiles/secrets/<name>.age (进 git; 平行于 enable/, **不**进 dotfile-services, 否则会被 blue home 部署到 ~/.config/secrets/ 暴露) | 明文 ~/.local/share/secrets-decrypted/<name> (不进 git) | .gitignore: .keys/ 通配排除 + !dotfiles/secrets/.keys/*.pub 放行公钥 (双向 git check-ignore -v 验证) | source/config.org user-packages-list 加 "age" (gnu/packages/golang-crypto.scm) | helper tools/secrets (encrypt/decrypt/edit/show/list/recipients). 坑: (a) set -euo pipefail + 多 cmd_* 函数下变量拼写错只在该路径炸 (cmd_list 内 $DECREPT_DIR 漏 Y, encrypt/decrypt 不触发); (b) bash -n 只查语法不查变量绑定, 必须真跑每个子命令; (c) guix time-machine 首次 >90s timeout, 已构建的 /gnu/store/<hash>-profile/bin/age 可直接用. 硬约束: dotfiles/secrets/ 绝不进 dotfile-services packages; 解密明文绝不放回 ~/.config/<app>/. §10 patch 已为 guix-configs-workflow 准备好, 等用户手动落盘到 stow 源.
§
[~/.config/agents/skills stow 软链 skill 都是 read-only] 改不了 stow 外部 skill (agenote-\*、guix-configs-workflow 等), skill_manage 报 "skill lives in skills.external_dirs, read-only to autonomous curation". 唯一路径: 把新内容 / patch 贴出来让 user 手动落盘到 ~/Projects/Config/Guix-configs/stow/<pkg>/ 源. 配套 reminder: 那些 stow 软链 skill 也不能写新版到 ~/.local/share/hermes/skills/ (那是 hermes bundled).