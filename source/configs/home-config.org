
我会尽量地把更多的配置放在这里，而 =system-config.md=
只会放置一些保障系统正常使用所必须的配置，这样的好处在于:

1. 在首次安装系统的时候，可以少安装很多很多的软件包，这样安装系统的时长不会说长的太过分，也可以尽量减少因为网络问题而造成的报错
2. 系统和用户分开配置，这样在修改某一块的配置时不用将一整个系统都 reconfighration 一遍，方便多次迭代

--------------

* Main

在这里，我利用了 =Org Mode= 的 =Noweb= 功能
这个功能允许我将代码块命名，然后利用命名来进行引用，将所有引用都放到到下面这个代码块中再自动拼合并输出
引用的语法为 *<<>>* , 所以你看到类似的东西时不要困惑，这不是 scheme 原生的功能，是 orgmode 的相关功能

#+NAME: main
#+begin_src scheme :tangle ../../tmp/home-config.scm :noweb yes
(load "../source/information.scm")

<<modules>>

<<services-modules>>

(home-environment
 (packages (append
            <<desktop-packages-list>>
            <<emacs-packages-list>>
            <<terminal-packages-list>>
            <<devtools-packages-list>>
            <<user-packages-list>>
            ))

 (services (append
            <<user-desktop-services>>
            <<home-shepherd-services>>
            <<timer-services>>
            <<fish-services>>
            <<modprobe-services>>
            <<dotfile-services>>
            <<home-files-services>>
            <<environment-variable-services>>
            <<font-services>>
            %rosenthal-desktop-home-services)))
#+end_src

* Modules

这些以及下面 =service= 块中的 module 都是为了导入对应变量而使用的，所以必须放置在一个配置文件的最前面

#+NAME: modules
#+begin_src scheme
(use-modules (gnu)
             (gnu home)
             (gnu home services)

             (gnu packages)
             (gnu services)
             (gnu system shadow)

             (guix gexp)
             (guix packages)
             (guix utils)

             (jeans packages games)

             (rosenthal utils file)
             (rosenthal utils packages))
#+end_src

* Packages
** 桌面环境 相关软件包

#+NAME: desktop-packages-list
#+begin_src scheme
(cons* (specs->pkgs+out
        ;; 桌面组件
        "fuzzel"
        "swaylock-effects"
        "swayidle"
        "swww"
        "waypaper"

        ;; 输入法
        "fcitx5"
        "fcitx5-lua"

        ;; 桌面服务
        "cliphist"
        "dex"
        "libnotify"
        "setxkbmap"
        "wl-clipboard"
        "xprop"

        ;; XDG 相关组件
        "nautilus" ; portal-gnome 依赖这个正常工作
        "xdg-dbus-proxy"
        "xdg-desktop-portal-gnome"
        "xdg-desktop-portal-gtk"
        "xdg-user-dirs"
        "xdg-utils"))
#+end_src

** Emacs 相关软件包

#+NAME: emacs-packages-list
#+begin_src scheme
(cons* (specs->pkgs+out
        ;; Emacs 核心与基础
        "emacs-gcmh"
        "emacs-general"
        "emacs-no-littering"
        "emacs-pgtk"
        "emacs-spinner"
        "emacs-use-package"

        ;; 补全与迷你缓冲区
        "emacs-consult"
        "emacs-corfu"
        "emacs-embark"
        "emacs-marginalia"
        "emacs-orderless"
        "emacs-vertico"

        ;; Evil 模式（Vim 模拟）
        "emacs-evil"
        "emacs-evil-collection"

        ;; 界面与外观
        "emacs-dashboard"
        "emacs-doom-modeline"
        "emacs-ef-themes"
        "emacs-kind-icon"
        "emacs-minimap"
        "emacs-nerd-icons"
        "emacs-rainbow-delimiters"
        "emacs-treemacs"
        "emacs-treemacs-nerd-icons"
        "emacs-which-key"

        ;; 编辑与导航
        "emacs-ace-jump-mode"
        "emacs-diff-hl"
        "emacs-expand-region"
        "emacs-multiple-cursors"
        "emacs-origami"
        "emacs-ws-butler"

        ;; 开发工具
        "emacs-eldoc-box"
        "emacs-rg"
        "emacs-stickyfunc-enhance"
        "emacs-vterm"
        "emacs-yasnippet"
        "emacs-yasnippet-snippets"

        ;; 编程语言支持
        "emacs-geiser"
        "emacs-geiser-guile"
        "emacs-json-mode"
        "emacs-kotlin-mode"
        "emacs-markdown-mode"
        "emacs-rust-mode"
        "emacs-sly"
        "emacs-typescript-mode"
        "emacs-web-mode"
        "emacs-yaml"
        "emacs-yaml-mode"
        "emacs-zig-mode"

        ;; Git 集成
        "emacs-git-messenger"
        "emacs-magit"
        "emacs-magit-todos"

        ;; 项目管理
        "emacs-projectile"

        ;; Org Mode 生态
        "emacs-htmlize"
        "emacs-org"
        "emacs-org-appear"
        "emacs-org-modern"
        "emacs-org-roam"
        "emacs-ox-gfm"

        ;; 帮助与文档
        "emacs-helpful"

        ;; 邮件与日历
        "emacs-calfw"
        "emacs-notmuch"

        ;; AI 工具
        "emacs-gptel"

        ;; 编程语言运行时与工具
        "graphviz"
        "plantuml"
        "sbcl"
        "texlive-scheme-full"

        ;; Tree-sitter（语法解析）
        "tree-sitter"
        "tree-sitter-bash"
        "tree-sitter-c"
        "tree-sitter-cpp"
        "tree-sitter-css"
        "tree-sitter-dockerfile"
        "tree-sitter-go"
        "tree-sitter-html"
        "tree-sitter-javascript"
        "tree-sitter-json"
        "tree-sitter-python"
        "tree-sitter-rust"
        "tree-sitter-typescript"))
#+end_src

** 终端 相关软件包

#+NAME: terminal-packages-list
#+begin_src scheme
(cons* (specs->pkgs+out
        ;; 工具
        "atuin"
        "broot"
        "btop"

        ;; 运行器
        "just"
        "maak"

        ;; Tmux 相关
        "tmux"
        "tmuxifier"
        "tmux-xpanes"

        ;; 基于 Rust 的命令行工具替代品
        "bat"
        "fd"
        "ripgrep"
        "fzf"
        "lolcat"
        "zoxide"))
#+end_src

** 开发环境 相关软件包

#+NAME: devtools-packages-list
#+begin_src scheme
(cons* (specs->pkgs+out
        ;; 开发环境
        "ccls"
        "clang"
        "gradle"
        "helix"
        "maven"
        "node"
        "openjdk@25"
        "pnpm@10"
        "python-black"
        "python-jsbeautifier"
        "python-lsp-black"
        "python-lsp-server"
        "python-pylsp-mypy"
        "rust-analyzer"

        ;; 工具
        "bun"
        "direnv"
        "gitui"
        "pandoc"
        "reuse"
        "sdkmanager"
        "uv"))
        #+end_src
** 其他软件包

#+NAME: user-packages-list
#+begin_src scheme
(cons* (specs->pkgs+out
        ;; AI工具
        "claude-code"
        "codex"

        ;; 影音
        "easyeffects"
        "helvum"
        "mpv"
        "nomacs"
        "obs-with-cef"
        "obs-pipewire-audio-capture"
        "obs-vkcapture"

        ;; 社交
        "notmuch"
        "telegram-desktop"
        "zulip-term"

        ;; 生产力工具
        "gimp"
        "keepassxc"
        "git-credential-keepassxc"
        "kdeconnect"
        "libreoffice"
        "seahorse"
        "virt-manager"
        "zen-browser-bin"

        ;; 娱乐
        "heroic"
        "mangohud"
        "osu-lazer-bin"
        "prismlauncher-dolly"
        "steam"

        ;; 实用工具
        "amule"
        "freerdp@3"
        "kanata"
        "postgresql"
        "winapps"

        ;; 主题
        "orchis-kde-themes"
        "orchis-theme"
        "adwaita-icon-theme"
        "bibata-cursor-theme"
        "papirus-icon-theme"

        ;; QT框架相关
        "kvantum"
        "qt5ct"
        "qt6ct"))
#+end_src

* Services

#+NAME: services-modules
#+begin_src scheme
(use-modules (gnu home services desktop)
             (gnu home services dotfiles)
             (gnu home services fontutils)
             (gnu home services gnupg)
             (gnu home services shells)
             (gnu home services shepherd)
             (gnu home services niri)
             (gnu home services sound)
             (gnu home services desktop)
             (gnu home services fontutils)
             (gnu home services syncthing)
             (gnu home services guix)


             (rosenthal services desktop)
             (rosenthal services shellutils))

(use-package-modules java shells)

(use-package-modules fcitx5
                     freedesktop
                     gnupg
                     kde-internet
                     linux
                     polkit
                     rust-apps
                     wm)
#+end_src

** modprobe 相关服务

#+NAME: modprobe-services
#+begin_src scheme
(list ;modprobed-db 配置文件
 (simple-service 'modprobed-db-config home-files-service-type
                 `((".config/modprobed-db/modprobed-db.conf" ,(plain-file
                                                               "modprobed-db.conf"
                                                               "IGNORE=(hid-uclogic wacom)"))))

 ;; modprobed-db 激活时运行（一次性初始化）
 (simple-service 'modprobed-db-activation home-activation-service-type
                 #~(begin
                     (use-modules (guix build utils))
                     ;; 确保配置目录存在
                     (mkdir-p (string-append (getenv "HOME")
                                             "/.config/modprobed-db"))
                     ;; 运行 modprobed-db store 记录当前模块
                     (system* #$(file-append modprobed-db
                                             "/bin/modprobed-db")
                              "storesilent"))))
#+end_src

** Fish 相关服务

#+NAME: fish-services
#+begin_src scheme :noweb yes
(list (simple-service 'fish-configs
                      home-xdg-configuration-files-service-type
                      (list `("fish/conf.d/10-source.fish" ,(computed-substitution-with-inputs
                                                             "config.fish"
                                                             (plain-file
                                                              "10-source.fish"
                                                              "
<<fish-cfg>>
")
                                                             (specs->pkgs
                                                              "atuin"
                                                              "direnv" "fzf"
                                                              "zoxide")))))

      (simple-service 'fish-functions
                      home-xdg-configuration-files-service-type
                      (list `("fish/functions/fenv.main.fish" ,(file-append
                                                                fish-foreign-env
                                                                "/share/fish/functions/fenv.main.fish"))
                            `("fish/functions/fenv.fish" ,(file-append
                                                           fish-foreign-env
                                                           "/share/fish/functions/fenv.fish")))))
#+end_src

#+NAME: fish-cfg
#+begin_src fish
status is-interactive
and begin

  $$bin/atuin$$ init fish | source
  $$bin/direnv$$ hook fish | source
  $$bin/fzf$$ --fish | source
  $$bin/zoxide$$ init fish | source

end
#+end_src

** 桌面常用软件的相关服务

#+NAME: user-desktop-services
#+begin_src scheme
(list (service home-mako-service-type)
      (service home-syncthing-service-type)

      (service home-fcitx5-service-type
               (home-fcitx5-configuration (themes (list fcitx5-material-color-theme))
                                          (input-method-editors (list fcitx5-rime))
                                          (utilities (list fcitx5-configtool fcitx5-lua))
                                          (gtk-im-module? #t)
                                          (qt-im-module? #t))))
#+end_src

** 软件自启动

这里利用 *scheme* 的 =lambda=
函数来包装了一个列表，这样的操作在我的配置文件里面多次出现，这样可以不用重复写很大一段内容
这也正是 Guix 的优势所在: 可以利用 Scheme 强大的生态来进行各种操作

#+NAME: home-shepherd-services
#+begin_src scheme
(let ((make-shepherd-service
       (lambda (pkg path requirement)
         (shepherd-service (provision (list (string->symbol (package-name pkg))))
                           (requirement requirement)
                           (start #~(make-forkexec-constructor
                                     (list #$(file-append pkg path))
                                     #:log-file (string-append (getenv "HOME") "/.var/log/"
                                                               #$(package-name pkg) ".log")))
                           (respawn? #t)))))
  (list (simple-service 'essential-desktop-services home-shepherd-service-type
                        (list (make-shepherd-service kanata "/bin/kanata" '(dbus))
                              (make-shepherd-service kdeconnect "/bin/kdeconnectd" '(dbus))
                              (make-shepherd-service polkit-gnome "/libexec/polkit-gnome-authentication-agent-1" '(dbus))
                              (make-shepherd-service poweralertd "/bin/poweralertd" '(dbus))
                              (make-shepherd-service swayidle "/bin/swayidle" '(dbus))
                              (make-shepherd-service waybar "/bin/waybar" '(dbus))
                              (make-shepherd-service xdg-desktop-portal "/libexec/xdg-desktop-portal" '(dbus))
                              (make-shepherd-service xdg-desktop-portal-gtk "/libexec/xdg-desktop-portal-gtk" '(xdg-desktop-portal))))))
#+end_src

** 计时器服务

#+NAME: timer-services
#+begin_src scheme
(list (simple-service 'auto-update home-shepherd-service-type
                      (list
                       ;; 用于自动更新flatpak中的软件包
                       (shepherd-timer '(flatpak-update)
                                       #~(calendar-event #:hours '(18)
                                                         #:minutes '(0))
                                       #~("/run/current-system/profile/bin/flatpak"
                                          "upgrade" "-y")
                                       #:requirement '(dbus)))))
#+end_src

** dotfiles 管理相关服务

*** 主要的一些内容

#+NAME: dotfile-services
#+begin_src scheme
(list (service home-dotfiles-service-type
             (home-dotfiles-configuration
               (directories '("../dotfiles"))
               (layout 'stow)
               (packages (list "autostart"
                               "desktop"
                               "fcitx5"
                               "termide"
                               "terminal"
                               "themes"
                               "tools"))
               (excluded '(".*~"
                           ".*\\.swp"
                           "/\\.git/.*"
                           "\\.gitignore"
                           "\\.github"
                           ".*\\.md"))))

      (service home-xdg-configuration-files-service-type
               `(("gdb/gdbinit" ,%default-gdbinit)
                 ("nano/nanorc" ,%default-nanorc)))

      (simple-service 'prism-jdks home-files-service-type
                      (map (lambda (jdk)
                             (list (in-vicinity
                                    ".local/share/PrismLauncher/java/"
                                    (package-version jdk)) jdk))
                           (list openjdk25 openjdk21 openjdk17))))
#+end_src

*** 区别对待某些配置文件

有一些软件会需要在其配置文件内写入某个二进制的具体位置，此时就可以使用由
*rosenthal* 提供的 =computed-substitution-with-inputs= 函数

用法为：

1. 在需要的配置文件内以 =&&bin/foo&&= 的形式来写你所需要写入的二进制路径
2. 在 =specs->pkgs= 参数中写入能够提供这些二进制的软件包

#+NAME: home-files-services
#+begin_src scheme
(list (service home-files-service-type
               `((".guile" ,%default-dotguile)
                 (".Xdefaults" ,%default-xdefaults)
                 (".config/git-credential-keepassxc" ,(computed-substitution-with-inputs
                                                       "git-credential-keepassxc"
                                                       (local-file
                                                        "../source/files/git-credential-keepassxc")
                                                       (specs->pkgs "git"
                                                                    "fish")))))

      (service home-niri-service-type
               (home-niri-configuration (config (computed-substitution-with-inputs
                                                 "config.kdl"
                                                 (local-file
                                                  "../source/files/niri.kdl")
                                                 (specs->pkgs
                                                  "brightnessctl"
                                                  "cliphist"
                                                  "dex"
                                                  "foot"
                                                  "fish"
                                                  "fuzzel"
                                                  "niri"
                                                  "swaylock-effects"
                                                  "waypaper"
                                                  "wl-clipboard"
                                                  "xwayland-satellite"))))))
#+end_src

** 环境变量

#+NAME: 环境变量

*** 软件相关的环境变量

#+NAME: extend-environment-variables
#+begin_src scheme
'(("ANDROID_HOME" . "$HOME/Programs/Android/SDK")
  ("EDITOR" . "hx")
  ("FREERDP_ASKPASS" . "1")
  ("GUIX_PROFILE" . "$HOME/.guix-profile")
  ("GUIX_SANDBOX_HOME" . "$XDG_DATA_HOME/Sandbox")
  ("HTTP_PROXY" . "http://127.0.0.1:7890")
  ("http_proxy" . "$HTTP_PROXY")
  ("HTTPS_PROXY" . "$HTTP_PROXY")
  ("https_proxy" . "$HTTP_PROXY")
  ("LIBVIRT_DEFAULT_URI" . "qemu:///system")
  ("no_proxy" . "127.0.0.1,localhost")
  ("NO_PROXY" . "127.0.0.1,localhost")
  ("QS_ICON_THEME" . "Papirus-Dark")
  ("QT_QPA_PLATFORMTHEME" . "qt5ct")

  ;; Wayland support.
  ("GDK_BACKEND" . "wayland")
  ("_JAVA_AWT_WM_NONREPARENTING" . "1")
  ("MOZ_ENABLE_WAYLAND" . "1")
  ("QT_AUTO_SCREEN_SCALE_FACTOR" . "1"))
#+end_src

*** 为了让软件尽量遵守XDG规范而提供的变量

#+NAME: xdg-basedir-env-vars
#+begin_src scheme
'(("HISTFILE" . "$XDG_STATE_HOME/bash/history")
  ;; docker
  ("DOCKER_CONFIG" . "$XDG_CONFIG_HOME/docker")
  ;; gdb
  ("GDBHISTFILE" . "$XDG_STATE_HOME/gdb/history")
  ;; gnupg
  ("GNUPGHOME" . "$XDG_DATA_HOME/gnupg")
  ;; go
  ("GOMODCACHE" . "$XDG_CACHE_HOME/go/mod")
  ("GOPATH" . "$XDG_DATA_HOME/go")
  ;; gradle
  ("GRADLE_USER_HOME" . "$XDG_DATA_HOME/gradle")
  ;; guile
  ("GUILE_HISTORY" . "$XDG_STATE_HOME/guile/history")
  ;; luanti
  ("MINETEST_USER_PATH" . "$XDG_DATA_HOME/luanti")
  ;; node
  ("NPM_CONFIG_USERCONFIG" . "$XDG_CONFIG_HOME/npm/npmrc")
  ;; nvidia-driver
  ("CUDA_CACHE_PATH" . "$XDG_CACHE_HOME/nv")
  ;; password-store
  ("PASSWORD_STORE_DIR" . "$XDG_DATA_HOME/pass")
  ;; python
  ("PYTHON_HISTORY" . "$XDG_STATE_HOME/python/history")
  ;; rust
  ("CARGO_HOME" . "$XDG_DATA_HOME/cargo")
  ;; sqlite
  ("SQLITE_HISTORY" . "$XDG_STATE_HOME/sqlite_history")
  ;; tmuxifier
  ("TMUXIFIER_LAYOUT_PATH" . "$XDG_CONFIG_HOME/tmuxifier/layouts"))
#+end_src

*** 引用以上的所有变量，并在这里放一些多行的环境变量

#+NAME: environment-variable-services
#+begin_src scheme :noweb yes
(list (simple-service 'environment-variables
                      home-environment-variables-service-type
                      (append
                       <<extend-environment-variables>>
                       <<xdg-basedir-env-vars>>
                       `(("QT_PLUGIN_PATH" unquote
                          (string-append
                           "/run/current-system/profile/lib/qt5/plugins:"
                           "/run/current-system/profile/lib/qt6/plugins:"
                           "$HOME/.guix-home/profile/lib/qt5/plugins:"
                           "$HOME/.guix-home/profile/lib/qt6/plugins"))
                         ("CHROMIUM_FLAGS" unquote
                          (string-append
                           "--enable-features=UseOzonePlatform,WaylandWindowDecorations "
                           "--ozone-platform-hint=wayland "
                           "--enable-wayland-ime "
                           "--wayland-text-input-version=3"))
                         ("_JAVA_OPTIONS" unquote
                          (string-append
                           "-Djava.util.prefs.userRoot=$XDG_CONFIG_HOME/java "
                           "-Dawt.toolkit.name=WLToolkit"))))))
#+end_src

* Font

** 字体相关配置

#+NAME: font-services
#+begin_src scheme
(list (simple-service 'extend-fontconfig home-fontconfig-service-type
                      (list "~/.local/share/fonts"
                            "/run/current-system/profile/share/fonts"
                            (let ((sans "Sarasa Gothic SC")
                                  (serif "Sarasa Gothic SC")
                                  (mono "Maple Mono NF CN")
                                  (emoji "Noto Color Emoji"))
                              `((alias (family "sans-serif")
                                       (prefer (family ,sans)
                                               (family ,emoji)))
                                (alias (family "serif")
                                       (prefer (family ,serif)
                                               (family ,emoji)))
                                (alias (family "monospace")
                                       (prefer (family ,mono)
                                               (family ,emoji)))
                                (alias (family "emoji")
                                       (prefer (family ,emoji))))))))
#+end_src
